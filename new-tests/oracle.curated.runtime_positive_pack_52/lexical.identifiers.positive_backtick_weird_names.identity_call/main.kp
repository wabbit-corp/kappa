module main

-- Source issue: KT-458
-- Spec: §3.1
`class` : Int
let `class` = 17

`$$$$` : Int
let `$$$$` = 25

baseResult : Int
let baseResult = `class` + `$$$$`

liftResult : Int -> Int
let liftResult value = value

result : Int
let result = liftResult baseResult

--! assertNoErrors
--! assertType result Int
--! assertExecute result 42
