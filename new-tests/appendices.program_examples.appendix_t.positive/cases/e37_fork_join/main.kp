module cases.e37_fork_join.main

main : UIO Unit
let main = do
    let fiber <- fork (pure 42)
    let value <- join fiber
    if value == 42 then println "joined" else println "bad join"

--! assertType cases.e37_fork_join.main.main UIO Unit
