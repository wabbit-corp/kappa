module main

import std.prelude.(type Option, ctor None)

-- Source issue: hylo#1562
-- Spec: §8.2
bad : UIO Unit
let bad = do
    var x : Option Int = None
    x = 1
    return ()

--! assertErrorCount 1
--! assertDiagnosticMatch assignment|Option|Int|type
