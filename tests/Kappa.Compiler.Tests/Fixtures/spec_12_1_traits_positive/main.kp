module main

trait Eq a =
    equals : a -> a -> Bool

trait Show a =
    show : a -> String
    debug : a -> String

--! assertNoErrors
--! assertDeclDescriptors trait Eq, trait Show
--! assertTraitMembers Eq equals
--! assertTraitMembers Show show, debug
