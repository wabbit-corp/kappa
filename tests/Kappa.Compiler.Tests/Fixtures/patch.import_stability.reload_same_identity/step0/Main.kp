module Main

import A.*
import B.*
import C.*

stableProof :
    (o : Ordering) ->
    match o
    case LT -> Unit
    case _  -> nonLT (idOrdering o) = True
let stableProof o =
    match o
    case LT -> ()
    case _  -> refl
