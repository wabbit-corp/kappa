module main

data Buffer : Type =
    Buffer

consume : (1 old : Buffer) -> Unit
let consume old = ()

demo : (1 r : (1 buf : Buffer, len : Nat)) -> Nat
let demo r =
    let old = r.buf
    let used = consume old
    r.len

--! assertNoErrors
