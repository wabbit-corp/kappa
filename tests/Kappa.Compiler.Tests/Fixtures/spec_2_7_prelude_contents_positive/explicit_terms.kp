module explicit_terms

import std.prelude.(not, False, True)

flag : Bool
let flag = not False

--! assertNoErrors
--! assertType flag Bool
--! assertEval flag True
--! assertDeclDescriptors import std.prelude.(not + False + True), signature flag, let flag
