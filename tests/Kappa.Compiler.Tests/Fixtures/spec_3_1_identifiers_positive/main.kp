module main

`type` : Int
let `type` = 42

snake_case : Int
let snake_case = `type`

--! assertNoErrors
--! assertType type Int
--! assertType snake_case Int
--! assertDeclDescriptors signature type, let type, signature snake_case, let snake_case
--! assertContainsTokenKinds Identifier
