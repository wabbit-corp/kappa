module main

import model.data.*
import dsl.sql.*

customersWithOrders : SqlQuery (customer : User, orders : Query Order)
let customersWithOrders =
    SqlQuery [
        for u in users
        left join o in orders on o.userId == u.id into customerOrders
        yield (customer = u, orders = customerOrders)
    ]
