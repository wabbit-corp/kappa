module short_circuit_and

import std.testing.*

result : Int
let result =
    if False && failNow "short-circuit && rhs evaluated"
    then 0
    else 42

--! assertNoErrors
--! assertType result Int
--! x-assertEval result 42
