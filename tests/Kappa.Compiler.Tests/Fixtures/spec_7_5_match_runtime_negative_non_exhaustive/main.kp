module main

headOnly : List Int -> Int
let headOnly xs =
    match xs
    case head :: tail -> head

result : Int
let result = headOnly Nil

--! assertNoErrors
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
--! assertEvalErrorContains result Non-exhaustive match
