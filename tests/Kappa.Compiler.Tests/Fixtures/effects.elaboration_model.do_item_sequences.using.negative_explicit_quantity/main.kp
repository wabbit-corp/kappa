module main

data File : Type =
    Handle Int

let openFile name = pure (Handle 1)

let main : IO Unit = do
    using & file <- openFile "data.txt"
    printInt 0

--! assertErrorCount 1
--! assertDiagnosticCodes E_QTT_USING_EXPLICIT_QUANTITY
