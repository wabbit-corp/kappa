module main_unhide_clarify

import std.rope.(unhide normalizeWorker, clarify Rope)
import std.rope.(unhide term normalizeWorker, clarify type Rope, unhide clarify term normalizeWorker)

value : String
let value = "rope"

--! assertNoErrors
--! assertType value String
--! assertDeclDescriptors import std.rope.(unhide normalizeWorker + clarify Rope), import std.rope.(unhide term normalizeWorker + clarify type Rope + unhide clarify term normalizeWorker), signature value, let value
