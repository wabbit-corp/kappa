module main

data File : Type =
    Handle Int

let leak (& file : File) = \ unit -> file

--! assertErrorCount 1
--! assertDiagnosticCodes E_QTT_BORROW_ESCAPE
--! assertDiagnosticAt main.kp error E_QTT_BORROW_ESCAPE 6
