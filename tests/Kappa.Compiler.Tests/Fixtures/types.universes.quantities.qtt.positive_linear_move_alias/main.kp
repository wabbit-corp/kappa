module main

data File : Type =
    Handle Int

let consume (1 file : File) = let _ = file in ()

let main : IO Unit = do
    let 1 file = Handle 1
    let alias = file
    consume alias

--! assertErrorCount 0
