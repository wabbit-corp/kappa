module main_unpinned_alias

import "https://example.com/lib" as remote

value : String
let value = "alias"

--! assertNoErrors
--! assertType value String
--! assertDeclDescriptors import "https://example.com/lib" as remote, signature value, let value
