module main

data File : Type =
    Handle Int

let consume (1 f : File) = ()

let main : IO Unit = do
    let 1 file = Handle 1
    consume file
    consume file

--! assertErrorCount 1
--! assertDiagnosticCodes E_QTT_LINEAR_OVERUSE
--! assertDiagnosticAt main.kp error E_QTT_LINEAR_OVERUSE 11
