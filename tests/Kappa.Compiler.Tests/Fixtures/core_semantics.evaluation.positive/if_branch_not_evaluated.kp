module if_branch_not_evaluated

import std.testing.*

result : Int
let result =
    if True
    then 42
    else failNow "if else branch evaluated"

--! assertNoErrors
--! assertType result Int
--! x-assertEval result 42
