module main

badOmitConsumed : (1 r : (1 x : Int, y : Int)) -> (1 x : Int, y : Int)
let badOmitConsumed r =
    let
        moved = r.x
    in
        r.{ y = r.y }

--! assertErrorCount 1
--! assertDiagnosticMatch consumed|update|restore|field

