module main

needOwned : (1 x : Int) -> Int
let needOwned x = x

badBorrowAsOwned : (& x : Int) -> Int
let badBorrowAsOwned x = needOwned x

--! assertErrorCount 1
--! assertDiagnosticMatch borrow|owned|quantity

