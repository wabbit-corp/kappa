module user_defined_and_is_eager

infix left 40 (&&)
let (&&) x y = x

result : Bool
let result = False && ((1 / 0) == 0)

--! assertNoErrors
--! assertType result Bool
--! x-assertEvalErrorContains result Division by zero
