module main

data Resource : Type =
    Resource

inspectOnly : (& x : Resource) -> Unit
let inspectOnly x = ()

consume : (1 x : Resource) -> Unit
let consume x =
    let _ = x
    in ()

demo : (1 x : Resource) -> Unit
let demo x =
    let _ = inspectOnly x
    consume x

--! assertNoErrors
