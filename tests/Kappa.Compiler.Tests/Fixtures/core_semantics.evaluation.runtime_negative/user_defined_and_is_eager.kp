module user_defined_and_is_eager

import std.testing.*

infix left 40 (&&)
let (&&) x y = x

result : Bool
let result = False && failNow "user-defined && rhs evaluated"

--! assertNoErrors
--! assertType result Bool
--! x-assertEvalErrorContains result user-defined && rhs evaluated
