module user_defined_and_is_eager

infix left 40 (&&)
let (&&) x y = x

result : Bool
let result = False && (match Nil
                       case _ :: _ -> True)

--! assertNoErrors
--! assertType result Bool
--! x-assertEvalErrorContains result Non-exhaustive match
