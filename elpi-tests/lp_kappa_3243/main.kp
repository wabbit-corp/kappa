module main

generated_3243 : Int
let generated_3243 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_3243

--! assertNoErrors
--! assertType generated_3243 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
