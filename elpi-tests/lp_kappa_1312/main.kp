module main

generated_1312 : Int
let generated_1312 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_1312

--! assertNoErrors
--! assertType generated_1312 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
