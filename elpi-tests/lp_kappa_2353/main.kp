module main

generated_2353 : Int
let generated_2353 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = (block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_2353

--! assertNoErrors
--! assertType generated_2353 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
