module main

generated_4063 : Set Int
let generated_4063 = {| yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) |}

result : Set Int
let result = generated_4063

--! assertNoErrors
--! assertType generated_4063 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
