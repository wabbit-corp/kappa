module main

generated_4498 : Int
let generated_4498 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 0)

        (aliased + 1)

result : Int
let result = generated_4498

--! assertNoErrors
--! assertType generated_4498 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
