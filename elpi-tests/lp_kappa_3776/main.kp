module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3776 : Int
let generated_3776 = ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0).generatedEcho

result : Int
let result = generated_3776

--! assertNoErrors
--! assertType generated_3776 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
