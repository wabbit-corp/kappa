module main

generated_3803 : Int
let generated_3803 =
    match (Some ((block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_3803

--! assertNoErrors
--! assertType generated_3803 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
