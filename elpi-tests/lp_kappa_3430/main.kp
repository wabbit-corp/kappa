module main

generated_3430 : List Int
let generated_3430 = [ for x in [1, 2, 3], skip 1, take 2, yield (match ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = x, y = (x + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) ]

result : List Int
let result = generated_3430

--! assertNoErrors
--! assertType generated_3430 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
