module main

generated_3386 : Int
let generated_3386 =
    match ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) == (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)))
      case True -> (block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other))
      case False -> 0

result : Int
let result = generated_3386

--! assertNoErrors
--! assertType generated_3386 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
