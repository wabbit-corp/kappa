module main

generated_1367 : Int
let generated_1367 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values), y = ((block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1367

--! assertNoErrors
--! assertType generated_1367 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
