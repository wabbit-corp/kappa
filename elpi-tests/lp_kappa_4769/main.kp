module main

generated_4769 : Int
let generated_4769 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4769

--! assertNoErrors
--! assertType generated_4769 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
