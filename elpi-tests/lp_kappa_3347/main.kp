module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_3347 : Int
let generated_3347 = generatedResize { image = (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0), height = 3, width = 2 }

result : Int
let result = generated_3347

--! assertNoErrors
--! assertType generated_3347 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
