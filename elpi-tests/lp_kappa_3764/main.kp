module main

generated_3764 : Int
let generated_3764 =
    block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3764

--! assertNoErrors
--! assertType generated_3764 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
