module main

generated_3431 : List Int
let generated_3431 = [ for x in [1, 2, 3], order by x desc, yield (match ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = x, y = (x + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) ]

result : List Int
let result = generated_3431

--! assertNoErrors
--! assertType generated_3431 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
