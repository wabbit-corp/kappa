module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_2808 : Int
let generated_2808 = ((generatedMakeConst ((block
    let pair = (x = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0)) ())

result : Int
let result = generated_2808

--! assertNoErrors
--! assertType generated_2808 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
