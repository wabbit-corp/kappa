module main

generated_3809 : List Int
let generated_3809 = [ for x in [1, 2, 3], order by x desc, yield (block
    let pair = (x = ((\(n : Int) -> n) x), y = (((\(n : Int) -> n) x) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_3809

--! assertNoErrors
--! assertType generated_3809 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
