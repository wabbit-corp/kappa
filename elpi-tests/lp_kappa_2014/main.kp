module main

generated_2014 : Int
let generated_2014 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = (((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 0)

        (aliased + 1)

result : Int
let result = generated_2014

--! assertNoErrors
--! assertType generated_2014 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
