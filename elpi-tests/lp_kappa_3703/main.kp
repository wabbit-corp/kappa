module main

generated_3703 : Int
let generated_3703 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = (match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0)

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_3703

--! assertNoErrors
--! assertType generated_3703 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
