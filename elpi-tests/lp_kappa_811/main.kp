module main

generated_811 : Set Int
let generated_811 = {| for x in [1, 2, 3], if x > 0, yield (match [(block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]), ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 1)]
  case head :: tail -> head
  case Nil -> 0) |}

result : Set Int
let result = generated_811

--! assertNoErrors
--! assertType generated_811 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
