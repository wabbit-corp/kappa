module main

generated_2801 : List Int
let generated_2801 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    let pair = (x = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_2801

--! assertNoErrors
--! assertType generated_2801 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
