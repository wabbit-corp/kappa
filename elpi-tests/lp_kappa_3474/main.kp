module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3474 : Int
let generated_3474 = generatedNeed @((((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0))

result : Int
let result = generated_3474

--! assertNoErrors
--! assertType generated_3474 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
