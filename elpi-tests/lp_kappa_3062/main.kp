module main

generated_3062 : Int
let generated_3062 =
    block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))
        let values = {|item, (item + 1)|}

        generatedSetCount values

result : Int
let result = generated_3062

--! assertNoErrors
--! assertType generated_3062 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
