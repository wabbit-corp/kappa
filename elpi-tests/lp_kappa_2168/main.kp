module main

generated_2168 : Int
let generated_2168 =
    block
        let base : (x : Int) = (x = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0))
        let extended = base.{ y := ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_2168

--! assertNoErrors
--! assertType generated_2168 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
