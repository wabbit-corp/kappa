module main

generated_1398 : IO Int
let generated_1398 =
    do
        value <- pure (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1398

--! assertNoErrors
--! assertType generated_1398 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
