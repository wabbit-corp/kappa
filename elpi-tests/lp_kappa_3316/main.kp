module main

type GeneratedTuple = (Int, Int)

generated_3316 : Int
let generated_3316 =
    block
        let pair : GeneratedTuple = (((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), ((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3316

--! assertNoErrors
--! assertType generated_3316 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
