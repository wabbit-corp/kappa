module main

generated_2383 : Int
let generated_2383 =
    block
        let pair = (x = (((\(n : Int) -> n) (match (Some 0)
          case Some payload -> payload
          case None -> 0)) + 0), y = (((\(n : Int) -> n) (match (Some 0)
          case Some payload -> payload
          case None -> 0)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_2383

--! assertNoErrors
--! assertType generated_2383 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
