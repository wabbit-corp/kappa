module main

generated_4002 : Int
let generated_4002 =
    match (((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (match (0 == 0)
          case True -> 0
          case False -> 0)

        generatedListLength [item, (item + 1), 0]) + 0) == ((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (match (0 == 0)
          case True -> 0
          case False -> 0)

        generatedListLength [item, (item + 1), 0]) + 0))
      case True -> ((block
          generatedListLength : List Int -> Int
          let generatedListLength xs decreases structural xs =
              match xs
                case Nil -> 0
                case _ :: tail -> 1 + generatedListLength tail

          let item = (match (0 == 0)
            case True -> 0
            case False -> 0)

          generatedListLength [item, (item + 1), 0]) + 0)
      case False -> 0

result : Int
let result = generated_4002

--! assertNoErrors
--! assertType generated_4002 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
