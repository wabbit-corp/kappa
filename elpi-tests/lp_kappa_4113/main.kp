module main

generated_4113 : IO Int
let generated_4113 =
    do
        let staged = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4113

--! assertNoErrors
--! assertType generated_4113 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
