module main

generated_4483 : Set Int
let generated_4483 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = ((Some (x + 1)) ?: (x + 2))

    generatedListLength [item, (item + 1), 0]) |}

result : Set Int
let result = generated_4483

--! assertNoErrors
--! assertType generated_4483 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
