module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_3795 : Int
let generated_3795 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 0)

result : Int
let result = generated_3795

--! assertNoErrors
--! assertType generated_3795 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
