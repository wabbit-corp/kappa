module main

generated_1891 : Set Int
let generated_1891 = {| for x in [1, 2, 3], if x > 0, yield ((\(n : Int) -> n) (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values)) |}

result : Set Int
let result = generated_1891

--! assertNoErrors
--! assertType generated_1891 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
