module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_4589 : Int
let generated_4589 = generatedResize { image = ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((Some (0 + 1)) ?: (0 + 2))
    let values = { yield "item" : item }

    generatedMapCount values) + 0), height = 3, width = 2 }

result : Int
let result = generated_4589

--! assertNoErrors
--! assertType generated_4589 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
