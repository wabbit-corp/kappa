module main

generated_2331 : IO Int
let generated_2331 =
    do
        let staged = ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2331

--! assertNoErrors
--! assertType generated_2331 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
