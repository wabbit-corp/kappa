module main

generated_1845 : IO Int
let generated_1845 =
    do
        let staged = ((block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1845

--! assertNoErrors
--! assertType generated_1845 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
