module main

generated_4286 : Int
let generated_4286 =
    block
        let (captured : Int) = ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        captured

result : Int
let result = generated_4286

--! assertNoErrors
--! assertType generated_4286 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
