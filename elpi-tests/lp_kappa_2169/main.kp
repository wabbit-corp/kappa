module main

generated_2169 : IO Int
let generated_2169 =
    do
        let staged = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2169

--! assertNoErrors
--! assertType generated_2169 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
