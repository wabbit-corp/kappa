module main

generated_2316 : IO Int
let generated_2316 =
    do
        value <- pure ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2316

--! assertNoErrors
--! assertType generated_2316 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
