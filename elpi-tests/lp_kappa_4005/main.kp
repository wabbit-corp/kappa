module main

generated_4005 : IO Int
let generated_4005 =
    do
        let staged = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4005

--! assertNoErrors
--! assertType generated_4005 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
