module main

generated_1433 : List Int
let generated_1433 = [ for x in [1, 2, 3], order by x desc, yield ((\(n : Int) -> n) (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values)) ]

result : List Int
let result = generated_1433

--! assertNoErrors
--! assertType generated_1433 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
