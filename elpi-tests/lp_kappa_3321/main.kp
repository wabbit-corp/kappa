module main

generated_3321 : Map String Int
let generated_3321 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_3321

--! assertNoErrors
--! assertType generated_3321 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
