module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1180 : Int
let generated_1180 =
    block
        let (@x : Int) = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = { yield "item" : item }

            generatedMapCount values)

        if True then generatedNeed else 0

result : Int
let result = generated_1180

--! assertNoErrors
--! assertType generated_1180 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
