module main

generated_3897 : IO Int
let generated_3897 =
    do
        let staged = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3897

--! assertNoErrors
--! assertType generated_3897 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
