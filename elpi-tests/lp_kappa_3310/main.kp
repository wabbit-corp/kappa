module main

generated_3310 : Int
let generated_3310 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        (aliased + 1)

result : Int
let result = generated_3310

--! assertNoErrors
--! assertType generated_3310 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
