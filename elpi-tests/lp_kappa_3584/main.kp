module main

generated_3584 : Int
let generated_3584 =
    block
        let (captured : Int) = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        captured

result : Int
let result = generated_3584

--! assertNoErrors
--! assertType generated_3584 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
