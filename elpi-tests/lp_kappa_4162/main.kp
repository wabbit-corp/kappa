module main

generated_4162 : Int
let generated_4162 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match (Some (match (0 == 0)
          case True -> 0
          case False -> 0))
          case Some payload -> payload
          case None -> 0) + 0)

result : Int
let result = generated_4162

--! assertNoErrors
--! assertType generated_4162 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
