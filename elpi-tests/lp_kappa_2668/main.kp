module main

type GeneratedTuple = (Int, Int)

generated_2668 : Int
let generated_2668 =
    block
        let pair : GeneratedTuple = (((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0), ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_2668

--! assertNoErrors
--! assertType generated_2668 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
