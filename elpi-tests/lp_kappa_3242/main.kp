module main

generated_3242 : Map String Int
let generated_3242 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (match [(block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) }

result : Map String Int
let result = generated_3242

--! assertNoErrors
--! assertType generated_3242 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
