module main

generated_379 : Set Int
let generated_379 = {| for x in [1, 2, 3], if x > 0, yield (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_379

--! assertNoErrors
--! assertType generated_379 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
