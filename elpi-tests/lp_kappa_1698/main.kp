module main

generated_1698 : Int
let generated_1698 =
    match [((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        let values = { yield "item" : item }

        generatedMapCount values) + 0), (((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        let values = { yield "item" : item }

        generatedMapCount values) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1698

--! assertNoErrors
--! assertType generated_1698 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
