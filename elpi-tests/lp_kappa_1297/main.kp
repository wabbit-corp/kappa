module main

generated_1297 : Set Int
let generated_1297 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match [(block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values), ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) |}

result : Set Int
let result = generated_1297

--! assertNoErrors
--! assertType generated_1297 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
