module main

generated_1235 : List Int
let generated_1235 = [ for x in [1, 2, 3], if x > 0, yield (match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) ]

result : List Int
let result = generated_1235

--! assertNoErrors
--! assertType generated_1235 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
