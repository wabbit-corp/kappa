module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3452 : Int
let generated_3452 = (((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0).generatedEcho

result : Int
let result = generated_3452

--! assertNoErrors
--! assertType generated_3452 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
