module main

generated_1452 : IO Int
let generated_1452 =
    do
        value <- pure ((match ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) == (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case True -> (block
              generatedSetCount : Set Int -> Int
              let generatedSetCount values = 2

              let item = 0
              let values = {|item, (item + 1)|}

              generatedSetCount values)
          case False -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1452

--! assertNoErrors
--! assertType generated_1452 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
