module main

generated_2335 : Set Int
let generated_2335 = {| yield ((block
    let pair = (x = (match (Some 0)
      case Some payload -> payload
      case None -> 0), y = ((match (Some 0)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_2335

--! assertNoErrors
--! assertType generated_2335 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
