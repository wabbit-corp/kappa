module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_403 : Int
let generated_403 = ((generatedMakeConst @Int ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0)) ())

result : Int
let result = generated_403

--! assertNoErrors
--! assertType generated_403 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
