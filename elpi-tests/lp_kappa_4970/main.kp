module main

generated_4970 : Map String Int
let generated_4970 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = x

            generatedListLength [item, (item + 1), 0])

        generatedListLength [item, (item + 1), 0])

    generatedListLength [item, (item + 1), 0]) }

result : Map String Int
let result = generated_4970

--! assertNoErrors
--! assertType generated_4970 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
