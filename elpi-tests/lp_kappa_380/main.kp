module main

generated_380 : Map String Int
let generated_380 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_380

--! assertNoErrors
--! assertType generated_380 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
