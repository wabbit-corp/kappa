module main

type GeneratedTuple = (Int, Int)

generated_1858 : Int
let generated_1858 =
    block
        let pair : GeneratedTuple = (((block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), ((block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_1858

--! assertNoErrors
--! assertType generated_1858 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
