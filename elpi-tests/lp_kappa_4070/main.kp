module main

generated_4070 : Int
let generated_4070 =
    block
        let (captured : Int) = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        captured

result : Int
let result = generated_4070

--! assertNoErrors
--! assertType generated_4070 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
