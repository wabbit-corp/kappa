module main

generated_1744 : Int
let generated_1744 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_1744

--! assertNoErrors
--! assertType generated_1744 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
