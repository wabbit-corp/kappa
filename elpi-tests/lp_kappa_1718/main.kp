module main

generated_1718 : Int
let generated_1718 =
    block
        ranked : Int
        let ranked = (match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0)

        (ranked + 0)

result : Int
let result = generated_1718

--! assertNoErrors
--! assertType generated_1718 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
