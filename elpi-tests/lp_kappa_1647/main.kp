module main

generated_1647 : Map String Int
let generated_1647 = { for x in [1, 2, 3], yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values), on conflict keep first }

result : Map String Int
let result = generated_1647

--! assertNoErrors
--! assertType generated_1647 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
