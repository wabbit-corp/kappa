module main

generated_1424 : Int
let generated_1424 =
    block
        let (captured : Int) = (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0)

        captured

result : Int
let result = generated_1424

--! assertNoErrors
--! assertType generated_1424 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
