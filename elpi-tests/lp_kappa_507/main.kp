module main

generated_507 : Int
let generated_507 =
    block
        let pair = (x = ((match (0 == 0)
          case True -> 0
          case False -> 0) + 0), y = ((match (0 == 0)
          case True -> 0
          case False -> 0) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_507

--! assertNoErrors
--! assertType generated_507 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
