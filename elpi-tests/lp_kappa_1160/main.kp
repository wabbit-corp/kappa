module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_1160 : Int
let generated_1160 =
    block
        let width = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_1160

--! assertNoErrors
--! assertType generated_1160 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
