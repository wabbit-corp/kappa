module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_396 : Int
let generated_396 = generatedNeed @(((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0))

result : Int
let result = generated_396

--! assertNoErrors
--! assertType generated_396 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
