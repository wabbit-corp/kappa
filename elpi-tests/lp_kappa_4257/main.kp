module main

generated_4257 : Int
let generated_4257 =
    let
        ranked : Int = (block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))
in
        (ranked + 0)

result : Int
let result = generated_4257

--! assertNoErrors
--! assertType generated_4257 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
