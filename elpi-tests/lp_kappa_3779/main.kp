module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_3779 : Int
let generated_3779 = generatedResize { image = ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0), height = 3, width = 2 }

result : Int
let result = generated_3779

--! assertNoErrors
--! assertType generated_3779 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
