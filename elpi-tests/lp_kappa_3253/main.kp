module main

generated_3253 : Set Int
let generated_3253 = {| yield ((match [(block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) |}

result : Set Int
let result = generated_3253

--! assertNoErrors
--! assertType generated_3253 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
