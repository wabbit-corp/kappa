module main

generated_1405 : Set Int
let generated_1405 = {| for x in [1, 2, 3], let y = x + 1, yield ((\(n : Int) -> n) (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values)) |}

result : Set Int
let result = generated_1405

--! assertNoErrors
--! assertType generated_1405 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
