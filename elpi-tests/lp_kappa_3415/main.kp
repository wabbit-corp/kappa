module main

generated_3415 : Set Int
let generated_3415 = {| yield ((match ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = 0, y = (0 + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) + 0) |}

result : Set Int
let result = generated_3415

--! assertNoErrors
--! assertType generated_3415 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
