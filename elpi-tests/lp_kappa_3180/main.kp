module main

generated_3180 : IO Int
let generated_3180 =
    do
        value <- pure ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3180

--! assertNoErrors
--! assertType generated_3180 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
