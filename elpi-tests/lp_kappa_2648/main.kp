module main

generated_2648 : Map String Int
let generated_2648 = { for x in [1, 2, 3], yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) }

result : Map String Int
let result = generated_2648

--! assertNoErrors
--! assertType generated_2648 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
