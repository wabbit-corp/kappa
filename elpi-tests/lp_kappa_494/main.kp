module main

generated_494 : Int
let generated_494 =
    block
        let base : (x : Int) = (x = ((match (0 == 0)
          case True -> 0
          case False -> 0) + 0))
        let extended = base.{ y := ((match (0 == 0)
          case True -> 0
          case False -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_494

--! assertNoErrors
--! assertType generated_494 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
