module main

generated_4378 : Int
let generated_4378 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match ((match (0 == 0)
          case True -> 0
          case False -> 0) == (match (0 == 0)
          case True -> 0
          case False -> 0))
          case True -> (match (0 == 0)
            case True -> 0
            case False -> 0)
          case False -> 0) + 0)

result : Int
let result = generated_4378

--! assertNoErrors
--! assertType generated_4378 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
