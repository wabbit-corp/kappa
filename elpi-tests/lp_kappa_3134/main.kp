module main

generated_3134 : Map String Int
let generated_3134 = { for x in [1, 2, 3], let y = x + 1, yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) }

result : Map String Int
let result = generated_3134

--! assertNoErrors
--! assertType generated_3134 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
