module main

generated_730 : List Int
let generated_730 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = x

        generatedListLength [item, (item + 1), 0])
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_730

--! assertNoErrors
--! assertType generated_730 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
