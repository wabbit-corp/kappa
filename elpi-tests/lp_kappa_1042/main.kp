module main

generated_1042 : Int
let generated_1042 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = (((Some ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 2)) + 0)

        (aliased + 1)

result : Int
let result = generated_1042

--! assertNoErrors
--! assertType generated_1042 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
