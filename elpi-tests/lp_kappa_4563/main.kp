module main

generated_4563 : Map String Int
let generated_4563 = { for x in [1, 2, 3], yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((Some (x + 1)) ?: (x + 2))
    let values = {|item, (item + 1)|}

    generatedSetCount values), on conflict keep first }

result : Map String Int
let result = generated_4563

--! assertNoErrors
--! assertType generated_4563 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
