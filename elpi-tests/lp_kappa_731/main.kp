module main

generated_731 : List Int
let generated_731 = [ for x in [1, 2, 3], order by x desc, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = x

        generatedListLength [item, (item + 1), 0])
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_731

--! assertNoErrors
--! assertType generated_731 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
