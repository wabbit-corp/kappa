module main

generated_2107 : Set Int
let generated_2107 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (Some x)
      case Some payload -> payload
      case None -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_2107

--! assertNoErrors
--! assertType generated_2107 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
