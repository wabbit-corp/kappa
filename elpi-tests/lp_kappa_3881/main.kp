module main

generated_3881 : List Int
let generated_3881 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match (((\(n : Int) -> n) x) == ((\(n : Int) -> n) x))
  case True -> ((\(n : Int) -> n) x)
  case False -> 0) ]

result : List Int
let result = generated_3881

--! assertNoErrors
--! assertType generated_3881 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
