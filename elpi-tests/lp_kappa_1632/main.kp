module main

generated_1632 : List Int
let generated_1632 = [ yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) ]

result : List Int
let result = generated_1632

--! assertNoErrors
--! assertType generated_1632 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
