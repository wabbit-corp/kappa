module main

generated_2620 : List Int
let generated_2620 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_2620

--! assertNoErrors
--! assertType generated_2620 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
