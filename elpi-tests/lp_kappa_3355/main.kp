module main

generated_3355 : Int
let generated_3355 =
    block
        let pair = (x = (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0), y = (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3355

--! assertNoErrors
--! assertType generated_3355 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
