module main

generated_3202 : Int
let generated_3202 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_3202

--! assertNoErrors
--! assertType generated_3202 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
