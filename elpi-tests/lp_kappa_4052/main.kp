module main

generated_4052 : Map String Int
let generated_4052 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) }

result : Map String Int
let result = generated_4052

--! assertNoErrors
--! assertType generated_4052 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
