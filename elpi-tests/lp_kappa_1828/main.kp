module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1828 : Int
let generated_1828 =
    block
        let (@x : Int) = (block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        if True then generatedNeed else 0

result : Int
let result = generated_1828

--! assertNoErrors
--! assertType generated_1828 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
