module main

generated_3101 : Int
let generated_3101 =
    match (Some ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_3101

--! assertNoErrors
--! assertType generated_3101 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
