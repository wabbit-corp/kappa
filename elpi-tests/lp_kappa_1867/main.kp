module main

generated_1867 : Int
let generated_1867 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = (block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_1867

--! assertNoErrors
--! assertType generated_1867 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
