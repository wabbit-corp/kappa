module main

generated_1648 : List Int
let generated_1648 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_1648

--! assertNoErrors
--! assertType generated_1648 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
