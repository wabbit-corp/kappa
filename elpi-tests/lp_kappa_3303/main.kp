module main

generated_3303 : IO Int
let generated_3303 =
    do
        let staged = ((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3303

--! assertNoErrors
--! assertType generated_3303 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
