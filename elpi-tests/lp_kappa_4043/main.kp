module main

generated_4043 : List Int
let generated_4043 = [ for x in [1, 2, 3], if x > 0, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_4043

--! assertNoErrors
--! assertType generated_4043 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
