module main

generated_1697 : Int
let generated_1697 =
    match (Some ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        let values = { yield "item" : item }

        generatedMapCount values) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_1697

--! assertNoErrors
--! assertType generated_1697 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
