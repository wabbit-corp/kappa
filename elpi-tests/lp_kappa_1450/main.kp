module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1450 : Int
let generated_1450 =
    block
        let (@x : Int) = (match ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) == (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case True -> (block
              generatedSetCount : Set Int -> Int
              let generatedSetCount values = 2

              let item = 0
              let values = {|item, (item + 1)|}

              generatedSetCount values)
          case False -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_1450

--! assertNoErrors
--! assertType generated_1450 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
