module main

generated_3362 : Map String Int
let generated_3362 = { yield "only" : (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0) }

result : Map String Int
let result = generated_3362

--! assertNoErrors
--! assertType generated_3362 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
