module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_990 : Int
let generated_990 = generatedNeed @(((match ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]) == (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]))
  case True -> (block
      generatedListLength : List Int -> Int
      let generatedListLength xs decreases structural xs =
          match xs
            case Nil -> 0
            case _ :: tail -> 1 + generatedListLength tail

      let item = 0

      generatedListLength [item, (item + 1), 0])
  case False -> 0) + 0))

result : Int
let result = generated_990

--! assertNoErrors
--! assertType generated_990 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
