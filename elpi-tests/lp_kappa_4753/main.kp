module main

generated_4753 : Set Int
let generated_4753 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    let pair = (x = ((Some (x + 1)) ?: (x + 2)), y = (((Some (x + 1)) ?: (x + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_4753

--! assertNoErrors
--! assertType generated_4753 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
