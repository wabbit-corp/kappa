module main

type GeneratedUnit = Unit

type GeneratedPair = (left : Int, right : Int)

type GeneratedTuple = (Int, Int)

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_54 : GeneratedPair
let generated_54 = if True then (left = 1, right = 2) else (left = 3, right = 4)

result : GeneratedPair
let result = generated_54

--! assertNoErrors
--! assertType generated_54 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, type, type, data, trait, instance, signature, let, signature, let
