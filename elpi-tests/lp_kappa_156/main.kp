module main

generated_156 : IO Int
let generated_156 =
    do
        value <- pure ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_156

--! assertNoErrors
--! assertType generated_156 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
