module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_3451 : Int
let generated_3451 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)

result : Int
let result = generated_3451

--! assertNoErrors
--! assertType generated_3451 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
