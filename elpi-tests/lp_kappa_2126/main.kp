module main

generated_2126 : Int
let generated_2126 =
    block
        let (captured : Int) = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        captured

result : Int
let result = generated_2126

--! assertNoErrors
--! assertType generated_2126 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
