module main

generated_1054 : List Int
let generated_1054 = [ for x in [1, 2, 3], skip 1, take 2, yield ((Some ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 2)) ]

result : List Int
let result = generated_1054

--! assertNoErrors
--! assertType generated_1054 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
