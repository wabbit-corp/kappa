module main

generated_1686 : List Int
let generated_1686 = [ yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) ]

result : List Int
let result = generated_1686

--! assertNoErrors
--! assertType generated_1686 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
