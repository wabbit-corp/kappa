module main

generated_4702 : Int
let generated_4702 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match [((Some (0 + 1)) ?: (0 + 2)), (((Some (0 + 1)) ?: (0 + 2)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_4702

--! assertNoErrors
--! assertType generated_4702 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
