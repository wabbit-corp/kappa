module main

type GeneratedTuple = (Int, Int)

generated_454 : Int
let generated_454 =
    block
        let pair : GeneratedTuple = ((((\(n : Int) -> n) 0) + 0), (((\(n : Int) -> n) 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_454

--! assertNoErrors
--! assertType generated_454 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
