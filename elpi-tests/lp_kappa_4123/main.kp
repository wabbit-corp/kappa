module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_4123 : Option Int
let generated_4123 = (Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0))?.generatedEcho

result : Option Int
let result = generated_4123

--! assertNoErrors
--! assertType generated_4123 Option Int
--! assertType result Option Int
--! assertDeclKinds signature, let, signature, let, signature, let
