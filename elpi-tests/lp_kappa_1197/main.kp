module main

generated_1197 : IO Int
let generated_1197 =
    do
        let staged = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1197

--! assertNoErrors
--! assertType generated_1197 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
