module main

generated_4024 : List Int
let generated_4024 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (match (x == x)
      case True -> x
      case False -> 0)

    generatedListLength [item, (item + 1), 0]) ]

result : List Int
let result = generated_4024

--! assertNoErrors
--! assertType generated_4024 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
