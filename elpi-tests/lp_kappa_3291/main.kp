module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_3291 : Int
let generated_3291 =
    match (GeneratedLeft ((block
        let pair = (x = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), y = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          let pair = (x = (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other)), y = ((block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other)) + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 0)

result : Int
let result = generated_3291

--! assertNoErrors
--! assertType generated_3291 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
