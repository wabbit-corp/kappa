module main

generated_3794 : Map String Int
let generated_3794 = { yield "only" : ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) }

result : Map String Int
let result = generated_3794

--! assertNoErrors
--! assertType generated_3794 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
