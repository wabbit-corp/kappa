module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3880 : Int
let generated_3880 =
    block
        let (@x : Int) = (match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_3880

--! assertNoErrors
--! assertType generated_3880 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
