module main

generated_3746 : Int
let generated_3746 =
    block
        let (captured : Int) = ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

        captured

result : Int
let result = generated_3746

--! assertNoErrors
--! assertType generated_3746 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
