module main

generated_2675 : List Int
let generated_2675 = [ for x in [1, 2, 3], order by x desc, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_2675

--! assertNoErrors
--! assertType generated_2675 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
