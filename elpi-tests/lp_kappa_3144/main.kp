module main

generated_3144 : List Int
let generated_3144 = [ yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) + 0) ]

result : List Int
let result = generated_3144

--! assertNoErrors
--! assertType generated_3144 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
