module main

generated_1914 : Int
let generated_1914 =
    match [(((\(n : Int) -> n) (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)) + 0), ((((\(n : Int) -> n) (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1914

--! assertNoErrors
--! assertType generated_1914 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
