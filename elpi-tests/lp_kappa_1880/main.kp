module main

generated_1880 : Int
let generated_1880 =
    block
        ranked : Int
        let ranked = ((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))

        (ranked + 0)

result : Int
let result = generated_1880

--! assertNoErrors
--! assertType generated_1880 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
