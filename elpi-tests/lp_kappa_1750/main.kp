module main

type GeneratedTuple = (Int, Int)

generated_1750 : Int
let generated_1750 =
    block
        let pair : GeneratedTuple = (((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0), ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_1750

--! assertNoErrors
--! assertType generated_1750 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
