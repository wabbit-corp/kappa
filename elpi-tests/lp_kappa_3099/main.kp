module main

generated_3099 : Int
let generated_3099 =
    block
        let pair = (x = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0), y = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3099

--! assertNoErrors
--! assertType generated_3099 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
