module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_2643 : Int
let generated_2643 =
    match (GeneratedLeft ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = { yield "item" : item }

        generatedMapCount values) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = (match [0, (0 + 1)]
            case head :: tail -> head
            case Nil -> 0)
          let values = { yield "item" : item }

          generatedMapCount values) + 0)

result : Int
let result = generated_2643

--! assertNoErrors
--! assertType generated_2643 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
