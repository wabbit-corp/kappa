module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_3181 : Int
let generated_3181 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)

result : Int
let result = generated_3181

--! assertNoErrors
--! assertType generated_3181 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
