module main

generated_220 : Int
let generated_220 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_220

--! assertNoErrors
--! assertType generated_220 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
