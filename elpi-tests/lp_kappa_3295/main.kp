module main

generated_3295 : Set Int
let generated_3295 = {| for x in [1, 2, 3], yield (block
    let pair = (x = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_3295

--! assertNoErrors
--! assertType generated_3295 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
