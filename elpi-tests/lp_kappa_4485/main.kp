module main

generated_4485 : Int
let generated_4485 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 0)

result : Int
let result = generated_4485

--! assertNoErrors
--! assertType generated_4485 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
