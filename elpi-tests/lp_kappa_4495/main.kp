module main

generated_4495 : Set Int
let generated_4495 = {| yield ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = ((Some (0 + 1)) ?: (0 + 2))

    generatedListLength [item, (item + 1), 0]) + 0) |}

result : Set Int
let result = generated_4495

--! assertNoErrors
--! assertType generated_4495 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
