module main

generated_4259 : List Int
let generated_4259 = [ for x in [1, 2, 3], if x > 0, yield (block
    let pair = (x = (match (x == x)
      case True -> x
      case False -> 0), y = ((match (x == x)
      case True -> x
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_4259

--! assertNoErrors
--! assertType generated_4259 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
