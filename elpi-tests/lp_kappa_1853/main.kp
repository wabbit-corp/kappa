module main

generated_1853 : Int
let generated_1853 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1853

--! assertNoErrors
--! assertType generated_1853 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
