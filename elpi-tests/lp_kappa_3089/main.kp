module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3089 : Int
let generated_3089 = ((.generatedEcho) ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0))

result : Int
let result = generated_3089

--! assertNoErrors
--! assertType generated_3089 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
