module main

generated_4131 : Map String Int
let generated_4131 = { for x in [1, 2, 3], yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values), on conflict keep first }

result : Map String Int
let result = generated_4131

--! assertNoErrors
--! assertType generated_4131 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
