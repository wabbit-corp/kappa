module main

generated_1902 : List Int
let generated_1902 = [ yield (((\(n : Int) -> n) (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values)) + 0) ]

result : List Int
let result = generated_1902

--! assertNoErrors
--! assertType generated_1902 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
