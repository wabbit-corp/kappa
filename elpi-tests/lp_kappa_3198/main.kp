module main

generated_3198 : List Int
let generated_3198 = [ yield ((match (Some (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) + 0) ]

result : List Int
let result = generated_3198

--! assertNoErrors
--! assertType generated_3198 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
