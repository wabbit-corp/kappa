module main

generated_920 : Map String Int
let generated_920 = { for x in [1, 2, 3], yield "k" : ((\(n : Int) -> n) (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0])) }

result : Map String Int
let result = generated_920

--! assertNoErrors
--! assertType generated_920 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
