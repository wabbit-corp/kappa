module main

generated_3365 : Int
let generated_3365 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3365

--! assertNoErrors
--! assertType generated_3365 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
