module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4068 : Int
let generated_4068 = generatedNeed @(((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0))

result : Int
let result = generated_4068

--! assertNoErrors
--! assertType generated_4068 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
