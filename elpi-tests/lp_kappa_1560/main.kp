module main

generated_1560 : IO Int
let generated_1560 =
    do
        value <- pure ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1560

--! assertNoErrors
--! assertType generated_1560 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
