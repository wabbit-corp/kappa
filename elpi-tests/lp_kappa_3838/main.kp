module main

generated_3838 : Int
let generated_3838 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0)

result : Int
let result = generated_3838

--! assertNoErrors
--! assertType generated_3838 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
