module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_4101 : Int
let generated_4101 =
    match (GeneratedLeft ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (match (0 == 0)
          case True -> 0
          case False -> 0)
        let values = { yield "item" : item }

        generatedMapCount values) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = (match (0 == 0)
            case True -> 0
            case False -> 0)
          let values = { yield "item" : item }

          generatedMapCount values) + 0)

result : Int
let result = generated_4101

--! assertNoErrors
--! assertType generated_4101 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
