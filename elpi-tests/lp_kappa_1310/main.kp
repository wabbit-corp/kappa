module main

generated_1310 : Map String Int
let generated_1310 = { yield "only" : ((match [(block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values), ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) }

result : Map String Int
let result = generated_1310

--! assertNoErrors
--! assertType generated_1310 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
