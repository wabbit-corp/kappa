module main

generated_3146 : Map String Int
let generated_3146 = { yield "only" : ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) + 0) }

result : Map String Int
let result = generated_3146

--! assertNoErrors
--! assertType generated_3146 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
