module main

generated_2324 : Map String Int
let generated_2324 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    let pair = (x = (match (Some x)
      case Some payload -> payload
      case None -> 0), y = ((match (Some x)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_2324

--! assertNoErrors
--! assertType generated_2324 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
