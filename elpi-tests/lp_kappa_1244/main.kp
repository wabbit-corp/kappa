module main

generated_1244 : Map String Int
let generated_1244 = { for x in [1, 2, 3], if x > 0, yield "k" : (match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) }

result : Map String Int
let result = generated_1244

--! assertNoErrors
--! assertType generated_1244 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
