module main

type GeneratedPair = (left : Int, right : Int)

generated_376 : GeneratedPair
let generated_376 = ((left = (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)), right = 0)).{ right = ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1) }

result : GeneratedPair
let result = generated_376

--! assertNoErrors
--! assertType generated_376 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, signature, let, signature, let
