module main

generated_1039 : Set Int
let generated_1039 = {| yield (((Some ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]) + 2)) + 0) |}

result : Set Int
let result = generated_1039

--! assertNoErrors
--! assertType generated_1039 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
