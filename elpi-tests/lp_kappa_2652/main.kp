module main

generated_2652 : Int
let generated_2652 =
    match (((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = { yield "item" : item }

        generatedMapCount values) + 0) == ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = { yield "item" : item }

        generatedMapCount values) + 0))
      case True -> ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = (match [0, (0 + 1)]
            case head :: tail -> head
            case Nil -> 0)
          let values = { yield "item" : item }

          generatedMapCount values) + 0)
      case False -> 0

result : Int
let result = generated_2652

--! assertNoErrors
--! assertType generated_2652 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
