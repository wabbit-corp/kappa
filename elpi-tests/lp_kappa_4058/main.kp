module main

generated_4058 : Int
let generated_4058 =
    block
        let base : (x : Int) = (x = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0))
        let extended = base.{ y := ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_4058

--! assertNoErrors
--! assertType generated_4058 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
