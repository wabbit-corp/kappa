module main

generated_1644 : Int
let generated_1644 =
    match [((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0), (((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1644

--! assertNoErrors
--! assertType generated_1644 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
