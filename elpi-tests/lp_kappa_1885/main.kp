module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_1885 : Int
let generated_1885 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0)

result : Int
let result = generated_1885

--! assertNoErrors
--! assertType generated_1885 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
