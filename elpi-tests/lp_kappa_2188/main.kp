module main

generated_2188 : List Int
let generated_2188 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (Some x)
      case Some payload -> payload
      case None -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_2188

--! assertNoErrors
--! assertType generated_2188 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
