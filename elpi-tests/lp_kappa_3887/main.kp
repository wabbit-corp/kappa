module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_3887 : Int
let generated_3887 = generatedResize { image = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
  case True -> ((\(n : Int) -> n) 0)
  case False -> 0) + 0), height = 3, width = 2 }

result : Int
let result = generated_3887

--! assertNoErrors
--! assertType generated_3887 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
