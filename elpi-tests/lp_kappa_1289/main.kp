module main

generated_1289 : List Int
let generated_1289 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match [(block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values), ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) ]

result : List Int
let result = generated_1289

--! assertNoErrors
--! assertType generated_1289 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
