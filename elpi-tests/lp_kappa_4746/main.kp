module main

generated_4746 : IO Int
let generated_4746 =
    do
        value <- pure ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_4746

--! assertNoErrors
--! assertType generated_4746 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
