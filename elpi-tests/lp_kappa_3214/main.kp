module main

generated_3214 : List Int
let generated_3214 = [ for x in [1, 2, 3], skip 1, take 2, yield (match (Some (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) ]

result : List Int
let result = generated_3214

--! assertNoErrors
--! assertType generated_3214 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
