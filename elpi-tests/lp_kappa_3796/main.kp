module main

generated_3796 : Int
let generated_3796 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        (aliased + 1)

result : Int
let result = generated_3796

--! assertNoErrors
--! assertType generated_3796 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
