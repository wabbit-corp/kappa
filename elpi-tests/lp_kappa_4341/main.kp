module main

generated_4341 : Int
let generated_4341 =
    block
        let pair = (x = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0), y = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_4341

--! assertNoErrors
--! assertType generated_4341 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
