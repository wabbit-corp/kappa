module main

generated_3051 : Map String Int
let generated_3051 = { for x in [1, 2, 3], yield "k" : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))

    generatedListLength [item, (item + 1), 0]), on conflict keep first }

result : Map String Int
let result = generated_3051

--! assertNoErrors
--! assertType generated_3051 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
