module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_4747 : Int
let generated_4747 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

result : Int
let result = generated_4747

--! assertNoErrors
--! assertType generated_4747 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
