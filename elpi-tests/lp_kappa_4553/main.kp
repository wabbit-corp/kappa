module main

generated_4553 : Int
let generated_4553 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4553

--! assertNoErrors
--! assertType generated_4553 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
