module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3286 : Int
let generated_3286 =
    block
        let (@x : Int) = (block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        if True then generatedNeed else 0

result : Int
let result = generated_3286

--! assertNoErrors
--! assertType generated_3286 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
