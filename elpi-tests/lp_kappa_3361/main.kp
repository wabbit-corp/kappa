module main

generated_3361 : Set Int
let generated_3361 = {| yield (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0) |}

result : Set Int
let result = generated_3361

--! assertNoErrors
--! assertType generated_3361 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
