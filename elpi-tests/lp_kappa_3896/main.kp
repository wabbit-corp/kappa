module main

generated_3896 : Int
let generated_3896 =
    block
        let base : (x : Int) = (x = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0))
        let extended = base.{ y := ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3896

--! assertNoErrors
--! assertType generated_3896 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
