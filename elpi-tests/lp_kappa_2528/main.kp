module main

generated_2528 : Int
let generated_2528 =
    block
        ranked : Int
        let ranked = (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)

            generatedListLength [item, (item + 1), 0])

        (ranked + 0)

result : Int
let result = generated_2528

--! assertNoErrors
--! assertType generated_2528 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
