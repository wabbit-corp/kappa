module main

generated_1287 : Int
let generated_1287 =
    let
        ranked : Int = (match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0)
in
        (ranked + 0)

result : Int
let result = generated_1287

--! assertNoErrors
--! assertType generated_1287 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
