module main

generated_3602 : Int
let generated_3602 =
    block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = ((\(n : Int) -> n) 0)
        let values = { yield "item" : item }

        generatedMapCount values

result : Int
let result = generated_3602

--! assertNoErrors
--! assertType generated_3602 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
