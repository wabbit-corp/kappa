module main

generated_1953 : IO Int
let generated_1953 =
    do
        let staged = ((match ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) == (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case True -> (block
              generatedMapCount : Map String Int -> Int
              let generatedMapCount values = 1

              let item = 0
              let values = { yield "item" : item }

              generatedMapCount values)
          case False -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1953

--! assertNoErrors
--! assertType generated_1953 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
