module main

generated_3094 : Int
let generated_3094 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        (aliased + 1)

result : Int
let result = generated_3094

--! assertNoErrors
--! assertType generated_3094 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
