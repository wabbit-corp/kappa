module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3798 : Int
let generated_3798 = generatedNeed @(((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0))

result : Int
let result = generated_3798

--! assertNoErrors
--! assertType generated_3798 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
