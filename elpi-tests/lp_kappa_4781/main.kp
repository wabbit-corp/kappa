module main

generated_4781 : List Int
let generated_4781 = [ for x in [1, 2, 3], order by x desc, yield (block
    let pair = (x = ((Some (x + 1)) ?: (x + 2)), y = (((Some (x + 1)) ?: (x + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_4781

--! assertNoErrors
--! assertType generated_4781 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
