module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4284 : Int
let generated_4284 = generatedNeed @(((block
    let pair = (x = (match (0 == 0)
      case True -> 0
      case False -> 0), y = ((match (0 == 0)
      case True -> 0
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0))

result : Int
let result = generated_4284

--! assertNoErrors
--! assertType generated_4284 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
