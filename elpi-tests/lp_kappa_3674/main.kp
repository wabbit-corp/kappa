module main

generated_3674 : Map String Int
let generated_3674 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (match (Some ((\(n : Int) -> n) x))
  case Some payload -> payload
  case None -> 0) }

result : Map String Int
let result = generated_3674

--! assertNoErrors
--! assertType generated_3674 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
