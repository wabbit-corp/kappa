module main

generated_3554 : Int
let generated_3554 =
    block
        ranked : Int
        let ranked = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)

        (ranked + 0)

result : Int
let result = generated_3554

--! assertNoErrors
--! assertType generated_3554 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
