module main

generated_812 : Map String Int
let generated_812 = { for x in [1, 2, 3], if x > 0, yield "k" : (match [(block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]), ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 1)]
  case head :: tail -> head
  case Nil -> 0) }

result : Map String Int
let result = generated_812

--! assertNoErrors
--! assertType generated_812 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
