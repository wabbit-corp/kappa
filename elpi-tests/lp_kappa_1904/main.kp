module main

generated_1904 : Map String Int
let generated_1904 = { yield "only" : (((\(n : Int) -> n) (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values)) + 0) }

result : Map String Int
let result = generated_1904

--! assertNoErrors
--! assertType generated_1904 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
