module main

generated_449 : Int
let generated_449 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_449

--! assertNoErrors
--! assertType generated_449 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
