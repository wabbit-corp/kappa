module main

generated_1526 : Map String Int
let generated_1526 = { yield "only" : (((Some ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)) ?: ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 2)) + 0) }

result : Map String Int
let result = generated_1526

--! assertNoErrors
--! assertType generated_1526 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
