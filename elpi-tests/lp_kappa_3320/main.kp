module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3320 : Int
let generated_3320 =
    block
        let width = ((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3320

--! assertNoErrors
--! assertType generated_3320 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
