module main

generated_3843 : IO Int
let generated_3843 =
    do
        let staged = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3843

--! assertNoErrors
--! assertType generated_3843 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
