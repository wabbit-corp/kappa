module main

generated_2639 : List Int
let generated_2639 = [ for x in [1, 2, 3], yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_2639

--! assertNoErrors
--! assertType generated_2639 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
