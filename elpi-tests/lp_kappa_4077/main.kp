module main

generated_4077 : Map String Int
let generated_4077 = { for x in [1, 2, 3], yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values), on conflict keep first }

result : Map String Int
let result = generated_4077

--! assertNoErrors
--! assertType generated_4077 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
