module main

type GeneratedTuple = (Int, Int)

generated_3748 : Int
let generated_3748 =
    block
        let pair : GeneratedTuple = (((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0), ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3748

--! assertNoErrors
--! assertType generated_3748 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
