module main

generated_3352 : Int
let generated_3352 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0)

result : Int
let result = generated_3352

--! assertNoErrors
--! assertType generated_3352 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
