module main

generated_2879 : Int
let generated_2879 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_2879

--! assertNoErrors
--! assertType generated_2879 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
