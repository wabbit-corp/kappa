module main

generated_2380 : Int
let generated_2380 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) (match (Some 0)
          case Some payload -> payload
          case None -> 0)) + 0)

result : Int
let result = generated_2380

--! assertNoErrors
--! assertType generated_2380 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
