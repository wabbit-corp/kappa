module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_3471 : Int
let generated_3471 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> (((Some ((block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 1)) ?: ((block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 2)) + 0)

result : Int
let result = generated_3471

--! assertNoErrors
--! assertType generated_3471 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
