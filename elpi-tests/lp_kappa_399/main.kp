module main

generated_399 : Int
let generated_399 =
    block
        let pair = (x = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), y = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_399

--! assertNoErrors
--! assertType generated_399 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
