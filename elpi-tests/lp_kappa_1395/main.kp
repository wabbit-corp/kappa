module main

generated_1395 : Int
let generated_1395 =
    let
        ranked : Int = ((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
in
        (ranked + 0)

result : Int
let result = generated_1395

--! assertNoErrors
--! assertType generated_1395 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
