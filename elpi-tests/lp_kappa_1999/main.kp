module main

generated_1999 : Set Int
let generated_1999 = {| for x in [1, 2, 3], yield ((Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 1)) ?: ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 2)) |}

result : Set Int
let result = generated_1999

--! assertNoErrors
--! assertType generated_1999 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
