module main

generated_3729 : Int
let generated_3729 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_3729

--! assertNoErrors
--! assertType generated_3729 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
