module main

generated_1675 : Set Int
let generated_1675 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_1675

--! assertNoErrors
--! assertType generated_1675 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
