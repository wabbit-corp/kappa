module main

generated_1189 : Set Int
let generated_1189 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = x
        let values = {|item, (item + 1)|}

        generatedSetCount values)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_1189

--! assertNoErrors
--! assertType generated_1189 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
