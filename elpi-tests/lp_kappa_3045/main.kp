module main

generated_3045 : Int
let generated_3045 =
    block
        let pair = (x = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))

            generatedListLength [item, (item + 1), 0]) + 0), y = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))

            generatedListLength [item, (item + 1), 0]) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3045

--! assertNoErrors
--! assertType generated_3045 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
