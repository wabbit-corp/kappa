module main

generated_3248 : Int
let generated_3248 =
    block
        let base : (x : Int) = (x = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0))
        let extended = base.{ y := ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3248

--! assertNoErrors
--! assertType generated_3248 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
