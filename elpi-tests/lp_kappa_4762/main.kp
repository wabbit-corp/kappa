module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4762 : IO Int
let generated_4762 =
    do
        let (@x : Int) = ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_4762

--! assertNoErrors
--! assertType generated_4762 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
