module main

generated_1520 : Int
let generated_1520 =
    block
        let base : (x : Int) = (x = (((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2)) + 0))
        let extended = base.{ y := (((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_1520

--! assertNoErrors
--! assertType generated_1520 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
