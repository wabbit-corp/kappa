module main

generated_2595 : Int
let generated_2595 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

result : Int
let result = generated_2595

--! assertNoErrors
--! assertType generated_2595 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
