module main

generated_4239 : Map String Int
let generated_4239 = { for x in [1, 2, 3], yield "k" : (match [(match (x == x)
  case True -> x
  case False -> 0), ((match (x == x)
  case True -> x
  case False -> 0) + 1)]
  case head :: tail -> head
  case Nil -> 0), on conflict keep first }

result : Map String Int
let result = generated_4239

--! assertNoErrors
--! assertType generated_4239 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
