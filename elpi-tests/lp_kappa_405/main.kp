module main

generated_405 : Map String Int
let generated_405 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_405

--! assertNoErrors
--! assertType generated_405 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
