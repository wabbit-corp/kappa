module main

type GeneratedTuple = (Int, Int)

generated_3910 : Int
let generated_3910 =
    block
        let pair : GeneratedTuple = (((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0), ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3910

--! assertNoErrors
--! assertType generated_3910 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
