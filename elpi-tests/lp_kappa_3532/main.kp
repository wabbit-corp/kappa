module main

type GeneratedTuple = (Int, Int)

generated_3532 : Int
let generated_3532 =
    block
        let pair : GeneratedTuple = (((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((\(n : Int) -> n) 0)

            generatedListLength [item, (item + 1), 0]) + 0), ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((\(n : Int) -> n) 0)

            generatedListLength [item, (item + 1), 0]) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3532

--! assertNoErrors
--! assertType generated_3532 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
