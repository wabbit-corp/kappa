module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3806 : Int
let generated_3806 =
    block
        let width = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3806

--! assertNoErrors
--! assertType generated_3806 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
