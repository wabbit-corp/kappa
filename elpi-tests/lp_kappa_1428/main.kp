module main

generated_1428 : Int
let generated_1428 =
    match [(((\(n : Int) -> n) (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = 0
        let values = {|item, (item + 1)|}

        generatedSetCount values)) + 0), ((((\(n : Int) -> n) (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = 0
        let values = {|item, (item + 1)|}

        generatedSetCount values)) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1428

--! assertNoErrors
--! assertType generated_1428 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
