module main

generated_750 : IO Int
let generated_750 =
    do
        value <- pure ((match (Some (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_750

--! assertNoErrors
--! assertType generated_750 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
