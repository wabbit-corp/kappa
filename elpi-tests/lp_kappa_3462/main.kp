module main

generated_3462 : Int
let generated_3462 =
    match ((((Some ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1)) ?: ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 2)) + 0) == (((Some ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1)) ?: ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 2)) + 0))
      case True -> (((Some ((block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 1)) ?: ((block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 2)) + 0)
      case False -> 0

result : Int
let result = generated_3462

--! assertNoErrors
--! assertType generated_3462 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
