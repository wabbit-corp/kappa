module main

type GeneratedTuple = (Int, Int)

generated_238 : Int
let generated_238 =
    block
        let pair : GeneratedTuple = (((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 0), ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_238

--! assertNoErrors
--! assertType generated_238 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
