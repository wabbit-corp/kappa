module main

type GeneratedTuple = (Int, Int)

generated_4342 : Int
let generated_4342 =
    block
        let pair : GeneratedTuple = ((((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0), (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_4342

--! assertNoErrors
--! assertType generated_4342 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
