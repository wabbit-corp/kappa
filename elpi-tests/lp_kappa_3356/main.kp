module main

generated_3356 : Int
let generated_3356 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3356

--! assertNoErrors
--! assertType generated_3356 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
