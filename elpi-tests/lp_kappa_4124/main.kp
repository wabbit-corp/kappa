module main

generated_4124 : Int
let generated_4124 =
    block
        let (captured : Int) = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        captured

result : Int
let result = generated_4124

--! assertNoErrors
--! assertType generated_4124 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
