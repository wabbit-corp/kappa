module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3150 : Int
let generated_3150 = generatedNeed @(((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) + 0))

result : Int
let result = generated_3150

--! assertNoErrors
--! assertType generated_3150 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
