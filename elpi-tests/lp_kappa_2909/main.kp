module main

generated_2909 : List Int
let generated_2909 = [ for x in [1, 2, 3], let y = x + 1, yield (match ((match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) == (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0))
  case True -> (match [x, (x + 1)]
    case head :: tail -> head
    case Nil -> 0)
  case False -> 0) ]

result : List Int
let result = generated_2909

--! assertNoErrors
--! assertType generated_2909 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
