module main

generated_4545 : IO Int
let generated_4545 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4545

--! assertNoErrors
--! assertType generated_4545 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
