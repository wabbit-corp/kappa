module main

generated_4754 : Map String Int
let generated_4754 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (block
    let pair = (x = ((Some (x + 1)) ?: (x + 2)), y = (((Some (x + 1)) ?: (x + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_4754

--! assertNoErrors
--! assertType generated_4754 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
