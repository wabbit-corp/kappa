module main

generated_3807 : Map String Int
let generated_3807 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = ((\(n : Int) -> n) x), y = (((\(n : Int) -> n) x) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_3807

--! assertNoErrors
--! assertType generated_3807 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
