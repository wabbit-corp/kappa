module main

generated_3449 : List Int
let generated_3449 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield ((Some ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) ]

result : List Int
let result = generated_3449

--! assertNoErrors
--! assertType generated_3449 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
