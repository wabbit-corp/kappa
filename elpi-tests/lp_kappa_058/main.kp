module main

generated_58 : Int
let generated_58 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (0 + 0)

result : Int
let result = generated_58

--! assertNoErrors
--! assertType generated_58 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
