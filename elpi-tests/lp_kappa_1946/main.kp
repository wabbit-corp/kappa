module main

generated_1946 : Map String Int
let generated_1946 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = x
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0) }

result : Map String Int
let result = generated_1946

--! assertNoErrors
--! assertType generated_1946 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
