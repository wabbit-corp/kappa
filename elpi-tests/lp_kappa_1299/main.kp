module main

generated_1299 : Int
let generated_1299 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_1299

--! assertNoErrors
--! assertType generated_1299 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
