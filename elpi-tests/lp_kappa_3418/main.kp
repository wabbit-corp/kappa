module main

generated_3418 : Int
let generated_3418 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_3418

--! assertNoErrors
--! assertType generated_3418 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
