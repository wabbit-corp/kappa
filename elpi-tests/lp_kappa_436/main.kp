module main

generated_436 : Int
let generated_436 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) 0) + 0)

result : Int
let result = generated_436

--! assertNoErrors
--! assertType generated_436 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
