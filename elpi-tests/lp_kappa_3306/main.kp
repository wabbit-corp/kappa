module main

generated_3306 : List Int
let generated_3306 = [ yield ((block
    let pair = (x = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) ]

result : List Int
let result = generated_3306

--! assertNoErrors
--! assertType generated_3306 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
