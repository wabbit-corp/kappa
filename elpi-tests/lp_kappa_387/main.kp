module main

generated_387 : IO Int
let generated_387 =
    do
        let staged = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_387

--! assertNoErrors
--! assertType generated_387 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
