module main

generated_1305 : IO Int
let generated_1305 =
    do
        let staged = ((match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1305

--! assertNoErrors
--! assertType generated_1305 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
