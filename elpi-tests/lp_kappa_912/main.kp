module main

generated_912 : IO Int
let generated_912 =
    do
        value <- pure (((\(n : Int) -> n) (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0])) + 0)
        pure (value + 2)

result : IO Int
let result = generated_912

--! assertNoErrors
--! assertType generated_912 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
