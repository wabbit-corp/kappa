module main

generated_406 : List Int
let generated_406 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_406

--! assertNoErrors
--! assertType generated_406 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
