module main

generated_3260 : Int
let generated_3260 =
    block
        let (captured : Int) = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

        captured

result : Int
let result = generated_3260

--! assertNoErrors
--! assertType generated_3260 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
