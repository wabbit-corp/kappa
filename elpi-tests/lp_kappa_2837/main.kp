module main

generated_2837 : List Int
let generated_2837 = [ for x in [1, 2, 3], order by x desc, yield (block
    let pair = (x = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_2837

--! assertNoErrors
--! assertType generated_2837 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
