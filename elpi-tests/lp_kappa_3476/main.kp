module main

generated_3476 : Int
let generated_3476 =
    block
        let (captured : Int) = (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)

        captured

result : Int
let result = generated_3476

--! assertNoErrors
--! assertType generated_3476 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
