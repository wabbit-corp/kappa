module main

type GeneratedTuple = (Int, Int)

generated_3964 : Int
let generated_3964 =
    block
        let pair : GeneratedTuple = ((((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 0), (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3964

--! assertNoErrors
--! assertType generated_3964 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
