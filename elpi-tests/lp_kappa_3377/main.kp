module main

generated_3377 : List Int
let generated_3377 = [ for x in [1, 2, 3], order by x desc, yield ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))) ]

result : List Int
let result = generated_3377

--! assertNoErrors
--! assertType generated_3377 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
