module main

generated_1411 : Int
let generated_1411 =
    block
        let pair = (x = (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0), y = (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_1411

--! assertNoErrors
--! assertType generated_1411 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
