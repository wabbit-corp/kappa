module main

generated_4977 : IO Int
let generated_4977 =
    do
        let staged = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                generatedListLength : List Int -> Int
                let generatedListLength xs decreases structural xs =
                    match xs
                      case Nil -> 0
                      case _ :: tail -> 1 + generatedListLength tail

                let item = (block
                    generatedListLength : List Int -> Int
                    let generatedListLength xs decreases structural xs =
                        match xs
                          case Nil -> 0
                          case _ :: tail -> 1 + generatedListLength tail

                    let item = 0

                    generatedListLength [item, (item + 1), 0])

                generatedListLength [item, (item + 1), 0])

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4977

--! assertNoErrors
--! assertType generated_4977 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
