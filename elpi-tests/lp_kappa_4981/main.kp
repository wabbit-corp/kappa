module main

generated_4981 : Set Int
let generated_4981 = {| yield ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0])

        generatedListLength [item, (item + 1), 0])

    generatedListLength [item, (item + 1), 0]) + 0) |}

result : Set Int
let result = generated_4981

--! assertNoErrors
--! assertType generated_4981 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
