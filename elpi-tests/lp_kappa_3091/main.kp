module main

generated_3091 : Set Int
let generated_3091 = {| yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) |}

result : Set Int
let result = generated_3091

--! assertNoErrors
--! assertType generated_3091 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
