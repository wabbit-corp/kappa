module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3394 : Int
let generated_3394 =
    block
        let (@x : Int) = (match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_3394

--! assertNoErrors
--! assertType generated_3394 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
