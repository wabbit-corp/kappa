module main

generated_3230 : Int
let generated_3230 =
    block
        ranked : Int
        let ranked = (match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0)

        (ranked + 0)

result : Int
let result = generated_3230

--! assertNoErrors
--! assertType generated_3230 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
