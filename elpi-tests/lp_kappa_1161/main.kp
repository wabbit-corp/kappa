module main

generated_1161 : Map String Int
let generated_1161 = { for x in [1, 2, 3], yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = x
        let values = {|item, (item + 1)|}

        generatedSetCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values), on conflict keep first }

result : Map String Int
let result = generated_1161

--! assertNoErrors
--! assertType generated_1161 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
