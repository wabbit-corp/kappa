module main

generated_1525 : Set Int
let generated_1525 = {| yield (((Some ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)) ?: ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 2)) + 0) |}

result : Set Int
let result = generated_1525

--! assertNoErrors
--! assertType generated_1525 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
