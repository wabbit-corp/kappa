module main

generated_1795 : Set Int
let generated_1795 = {| yield ((match [(block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values), ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) |}

result : Set Int
let result = generated_1795

--! assertNoErrors
--! assertType generated_1795 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
