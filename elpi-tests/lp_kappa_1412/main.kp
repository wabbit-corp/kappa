module main

generated_1412 : Int
let generated_1412 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_1412

--! assertNoErrors
--! assertType generated_1412 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
