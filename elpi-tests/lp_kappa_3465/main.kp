module main

generated_3465 : IO Int
let generated_3465 =
    do
        let staged = (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3465

--! assertNoErrors
--! assertType generated_3465 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
