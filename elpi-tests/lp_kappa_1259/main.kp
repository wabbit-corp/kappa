module main

generated_1259 : Int
let generated_1259 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (Some (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case Some payload -> payload
          case None -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1259

--! assertNoErrors
--! assertType generated_1259 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
