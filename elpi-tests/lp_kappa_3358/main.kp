module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3358 : IO Int
let generated_3358 =
    do
        let (@x : Int) = (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3358

--! assertNoErrors
--! assertType generated_3358 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
