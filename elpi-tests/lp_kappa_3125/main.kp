module main

generated_3125 : List Int
let generated_3125 = [ for x in [1, 2, 3], let y = x + 1, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_3125

--! assertNoErrors
--! assertType generated_3125 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
