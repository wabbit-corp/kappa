module main

type GeneratedPair = (left : Int, right : Int)

generated_1564 : GeneratedPair
let generated_1564 = ((left = (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)

    generatedListLength [item, (item + 1), 0]), right = 0)).{ right = ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)

    generatedListLength [item, (item + 1), 0]) + 1) }

result : GeneratedPair
let result = generated_1564

--! assertNoErrors
--! assertType generated_1564 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, signature, let, signature, let
