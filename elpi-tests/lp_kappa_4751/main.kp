module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_4751 : Int
let generated_4751 = generatedResize { image = ((block
    let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0), height = 3, width = 2 }

result : Int
let result = generated_4751

--! assertNoErrors
--! assertType generated_4751 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
