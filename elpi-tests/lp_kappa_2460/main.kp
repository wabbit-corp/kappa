module main

generated_2460 : Int
let generated_2460 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = (match ((match (Some 0)
          case Some payload -> payload
          case None -> 0) == (match (Some 0)
          case Some payload -> payload
          case None -> 0))
          case True -> (match (Some 0)
            case Some payload -> payload
            case None -> 0)
          case False -> 0)

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_2460

--! assertNoErrors
--! assertType generated_2460 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
