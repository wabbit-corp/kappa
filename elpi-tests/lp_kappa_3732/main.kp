module main

generated_3732 : Int
let generated_3732 =
    match (((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
      case head :: tail -> head
      case Nil -> 0) + 0) == ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
      case head :: tail -> head
      case Nil -> 0) + 0))
      case True -> ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
        case head :: tail -> head
        case Nil -> 0) + 0)
      case False -> 0

result : Int
let result = generated_3732

--! assertNoErrors
--! assertType generated_3732 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
