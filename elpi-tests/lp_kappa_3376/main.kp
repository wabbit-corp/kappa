module main

generated_3376 : List Int
let generated_3376 = [ for x in [1, 2, 3], skip 1, take 2, yield ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))) ]

result : List Int
let result = generated_3376

--! assertNoErrors
--! assertType generated_3376 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
