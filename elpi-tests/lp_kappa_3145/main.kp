module main

generated_3145 : Set Int
let generated_3145 = {| yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) + 0) |}

result : Set Int
let result = generated_3145

--! assertNoErrors
--! assertType generated_3145 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
