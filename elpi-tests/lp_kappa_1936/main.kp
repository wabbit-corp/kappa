module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1936 : Int
let generated_1936 =
    block
        let (@x : Int) = (match ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) == (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case True -> (block
              generatedMapCount : Map String Int -> Int
              let generatedMapCount values = 1

              let item = 0
              let values = { yield "item" : item }

              generatedMapCount values)
          case False -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_1936

--! assertNoErrors
--! assertType generated_1936 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
