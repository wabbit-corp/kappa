module main

generated_3789 : IO Int
let generated_3789 =
    do
        let staged = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3789

--! assertNoErrors
--! assertType generated_3789 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
