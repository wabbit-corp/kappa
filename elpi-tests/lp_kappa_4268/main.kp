module main

generated_4268 : Map String Int
let generated_4268 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    let pair = (x = (match (x == x)
      case True -> x
      case False -> 0), y = ((match (x == x)
      case True -> x
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_4268

--! assertNoErrors
--! assertType generated_4268 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
