module main

type GeneratedPair = (left : Int, right : Int)

generated_3616 : GeneratedPair
let generated_3616 = ((left = (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) 0)
    let values = { yield "item" : item }

    generatedMapCount values), right = 0)).{ right = ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 1) }

result : GeneratedPair
let result = generated_3616

--! assertNoErrors
--! assertType generated_3616 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, signature, let, signature, let
