module main

generated_3033 : IO Int
let generated_3033 =
    do
        let staged = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3033

--! assertNoErrors
--! assertType generated_3033 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
