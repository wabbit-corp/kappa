module main

generated_3375 : Map String Int
let generated_3375 = { for x in [1, 2, 3], yield "k" : ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))), on conflict keep first }

result : Map String Int
let result = generated_3375

--! assertNoErrors
--! assertType generated_3375 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
