module main

generated_1270 : List Int
let generated_1270 = [ for x in [1, 2, 3], skip 1, take 2, yield (match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) ]

result : List Int
let result = generated_1270

--! assertNoErrors
--! assertType generated_1270 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
