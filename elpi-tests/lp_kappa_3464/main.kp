module main

generated_3464 : Int
let generated_3464 =
    block
        let base : (x : Int) = (x = (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0))
        let extended = base.{ y := (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3464

--! assertNoErrors
--! assertType generated_3464 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
