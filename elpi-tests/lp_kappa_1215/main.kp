module main

generated_1215 : Map String Int
let generated_1215 = { for x in [1, 2, 3], yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = x
        let values = {|item, (item + 1)|}

        generatedSetCount values)
    let values = { yield "item" : item }

    generatedMapCount values), on conflict keep first }

result : Map String Int
let result = generated_1215

--! assertNoErrors
--! assertType generated_1215 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
