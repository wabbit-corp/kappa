module main

type GeneratedUnit = Unit

type GeneratedPair = (left : Int, right : Int)

type GeneratedTuple = (Int, Int)

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_50 : (GeneratedPair -> Int)
let generated_50 = \(rec : GeneratedPair) -> ((rec).left + (rec).right)

result : (GeneratedPair -> Int)
let result = generated_50

--! assertNoErrors
--! assertType generated_50 (GeneratedPair -> Int)
--! assertType result (GeneratedPair -> Int)
--! assertDeclKinds type, type, type, data, trait, instance, signature, let, signature, let
