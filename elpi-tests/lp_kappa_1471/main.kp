module main

generated_1471 : Set Int
let generated_1471 = {| yield ((match ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) == (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case True -> (block
      generatedSetCount : Set Int -> Int
      let generatedSetCount values = 2

      let item = 0
      let values = {|item, (item + 1)|}

      generatedSetCount values)
  case False -> 0) + 0) |}

result : Set Int
let result = generated_1471

--! assertNoErrors
--! assertType generated_1471 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
