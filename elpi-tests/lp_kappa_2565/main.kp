module main

generated_2565 : Map String Int
let generated_2565 = { for x in [1, 2, 3], yield "k" : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)

    generatedListLength [item, (item + 1), 0]), on conflict keep first }

result : Map String Int
let result = generated_2565

--! assertNoErrors
--! assertType generated_2565 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
