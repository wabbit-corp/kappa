module main

generated_4780 : List Int
let generated_4780 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    let pair = (x = ((Some (x + 1)) ?: (x + 2)), y = (((Some (x + 1)) ?: (x + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_4780

--! assertNoErrors
--! assertType generated_4780 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
