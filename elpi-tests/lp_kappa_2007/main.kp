module main

generated_2007 : IO Int
let generated_2007 =
    do
        let staged = (((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2007

--! assertNoErrors
--! assertType generated_2007 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
