module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_1887 : Int
let generated_1887 =
    match (GeneratedLeft (((\(n : Int) -> n) (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> (((\(n : Int) -> n) (block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = 0
          let values = { yield "item" : item }

          generatedMapCount values)) + 0)

result : Int
let result = generated_1887

--! assertNoErrors
--! assertType generated_1887 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
