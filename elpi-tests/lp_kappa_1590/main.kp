module main

generated_1590 : Int
let generated_1590 =
    match [((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)

        generatedListLength [item, (item + 1), 0]) + 0), (((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)

        generatedListLength [item, (item + 1), 0]) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1590

--! assertNoErrors
--! assertType generated_1590 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
