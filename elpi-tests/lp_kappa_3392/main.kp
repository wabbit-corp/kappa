module main

generated_3392 : Int
let generated_3392 =
    block
        ranked : Int
        let ranked = (match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0)

        (ranked + 0)

result : Int
let result = generated_3392

--! assertNoErrors
--! assertType generated_3392 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
