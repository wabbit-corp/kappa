module main

generated_1628 : Int
let generated_1628 =
    block
        let base : (x : Int) = (x = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0))
        let extended = base.{ y := ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_1628

--! assertNoErrors
--! assertType generated_1628 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
