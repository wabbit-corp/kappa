module main

generated_3842 : Int
let generated_3842 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3842

--! assertNoErrors
--! assertType generated_3842 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
