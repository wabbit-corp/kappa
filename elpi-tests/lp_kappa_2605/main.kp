module main

generated_2605 : Set Int
let generated_2605 = {| yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) |}

result : Set Int
let result = generated_2605

--! assertNoErrors
--! assertType generated_2605 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
