module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1738 : IO Int
let generated_1738 =
    do
        let (@x : Int) = ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)
        pure generatedNeed

result : IO Int
let result = generated_1738

--! assertNoErrors
--! assertType generated_1738 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
