module main

generated_4542 : Int
let generated_4542 =
    match (((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = ((Some (0 + 1)) ?: (0 + 2))
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0) == ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = ((Some (0 + 1)) ?: (0 + 2))
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0))
      case True -> ((block
          generatedSetCount : Set Int -> Int
          let generatedSetCount values = 2

          let item = ((Some (0 + 1)) ?: (0 + 2))
          let values = {|item, (item + 1)|}

          generatedSetCount values) + 0)
      case False -> 0

result : Int
let result = generated_4542

--! assertNoErrors
--! assertType generated_4542 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
