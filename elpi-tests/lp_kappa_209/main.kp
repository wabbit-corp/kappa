module main

generated_209 : List Int
let generated_209 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_209

--! assertNoErrors
--! assertType generated_209 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
