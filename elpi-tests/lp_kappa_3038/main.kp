module main

generated_3038 : Map String Int
let generated_3038 = { yield "only" : ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))

    generatedListLength [item, (item + 1), 0]) + 0) }

result : Map String Int
let result = generated_3038

--! assertNoErrors
--! assertType generated_3038 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
