module main

generated_3878 : Int
let generated_3878 =
    block
        ranked : Int
        let ranked = (match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0)

        (ranked + 0)

result : Int
let result = generated_3878

--! assertNoErrors
--! assertType generated_3878 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
