module main

generated_3152 : Int
let generated_3152 =
    block
        let (captured : Int) = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        captured

result : Int
let result = generated_3152

--! assertNoErrors
--! assertType generated_3152 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
