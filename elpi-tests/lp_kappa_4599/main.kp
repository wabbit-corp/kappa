module main

generated_4599 : IO Int
let generated_4599 =
    do
        let staged = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4599

--! assertNoErrors
--! assertType generated_4599 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
