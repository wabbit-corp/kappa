module main

generated_1394 : Int
let generated_1394 =
    block
        ranked : Int
        let ranked = ((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))

        (ranked + 0)

result : Int
let result = generated_1394

--! assertNoErrors
--! assertType generated_1394 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
