module main

generated_245 : List Int
let generated_245 = [ for x in [1, 2, 3], order by x desc, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_245

--! assertNoErrors
--! assertType generated_245 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
