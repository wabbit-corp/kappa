module main

generated_3558 : IO Int
let generated_3558 =
    do
        value <- pure ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3558

--! assertNoErrors
--! assertType generated_3558 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
