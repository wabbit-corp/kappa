module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3914 : Int
let generated_3914 =
    block
        let width = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3914

--! assertNoErrors
--! assertType generated_3914 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
