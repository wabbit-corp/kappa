module main

generated_3269 : List Int
let generated_3269 = [ for x in [1, 2, 3], order by x desc, yield (match [(block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) ]

result : List Int
let result = generated_3269

--! assertNoErrors
--! assertType generated_3269 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
