module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_2332 : IO Int
let generated_2332 =
    do
        let (@x : Int) = ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_2332

--! assertNoErrors
--! assertType generated_2332 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
