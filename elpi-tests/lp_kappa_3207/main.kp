module main

generated_3207 : Int
let generated_3207 =
    block
        let pair = (x = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0), y = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3207

--! assertNoErrors
--! assertType generated_3207 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
