module main

generated_3621 : Int
let generated_3621 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((\(n : Int) -> n) 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_3621

--! assertNoErrors
--! assertType generated_3621 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
