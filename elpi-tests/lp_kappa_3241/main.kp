module main

generated_3241 : Set Int
let generated_3241 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match [(block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) |}

result : Set Int
let result = generated_3241

--! assertNoErrors
--! assertType generated_3241 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
