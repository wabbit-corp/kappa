module main

generated_4503 : Int
let generated_4503 =
    block
        let pair = (x = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 0), y = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_4503

--! assertNoErrors
--! assertType generated_4503 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
