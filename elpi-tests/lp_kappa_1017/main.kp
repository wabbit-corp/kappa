module main

generated_1017 : Int
let generated_1017 =
    let
        ranked : Int = ((Some ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 2))
in
        (ranked + 0)

result : Int
let result = generated_1017

--! assertNoErrors
--! assertType generated_1017 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
