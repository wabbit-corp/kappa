module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_3373 : Int
let generated_3373 = ((generatedMakeConst @Int (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0)) ())

result : Int
let result = generated_3373

--! assertNoErrors
--! assertType generated_3373 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
