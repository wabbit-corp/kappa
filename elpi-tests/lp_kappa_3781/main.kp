module main

generated_3781 : Set Int
let generated_3781 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    let pair = (x = ((\(n : Int) -> n) x), y = (((\(n : Int) -> n) x) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_3781

--! assertNoErrors
--! assertType generated_3781 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
