module main

generated_4618 : List Int
let generated_4618 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((Some (x + 1)) ?: (x + 2))
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_4618

--! assertNoErrors
--! assertType generated_4618 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
