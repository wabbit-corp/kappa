module main

generated_4117 : Set Int
let generated_4117 = {| yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) |}

result : Set Int
let result = generated_4117

--! assertNoErrors
--! assertType generated_4117 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
