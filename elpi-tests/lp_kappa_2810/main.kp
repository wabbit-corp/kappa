module main

generated_2810 : Map String Int
let generated_2810 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (block
    let pair = (x = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_2810

--! assertNoErrors
--! assertType generated_2810 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
