module main

generated_1529 : Int
let generated_1529 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1529

--! assertNoErrors
--! assertType generated_1529 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
