module main

generated_2546 : Int
let generated_2546 =
    block
        let base : (x : Int) = (x = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)

            generatedListLength [item, (item + 1), 0]) + 0))
        let extended = base.{ y := ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)

            generatedListLength [item, (item + 1), 0]) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_2546

--! assertNoErrors
--! assertType generated_2546 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
