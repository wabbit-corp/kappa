module main

generated_1694 : Int
let generated_1694 =
    block
        let (captured : Int) = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        captured

result : Int
let result = generated_1694

--! assertNoErrors
--! assertType generated_1694 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
