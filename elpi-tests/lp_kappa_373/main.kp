module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_373 : Int
let generated_373 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

result : Int
let result = generated_373

--! assertNoErrors
--! assertType generated_373 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
