module main

generated_1989 : Int
let generated_1989 =
    let
        ranked : Int = ((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2))
in
        (ranked + 0)

result : Int
let result = generated_1989

--! assertNoErrors
--! assertType generated_1989 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
