module main

generated_4591 : Set Int
let generated_4591 = {| for x in [1, 2, 3], yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((Some (x + 1)) ?: (x + 2))
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_4591

--! assertNoErrors
--! assertType generated_4591 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
