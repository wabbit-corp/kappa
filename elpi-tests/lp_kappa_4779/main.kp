module main

generated_4779 : Map String Int
let generated_4779 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = ((Some (x + 1)) ?: (x + 2)), y = (((Some (x + 1)) ?: (x + 2)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_4779

--! assertNoErrors
--! assertType generated_4779 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
