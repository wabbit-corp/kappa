module main

generated_1776 : IO Int
let generated_1776 =
    do
        value <- pure ((match [(block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values), ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1776

--! assertNoErrors
--! assertType generated_1776 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
