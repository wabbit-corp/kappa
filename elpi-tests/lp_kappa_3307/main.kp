module main

generated_3307 : Set Int
let generated_3307 = {| yield ((block
    let pair = (x = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_3307

--! assertNoErrors
--! assertType generated_3307 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
