module main

generated_2647 : Set Int
let generated_2647 = {| for x in [1, 2, 3], yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_2647

--! assertNoErrors
--! assertType generated_2647 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
