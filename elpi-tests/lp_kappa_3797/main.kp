module main

generated_3797 : Int
let generated_3797 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3797

--! assertNoErrors
--! assertType generated_3797 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
