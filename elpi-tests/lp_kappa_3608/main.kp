module main

generated_3608 : Int
let generated_3608 =
    block
        ranked : Int
        let ranked = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((\(n : Int) -> n) 0)
            let values = { yield "item" : item }

            generatedMapCount values)

        (ranked + 0)

result : Int
let result = generated_3608

--! assertNoErrors
--! assertType generated_3608 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
