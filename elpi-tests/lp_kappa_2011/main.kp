module main

generated_2011 : Set Int
let generated_2011 = {| yield (((Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) + 1)) ?: ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) + 2)) + 0) |}

result : Set Int
let result = generated_2011

--! assertNoErrors
--! assertType generated_2011 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
