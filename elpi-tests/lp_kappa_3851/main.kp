module main

generated_3851 : Int
let generated_3851 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3851

--! assertNoErrors
--! assertType generated_3851 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
