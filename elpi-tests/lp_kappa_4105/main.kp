module main

generated_4105 : Set Int
let generated_4105 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_4105

--! assertNoErrors
--! assertType generated_4105 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
