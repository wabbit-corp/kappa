module main

generated_2000 : Map String Int
let generated_2000 = { for x in [1, 2, 3], yield "k" : ((Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 1)) ?: ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 2)) }

result : Map String Int
let result = generated_2000

--! assertNoErrors
--! assertType generated_2000 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
