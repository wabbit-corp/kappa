module main

generated_326 : Map String Int
let generated_326 = { for x in [1, 2, 3], let y = x + 1, yield "k" : (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) }

result : Map String Int
let result = generated_326

--! assertNoErrors
--! assertType generated_326 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
