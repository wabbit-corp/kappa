module main

generated_4543 : Int
let generated_4543 =
    block
        let pair = (x = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0), y = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_4543

--! assertNoErrors
--! assertType generated_4543 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
