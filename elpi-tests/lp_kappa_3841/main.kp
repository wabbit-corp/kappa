module main

generated_3841 : Int
let generated_3841 =
    block
        let pair = (x = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0), y = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3841

--! assertNoErrors
--! assertType generated_3841 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
