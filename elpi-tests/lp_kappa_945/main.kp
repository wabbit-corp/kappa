module main

generated_945 : Map String Int
let generated_945 = { for x in [1, 2, 3], yield "k" : ((\(n : Int) -> n) (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0])), on conflict keep first }

result : Map String Int
let result = generated_945

--! assertNoErrors
--! assertType generated_945 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
