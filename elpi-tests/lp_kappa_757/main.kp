module main

generated_757 : Set Int
let generated_757 = {| for x in [1, 2, 3], let y = x + 1, yield (match (Some (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]))
  case Some payload -> payload
  case None -> 0) |}

result : Set Int
let result = generated_757

--! assertNoErrors
--! assertType generated_757 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
