module main

generated_4606 : Int
let generated_4606 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        (aliased + 1)

result : Int
let result = generated_4606

--! assertNoErrors
--! assertType generated_4606 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
