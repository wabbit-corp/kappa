module main

generated_1413 : IO Int
let generated_1413 =
    do
        let staged = (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1413

--! assertNoErrors
--! assertType generated_1413 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
