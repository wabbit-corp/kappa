module main

generated_3632 : Map String Int
let generated_3632 = { yield "only" : ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) }

result : Map String Int
let result = generated_3632

--! assertNoErrors
--! assertType generated_3632 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
