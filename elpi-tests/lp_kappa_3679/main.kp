module main

generated_3679 : Int
let generated_3679 =
    block
        let pair = (x = ((match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0) + 0), y = ((match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3679

--! assertNoErrors
--! assertType generated_3679 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
