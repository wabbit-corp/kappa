module main

generated_1731 : Int
let generated_1731 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)

result : Int
let result = generated_1731

--! assertNoErrors
--! assertType generated_1731 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
