module main

generated_4761 : IO Int
let generated_4761 =
    do
        let staged = ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4761

--! assertNoErrors
--! assertType generated_4761 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
