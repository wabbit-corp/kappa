module main

generated_2161 : Set Int
let generated_2161 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (Some x)
      case Some payload -> payload
      case None -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_2161

--! assertNoErrors
--! assertType generated_2161 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
