module main

generated_4499 : Int
let generated_4499 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4499

--! assertNoErrors
--! assertType generated_4499 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
