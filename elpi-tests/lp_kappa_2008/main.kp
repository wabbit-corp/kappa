module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_2008 : IO Int
let generated_2008 =
    do
        let (@x : Int) = (((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_2008

--! assertNoErrors
--! assertType generated_2008 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
