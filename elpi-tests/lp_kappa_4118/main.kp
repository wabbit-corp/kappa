module main

generated_4118 : Map String Int
let generated_4118 = { yield "only" : ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) }

result : Map String Int
let result = generated_4118

--! assertNoErrors
--! assertType generated_4118 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
