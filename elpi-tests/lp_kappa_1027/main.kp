module main

generated_1027 : Set Int
let generated_1027 = {| for x in [1, 2, 3], if x > 0, yield ((Some ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) + 2)) |}

result : Set Int
let result = generated_1027

--! assertNoErrors
--! assertType generated_1027 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
