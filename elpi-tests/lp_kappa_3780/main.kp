module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_3780 : Int
let generated_3780 = ((generatedMakeConst ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0)) ())

result : Int
let result = generated_3780

--! assertNoErrors
--! assertType generated_3780 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
