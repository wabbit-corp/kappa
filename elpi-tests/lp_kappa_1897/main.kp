module main

generated_1897 : Int
let generated_1897 =
    block
        let pair = (x = (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0), y = (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_1897

--! assertNoErrors
--! assertType generated_1897 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
