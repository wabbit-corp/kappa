module main

generated_2123 : Int
let generated_2123 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_2123

--! assertNoErrors
--! assertType generated_2123 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
