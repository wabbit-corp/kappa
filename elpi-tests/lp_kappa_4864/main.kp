module main

generated_4864 : Int
let generated_4864 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match (((Some (0 + 1)) ?: (0 + 2)) == ((Some (0 + 1)) ?: (0 + 2)))
          case True -> ((Some (0 + 1)) ?: (0 + 2))
          case False -> 0) + 0)

result : Int
let result = generated_4864

--! assertNoErrors
--! assertType generated_4864 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
