module main

generated_3247 : Int
let generated_3247 =
    block
        let pair = (x = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0), y = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3247

--! assertNoErrors
--! assertType generated_3247 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
