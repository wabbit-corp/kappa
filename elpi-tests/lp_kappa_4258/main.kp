module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4258 : Int
let generated_4258 =
    block
        let (@x : Int) = (block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        if True then generatedNeed else 0

result : Int
let result = generated_4258

--! assertNoErrors
--! assertType generated_4258 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
