module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3466 : IO Int
let generated_3466 =
    do
        let (@x : Int) = (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3466

--! assertNoErrors
--! assertType generated_3466 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
