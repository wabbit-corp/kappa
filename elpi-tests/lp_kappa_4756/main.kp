module main

generated_4756 : Int
let generated_4756 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

result : Int
let result = generated_4756

--! assertNoErrors
--! assertType generated_4756 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
