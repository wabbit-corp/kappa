module main

generated_3351 : Int
let generated_3351 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0)

result : Int
let result = generated_3351

--! assertNoErrors
--! assertType generated_3351 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
