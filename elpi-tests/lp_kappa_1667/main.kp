module main

generated_1667 : List Int
let generated_1667 = [ for x in [1, 2, 3], if x > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_1667

--! assertNoErrors
--! assertType generated_1667 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
