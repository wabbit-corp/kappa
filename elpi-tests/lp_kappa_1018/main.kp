module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1018 : Int
let generated_1018 =
    block
        let (@x : Int) = ((Some ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 1)) ?: ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 2))

        if True then generatedNeed else 0

result : Int
let result = generated_1018

--! assertNoErrors
--! assertType generated_1018 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
