module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_2589 : Int
let generated_2589 =
    match (GeneratedLeft ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          generatedSetCount : Set Int -> Int
          let generatedSetCount values = 2

          let item = (match [0, (0 + 1)]
            case head :: tail -> head
            case Nil -> 0)
          let values = {|item, (item + 1)|}

          generatedSetCount values) + 0)

result : Int
let result = generated_2589

--! assertNoErrors
--! assertType generated_2589 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
