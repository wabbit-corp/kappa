module main

generated_3014 : Int
let generated_3014 =
    block
        ranked : Int
        let ranked = (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))

            generatedListLength [item, (item + 1), 0])

        (ranked + 0)

result : Int
let result = generated_3014

--! assertNoErrors
--! assertType generated_3014 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
