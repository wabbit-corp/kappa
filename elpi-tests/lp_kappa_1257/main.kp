module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_1257 : Int
let generated_1257 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((match (Some (block
          generatedSetCount : Set Int -> Int
          let generatedSetCount values = 2

          let item = 0
          let values = {|item, (item + 1)|}

          generatedSetCount values))
        case Some payload -> payload
        case None -> 0) + 0)

result : Int
let result = generated_1257

--! assertNoErrors
--! assertType generated_1257 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
