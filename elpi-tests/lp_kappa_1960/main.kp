module main

generated_1960 : Int
let generated_1960 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) == (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case True -> (block
              generatedMapCount : Map String Int -> Int
              let generatedMapCount values = 1

              let item = 0
              let values = { yield "item" : item }

              generatedMapCount values)
          case False -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_1960

--! assertNoErrors
--! assertType generated_1960 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
