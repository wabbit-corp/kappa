module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_2818 : IO Int
let generated_2818 =
    do
        let (@x : Int) = ((block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_2818

--! assertNoErrors
--! assertType generated_2818 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
