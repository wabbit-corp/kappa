module main

generated_4675 : Int
let generated_4675 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = (match (Some ((Some (0 + 1)) ?: (0 + 2)))
          case Some payload -> payload
          case None -> 0)

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_4675

--! assertNoErrors
--! assertType generated_4675 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
