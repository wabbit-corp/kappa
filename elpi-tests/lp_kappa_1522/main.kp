module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1522 : IO Int
let generated_1522 =
    do
        let (@x : Int) = (((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_1522

--! assertNoErrors
--! assertType generated_1522 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
