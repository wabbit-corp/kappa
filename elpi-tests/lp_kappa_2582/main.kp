module main

generated_2582 : Int
let generated_2582 =
    block
        ranked : Int
        let ranked = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)

        (ranked + 0)

result : Int
let result = generated_2582

--! assertNoErrors
--! assertType generated_2582 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
