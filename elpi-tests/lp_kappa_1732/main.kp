module main

generated_1732 : Int
let generated_1732 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)

result : Int
let result = generated_1732

--! assertNoErrors
--! assertType generated_1732 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
