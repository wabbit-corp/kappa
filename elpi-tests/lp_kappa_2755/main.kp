module main

generated_2755 : Set Int
let generated_2755 = {| for x in [1, 2, 3], if x > 0, yield (match [(match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0), ((match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) + 1)]
  case head :: tail -> head
  case Nil -> 0) |}

result : Set Int
let result = generated_2755

--! assertNoErrors
--! assertType generated_2755 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
