module main

generated_3133 : Set Int
let generated_3133 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_3133

--! assertNoErrors
--! assertType generated_3133 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
