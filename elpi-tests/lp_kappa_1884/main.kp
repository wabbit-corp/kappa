module main

generated_1884 : IO Int
let generated_1884 =
    do
        value <- pure (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1884

--! assertNoErrors
--! assertType generated_1884 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
