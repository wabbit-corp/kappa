module main

generated_3890 : Map String Int
let generated_3890 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (match (((\(n : Int) -> n) x) == ((\(n : Int) -> n) x))
  case True -> ((\(n : Int) -> n) x)
  case False -> 0) }

result : Map String Int
let result = generated_3890

--! assertNoErrors
--! assertType generated_3890 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
