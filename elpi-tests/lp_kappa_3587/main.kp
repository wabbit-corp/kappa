module main

generated_3587 : Int
let generated_3587 =
    match (Some ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = ((\(n : Int) -> n) 0)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_3587

--! assertNoErrors
--! assertType generated_3587 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
