module main

generated_4327 : Int
let generated_4327 =
    block
        let pair = (x = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0), y = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_4327

--! assertNoErrors
--! assertType generated_4327 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
