module main

generated_4890 : Int
let generated_4890 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = (match (((Some (0 + 1)) ?: (0 + 2)) == ((Some (0 + 1)) ?: (0 + 2)))
          case True -> ((Some (0 + 1)) ?: (0 + 2))
          case False -> 0)

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_4890

--! assertNoErrors
--! assertType generated_4890 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
