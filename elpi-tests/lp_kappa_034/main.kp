module main

generated_34 : Int
let generated_34 =
    block
        let base : (x : Int) = (x = 1)
        let extended = base.{ y := 41 }

        (extended.x + extended.y)

result : Int
let result = generated_34

--! assertNoErrors
--! assertType generated_34 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
