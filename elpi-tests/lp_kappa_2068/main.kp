module main

generated_2068 : Int
let generated_2068 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)

        (aliased + 1)

result : Int
let result = generated_2068

--! assertNoErrors
--! assertType generated_2068 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
