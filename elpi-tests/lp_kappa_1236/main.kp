module main

generated_1236 : IO Int
let generated_1236 =
    do
        value <- pure ((match (Some (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1236

--! assertNoErrors
--! assertType generated_1236 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
