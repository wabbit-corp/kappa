module main

generated_3468 : List Int
let generated_3468 = [ yield (((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0) ]

result : List Int
let result = generated_3468

--! assertNoErrors
--! assertType generated_3468 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
