module main

generated_4064 : Map String Int
let generated_4064 = { yield "only" : ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) }

result : Map String Int
let result = generated_4064

--! assertNoErrors
--! assertType generated_4064 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
