module main

generated_3959 : Int
let generated_3959 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3959

--! assertNoErrors
--! assertType generated_3959 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
