module main

generated_1830 : IO Int
let generated_1830 =
    do
        value <- pure ((block
            let pair = (x = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values), y = ((block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1830

--! assertNoErrors
--! assertType generated_1830 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
