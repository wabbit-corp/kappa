module main

generated_4813 : Int
let generated_4813 =
    block
        let pair = (x = (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 0), y = (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_4813

--! assertNoErrors
--! assertType generated_4813 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
