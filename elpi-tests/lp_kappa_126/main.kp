module main

generated_126 : Int
let generated_126 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((((0 + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_126

--! assertNoErrors
--! assertType generated_126 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
