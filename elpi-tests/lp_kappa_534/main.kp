module main

generated_534 : IO Int
let generated_534 =
    do
        value <- pure (((Some (0 + 1)) ?: (0 + 2)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_534

--! assertNoErrors
--! assertType generated_534 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
