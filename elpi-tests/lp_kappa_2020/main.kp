module main

type GeneratedTuple = (Int, Int)

generated_2020 : Int
let generated_2020 =
    block
        let pair : GeneratedTuple = ((((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 0), (((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_2020

--! assertNoErrors
--! assertType generated_2020 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
