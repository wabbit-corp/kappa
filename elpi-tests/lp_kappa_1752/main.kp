module main

generated_1752 : Int
let generated_1752 =
    match [((match (Some (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values))
      case Some payload -> payload
      case None -> 0) + 0), (((match (Some (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values))
      case Some payload -> payload
      case None -> 0) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_1752

--! assertNoErrors
--! assertType generated_1752 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
