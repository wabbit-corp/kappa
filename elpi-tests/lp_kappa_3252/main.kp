module main

generated_3252 : List Int
let generated_3252 = [ yield ((match [(block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) ]

result : List Int
let result = generated_3252

--! assertNoErrors
--! assertType generated_3252 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
