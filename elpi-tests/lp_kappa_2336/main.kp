module main

generated_2336 : Map String Int
let generated_2336 = { yield "only" : ((block
    let pair = (x = (match (Some 0)
      case Some payload -> payload
      case None -> 0), y = ((match (Some 0)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) }

result : Map String Int
let result = generated_2336

--! assertNoErrors
--! assertType generated_2336 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
