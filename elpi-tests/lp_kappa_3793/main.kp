module main

generated_3793 : Set Int
let generated_3793 = {| yield ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_3793

--! assertNoErrors
--! assertType generated_3793 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
