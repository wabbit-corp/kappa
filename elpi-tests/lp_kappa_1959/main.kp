module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_1959 : Int
let generated_1959 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((match ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = 0
          let values = { yield "item" : item }

          generatedMapCount values) == (block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = 0
          let values = { yield "item" : item }

          generatedMapCount values))
        case True -> (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)
        case False -> 0) + 0)

result : Int
let result = generated_1959

--! assertNoErrors
--! assertType generated_1959 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
