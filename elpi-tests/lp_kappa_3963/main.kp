module main

generated_3963 : Int
let generated_3963 =
    block
        let pair = (x = (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 0), y = (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3963

--! assertNoErrors
--! assertType generated_3963 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
