module main

generated_353 : List Int
let generated_353 = [ for x in [1, 2, 3], order by x desc, yield (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) ]

result : List Int
let result = generated_353

--! assertNoErrors
--! assertType generated_353 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
