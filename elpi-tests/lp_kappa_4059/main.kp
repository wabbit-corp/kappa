module main

generated_4059 : IO Int
let generated_4059 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_4059

--! assertNoErrors
--! assertType generated_4059 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
