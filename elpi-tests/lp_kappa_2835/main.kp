module main

generated_2835 : Map String Int
let generated_2835 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_2835

--! assertNoErrors
--! assertType generated_2835 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
