module main

generated_1919 : List Int
let generated_1919 = [ for x in [1, 2, 3], order by x desc, yield ((\(n : Int) -> n) (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values)) ]

result : List Int
let result = generated_1919

--! assertNoErrors
--! assertType generated_1919 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
