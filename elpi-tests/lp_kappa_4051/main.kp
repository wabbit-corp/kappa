module main

generated_4051 : Set Int
let generated_4051 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_4051

--! assertNoErrors
--! assertType generated_4051 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
