module main

generated_2883 : Int
let generated_2883 =
    block
        let pair = (x = (((\(n : Int) -> n) (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)) + 0), y = (((\(n : Int) -> n) (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_2883

--! assertNoErrors
--! assertType generated_2883 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
