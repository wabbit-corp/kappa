module main

generated_3179 : List Int
let generated_3179 = [ for x in [1, 2, 3], if x > 0, yield (match (Some (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) ]

result : List Int
let result = generated_3179

--! assertNoErrors
--! assertType generated_3179 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
