module main

generated_490 : Int
let generated_490 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match (0 == 0)
          case True -> 0
          case False -> 0) + 0)

result : Int
let result = generated_490

--! assertNoErrors
--! assertType generated_490 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
