module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_1754 : Int
let generated_1754 =
    block
        let width = ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_1754

--! assertNoErrors
--! assertType generated_1754 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
