module main

generated_3879 : Int
let generated_3879 =
    let
        ranked : Int = (match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0)
in
        (ranked + 0)

result : Int
let result = generated_3879

--! assertNoErrors
--! assertType generated_3879 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
