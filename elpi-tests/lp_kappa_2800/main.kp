module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_2800 : Int
let generated_2800 =
    block
        let (@x : Int) = (block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        if True then generatedNeed else 0

result : Int
let result = generated_2800

--! assertNoErrors
--! assertType generated_2800 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
