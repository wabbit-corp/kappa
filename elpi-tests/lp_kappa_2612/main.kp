module main

generated_2612 : Int
let generated_2612 =
    block
        let (captured : Int) = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        captured

result : Int
let result = generated_2612

--! assertNoErrors
--! assertType generated_2612 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
