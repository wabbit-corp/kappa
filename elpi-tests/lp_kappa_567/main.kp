module main

generated_567 : Map String Int
let generated_567 = { for x in [1, 2, 3], yield "k" : ((Some (x + 1)) ?: (x + 2)), on conflict keep first }

result : Map String Int
let result = generated_567

--! assertNoErrors
--! assertType generated_567 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
