module main

generated_1110 : Map String Int
let generated_1110 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (((((((if True then ((left = (x + 0), right = 0)).left else 0) + 0) + 0) + 0) + 0) + 0) + 0) }

result : Map String Int
let result = generated_1110

--! assertNoErrors
--! assertType generated_1110 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
