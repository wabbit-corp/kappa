module main

generated_1729 : Set Int
let generated_1729 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match (Some (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case Some payload -> payload
  case None -> 0) |}

result : Set Int
let result = generated_1729

--! assertNoErrors
--! assertType generated_1729 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
