module main

generated_935 : Int
let generated_935 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0])) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_935

--! assertNoErrors
--! assertType generated_935 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
