module main

generated_3079 : Set Int
let generated_3079 = {| for x in [1, 2, 3], yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_3079

--! assertNoErrors
--! assertType generated_3079 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
