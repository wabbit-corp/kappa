module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4130 : Int
let generated_4130 =
    block
        let width = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4130

--! assertNoErrors
--! assertType generated_4130 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
