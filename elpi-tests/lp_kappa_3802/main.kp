module main

type GeneratedTuple = (Int, Int)

generated_3802 : Int
let generated_3802 =
    block
        let pair : GeneratedTuple = (((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3802

--! assertNoErrors
--! assertType generated_3802 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
