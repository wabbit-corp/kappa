module main

generated_3267 : Map String Int
let generated_3267 = { for x in [1, 2, 3], yield "k" : (match [(block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0), on conflict keep first }

result : Map String Int
let result = generated_3267

--! assertNoErrors
--! assertType generated_3267 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
