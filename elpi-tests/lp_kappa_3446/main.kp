module main

generated_3446 : Int
let generated_3446 =
    block
        ranked : Int
        let ranked = ((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2))

        (ranked + 0)

result : Int
let result = generated_3446

--! assertNoErrors
--! assertType generated_3446 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
