module main

type GeneratedTuple = (Int, Int)

generated_3424 : Int
let generated_3424 =
    block
        let pair : GeneratedTuple = (((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0), ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3424

--! assertNoErrors
--! assertType generated_3424 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
