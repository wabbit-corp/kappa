module main

generated_433 : Set Int
let generated_433 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield ((\(n : Int) -> n) x) |}

result : Set Int
let result = generated_433

--! assertNoErrors
--! assertType generated_433 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
