module main

generated_3483 : Map String Int
let generated_3483 = { for x in [1, 2, 3], yield "k" : ((Some ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)), on conflict keep first }

result : Map String Int
let result = generated_3483

--! assertNoErrors
--! assertType generated_3483 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
