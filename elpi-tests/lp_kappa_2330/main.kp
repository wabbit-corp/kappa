module main

generated_2330 : Int
let generated_2330 =
    block
        let base : (x : Int) = (x = ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0))
        let extended = base.{ y := ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_2330

--! assertNoErrors
--! assertType generated_2330 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
