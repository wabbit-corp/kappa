module main

generated_2920 : Int
let generated_2920 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match ((match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0) == (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0))
          case True -> (match [0, (0 + 1)]
            case head :: tail -> head
            case Nil -> 0)
          case False -> 0) + 0)

result : Int
let result = generated_2920

--! assertNoErrors
--! assertType generated_2920 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
