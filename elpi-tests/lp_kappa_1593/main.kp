module main

generated_1593 : Map String Int
let generated_1593 = { for x in [1, 2, 3], yield "k" : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)

    generatedListLength [item, (item + 1), 0]), on conflict keep first }

result : Map String Int
let result = generated_1593

--! assertNoErrors
--! assertType generated_1593 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
