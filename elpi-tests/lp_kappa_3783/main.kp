module main

generated_3783 : Int
let generated_3783 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

result : Int
let result = generated_3783

--! assertNoErrors
--! assertType generated_3783 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
