module main

generated_3918 : Int
let generated_3918 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = (match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0)

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_3918

--! assertNoErrors
--! assertType generated_3918 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
