module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3413 : Int
let generated_3413 = ((.generatedEcho) ((match ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = 0, y = (0 + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) + 0))

result : Int
let result = generated_3413

--! assertNoErrors
--! assertType generated_3413 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
