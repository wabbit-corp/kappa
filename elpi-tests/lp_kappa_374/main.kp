module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_374 : Int
let generated_374 = ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0).generatedEcho

result : Int
let result = generated_374

--! assertNoErrors
--! assertType generated_374 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
