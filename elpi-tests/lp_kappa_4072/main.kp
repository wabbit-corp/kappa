module main

type GeneratedTuple = (Int, Int)

generated_4072 : Int
let generated_4072 =
    block
        let pair : GeneratedTuple = (((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_4072

--! assertNoErrors
--! assertType generated_4072 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
