module main

generated_1397 : List Int
let generated_1397 = [ for x in [1, 2, 3], let y = x + 1, yield ((\(n : Int) -> n) (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values)) ]

result : List Int
let result = generated_1397

--! assertNoErrors
--! assertType generated_1397 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
