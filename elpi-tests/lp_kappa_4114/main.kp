module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4114 : IO Int
let generated_4114 =
    do
        let (@x : Int) = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure generatedNeed

result : IO Int
let result = generated_4114

--! assertNoErrors
--! assertType generated_4114 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
