module main

generated_3087 : IO Int
let generated_3087 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3087

--! assertNoErrors
--! assertType generated_3087 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
