module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3344 : Int
let generated_3344 = (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0).generatedEcho

result : Int
let result = generated_3344

--! assertNoErrors
--! assertType generated_3344 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
