module main

generated_2943 : Map String Int
let generated_2943 = { for x in [1, 2, 3], yield "k" : (match ((match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) == (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0))
  case True -> (match [x, (x + 1)]
    case head :: tail -> head
    case Nil -> 0)
  case False -> 0), on conflict keep first }

result : Map String Int
let result = generated_2943

--! assertNoErrors
--! assertType generated_2943 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
