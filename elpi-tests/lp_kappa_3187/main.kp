module main

generated_3187 : Set Int
let generated_3187 = {| for x in [1, 2, 3], if x > 0, yield (match (Some (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) |}

result : Set Int
let result = generated_3187

--! assertNoErrors
--! assertType generated_3187 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
