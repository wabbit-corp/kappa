module main

generated_439 : Int
let generated_439 =
    block
        let pair = (x = (((\(n : Int) -> n) 0) + 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_439

--! assertNoErrors
--! assertType generated_439 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
