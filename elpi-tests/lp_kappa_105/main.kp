module main

generated_105 : Int
let generated_105 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((0 + 0) + 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_105

--! assertNoErrors
--! assertType generated_105 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
