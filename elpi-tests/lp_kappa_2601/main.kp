module main

generated_2601 : IO Int
let generated_2601 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2601

--! assertNoErrors
--! assertType generated_2601 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
