module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_427 : Int
let generated_427 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((\(n : Int) -> n) 0) + 0)

result : Int
let result = generated_427

--! assertNoErrors
--! assertType generated_427 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
