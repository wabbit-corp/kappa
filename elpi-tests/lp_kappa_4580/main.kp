module main

generated_4580 : Int
let generated_4580 =
    block
        ranked : Int
        let ranked = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values)

        (ranked + 0)

result : Int
let result = generated_4580

--! assertNoErrors
--! assertType generated_4580 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
