module main

generated_819 : IO Int
let generated_819 =
    do
        let staged = ((match [(block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]), ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_819

--! assertNoErrors
--! assertType generated_819 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
