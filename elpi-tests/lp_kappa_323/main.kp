module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_323 : Int
let generated_323 = generatedResize { image = ((match [0, (0 + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0), height = 3, width = 2 }

result : Int
let result = generated_323

--! assertNoErrors
--! assertType generated_323 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
