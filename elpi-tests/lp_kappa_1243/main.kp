module main

generated_1243 : Set Int
let generated_1243 = {| for x in [1, 2, 3], if x > 0, yield (match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) |}

result : Set Int
let result = generated_1243

--! assertNoErrors
--! assertType generated_1243 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
