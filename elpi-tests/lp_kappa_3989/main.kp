module main

generated_3989 : List Int
let generated_3989 = [ for x in [1, 2, 3], let y = x + 1, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (match (x == x)
      case True -> x
      case False -> 0)

    generatedListLength [item, (item + 1), 0]) ]

result : List Int
let result = generated_3989

--! assertNoErrors
--! assertType generated_3989 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
