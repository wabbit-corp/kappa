module main

generated_587 : List Int
let generated_587 = [ for x in [1, 2, 3], if x > 0, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = x

        generatedListLength [item, (item + 1), 0])

    generatedListLength [item, (item + 1), 0]) ]

result : List Int
let result = generated_587

--! assertNoErrors
--! assertType generated_587 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
