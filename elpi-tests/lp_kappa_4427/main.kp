module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_4427 : Int
let generated_4427 = generatedResize { image = (((Some ((match (0 == 0)
  case True -> 0
  case False -> 0) + 1)) ?: ((match (0 == 0)
  case True -> 0
  case False -> 0) + 2)) + 0), height = 3, width = 2 }

result : Int
let result = generated_4427

--! assertNoErrors
--! assertType generated_4427 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
