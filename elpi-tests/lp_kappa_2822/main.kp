module main

generated_2822 : Map String Int
let generated_2822 = { yield "only" : ((block
    let pair = (x = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) }

result : Map String Int
let result = generated_2822

--! assertNoErrors
--! assertType generated_2822 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
