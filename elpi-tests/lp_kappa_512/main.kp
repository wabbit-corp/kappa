module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_512 : Int
let generated_512 =
    block
        let width = ((match (0 == 0)
          case True -> 0
          case False -> 0) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_512

--! assertNoErrors
--! assertType generated_512 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
