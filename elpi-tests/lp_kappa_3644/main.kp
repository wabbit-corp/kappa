module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3644 : Int
let generated_3644 =
    block
        let width = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((\(n : Int) -> n) 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3644

--! assertNoErrors
--! assertType generated_3644 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
