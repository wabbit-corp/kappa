module main

generated_4459 : Int
let generated_4459 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = ((Some ((match (0 == 0)
          case True -> 0
          case False -> 0) + 1)) ?: ((match (0 == 0)
          case True -> 0
          case False -> 0) + 2))

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_4459

--! assertNoErrors
--! assertType generated_4459 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
