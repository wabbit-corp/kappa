module main

generated_3889 : Set Int
let generated_3889 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match (((\(n : Int) -> n) x) == ((\(n : Int) -> n) x))
  case True -> ((\(n : Int) -> n) x)
  case False -> 0) |}

result : Set Int
let result = generated_3889

--! assertNoErrors
--! assertType generated_3889 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
