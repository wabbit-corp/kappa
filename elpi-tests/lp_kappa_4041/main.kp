module main

generated_4041 : Int
let generated_4041 =
    let
        ranked : Int = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)
in
        (ranked + 0)

result : Int
let result = generated_4041

--! assertNoErrors
--! assertType generated_4041 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
