module main

generated_2701 : Set Int
let generated_2701 = {| for x in [1, 2, 3], let y = x + 1, yield (match (Some (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0))
  case Some payload -> payload
  case None -> 0) |}

result : Set Int
let result = generated_2701

--! assertNoErrors
--! assertType generated_2701 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
