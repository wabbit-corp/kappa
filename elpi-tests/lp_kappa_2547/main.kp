module main

generated_2547 : IO Int
let generated_2547 =
    do
        let staged = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2547

--! assertNoErrors
--! assertType generated_2547 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
