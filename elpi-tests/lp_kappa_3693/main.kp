module main

generated_3693 : Int
let generated_3693 =
    block
        let pair = (x = ((match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0) + 0), y = ((match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3693

--! assertNoErrors
--! assertType generated_3693 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
