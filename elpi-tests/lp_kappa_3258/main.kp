module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3258 : Int
let generated_3258 = generatedNeed @(((match [(block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0))

result : Int
let result = generated_3258

--! assertNoErrors
--! assertType generated_3258 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
