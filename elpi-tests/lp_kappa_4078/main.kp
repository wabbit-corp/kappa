module main

generated_4078 : List Int
let generated_4078 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_4078

--! assertNoErrors
--! assertType generated_4078 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
