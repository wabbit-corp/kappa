module main

generated_3635 : Int
let generated_3635 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((\(n : Int) -> n) 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3635

--! assertNoErrors
--! assertType generated_3635 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
