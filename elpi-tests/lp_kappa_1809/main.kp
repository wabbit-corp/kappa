module main

generated_1809 : Map String Int
let generated_1809 = { for x in [1, 2, 3], yield "k" : (match [(block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values), ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0), on conflict keep first }

result : Map String Int
let result = generated_1809

--! assertNoErrors
--! assertType generated_1809 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
