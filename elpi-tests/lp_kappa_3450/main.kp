module main

generated_3450 : IO Int
let generated_3450 =
    do
        value <- pure (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3450

--! assertNoErrors
--! assertType generated_3450 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
