module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_1916 : Int
let generated_1916 =
    block
        let width = (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_1916

--! assertNoErrors
--! assertType generated_1916 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
