module main

generated_1736 : Int
let generated_1736 =
    block
        let base : (x : Int) = (x = ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0))
        let extended = base.{ y := ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_1736

--! assertNoErrors
--! assertType generated_1736 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
