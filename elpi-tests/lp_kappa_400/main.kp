module main

type GeneratedTuple = (Int, Int)

generated_400 : Int
let generated_400 =
    block
        let pair : GeneratedTuple = (((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_400

--! assertNoErrors
--! assertType generated_400 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
