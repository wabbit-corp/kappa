module main

generated_164 : Map String Int
let generated_164 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) }

result : Map String Int
let result = generated_164

--! assertNoErrors
--! assertType generated_164 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
