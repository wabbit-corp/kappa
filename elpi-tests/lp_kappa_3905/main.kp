module main

generated_3905 : Int
let generated_3905 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3905

--! assertNoErrors
--! assertType generated_3905 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
