module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3367 : Option Int
let generated_3367 = (Some (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0))?.generatedEcho

result : Option Int
let result = generated_3367

--! assertNoErrors
--! assertType generated_3367 Option Int
--! assertType result Option Int
--! assertDeclKinds signature, let, signature, let, signature, let
