module main

generated_4108 : Int
let generated_4108 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_4108

--! assertNoErrors
--! assertType generated_4108 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
