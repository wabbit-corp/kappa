module main

generated_2659 : Set Int
let generated_2659 = {| yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) |}

result : Set Int
let result = generated_2659

--! assertNoErrors
--! assertType generated_2659 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
