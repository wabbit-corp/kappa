module main

generated_4293 : Map String Int
let generated_4293 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = (match (x == x)
      case True -> x
      case False -> 0), y = ((match (x == x)
      case True -> x
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_4293

--! assertNoErrors
--! assertType generated_4293 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
