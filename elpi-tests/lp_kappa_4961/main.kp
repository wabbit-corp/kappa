module main

generated_4961 : List Int
let generated_4961 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = x

            generatedListLength [item, (item + 1), 0])

        generatedListLength [item, (item + 1), 0])

    generatedListLength [item, (item + 1), 0]) ]

result : List Int
let result = generated_4961

--! assertNoErrors
--! assertType generated_4961 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
