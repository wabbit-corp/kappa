module main

generated_3159 : Map String Int
let generated_3159 = { for x in [1, 2, 3], yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values), on conflict keep first }

result : Map String Int
let result = generated_3159

--! assertNoErrors
--! assertType generated_3159 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
