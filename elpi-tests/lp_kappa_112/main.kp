module main

generated_112 : Int
let generated_112 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 0)

result : Int
let result = generated_112

--! assertNoErrors
--! assertType generated_112 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
