module main

generated_3192 : Int
let generated_3192 =
    match (((match (Some (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)))
      case Some payload -> payload
      case None -> 0) + 0) == ((match (Some (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)))
      case Some payload -> payload
      case None -> 0) + 0))
      case True -> ((match (Some (block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)))
        case Some payload -> payload
        case None -> 0) + 0)
      case False -> 0

result : Int
let result = generated_3192

--! assertNoErrors
--! assertType generated_3192 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
