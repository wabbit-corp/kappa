module main

generated_1474 : Int
let generated_1474 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) == (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case True -> (block
              generatedSetCount : Set Int -> Int
              let generatedSetCount values = 2

              let item = 0
              let values = {|item, (item + 1)|}

              generatedSetCount values)
          case False -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_1474

--! assertNoErrors
--! assertType generated_1474 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
