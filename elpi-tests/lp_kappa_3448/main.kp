module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3448 : Int
let generated_3448 =
    block
        let (@x : Int) = ((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2))

        if True then generatedNeed else 0

result : Int
let result = generated_3448

--! assertNoErrors
--! assertType generated_3448 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
