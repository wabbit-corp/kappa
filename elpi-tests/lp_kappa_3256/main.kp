module main

generated_3256 : Int
let generated_3256 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_3256

--! assertNoErrors
--! assertType generated_3256 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
