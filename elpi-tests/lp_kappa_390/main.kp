module main

generated_390 : List Int
let generated_390 = [ yield ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) ]

result : List Int
let result = generated_390

--! assertNoErrors
--! assertType generated_390 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
