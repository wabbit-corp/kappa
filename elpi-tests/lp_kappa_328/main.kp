module main

generated_328 : Int
let generated_328 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_328

--! assertNoErrors
--! assertType generated_328 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
