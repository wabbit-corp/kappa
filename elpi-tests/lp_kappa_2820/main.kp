module main

generated_2820 : List Int
let generated_2820 = [ yield ((block
    let pair = (x = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) ]

result : List Int
let result = generated_2820

--! assertNoErrors
--! assertType generated_2820 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
