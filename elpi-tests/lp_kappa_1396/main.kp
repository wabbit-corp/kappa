module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1396 : Int
let generated_1396 =
    block
        let (@x : Int) = ((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))

        if True then generatedNeed else 0

result : Int
let result = generated_1396

--! assertNoErrors
--! assertType generated_1396 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
