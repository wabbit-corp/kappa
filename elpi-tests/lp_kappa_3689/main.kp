module main

generated_3689 : Int
let generated_3689 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (Some ((\(n : Int) -> n) 0))
          case Some payload -> payload
          case None -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3689

--! assertNoErrors
--! assertType generated_3689 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
