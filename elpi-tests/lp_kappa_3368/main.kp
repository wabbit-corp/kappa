module main

generated_3368 : Int
let generated_3368 =
    block
        let (captured : Int) = (((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))) + 0)

        captured

result : Int
let result = generated_3368

--! assertNoErrors
--! assertType generated_3368 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
