module main

generated_2120 : Map String Int
let generated_2120 = { yield "only" : ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (Some 0)
      case Some payload -> payload
      case None -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) }

result : Map String Int
let result = generated_2120

--! assertNoErrors
--! assertType generated_2120 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
