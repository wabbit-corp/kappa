module main

generated_1783 : Set Int
let generated_1783 = {| for x in [1, 2, 3], yield (match [(block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values), ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) |}

result : Set Int
let result = generated_1783

--! assertNoErrors
--! assertType generated_1783 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
