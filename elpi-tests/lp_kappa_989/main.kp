module main

generated_989 : Int
let generated_989 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) == (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]))
          case True -> (block
              generatedListLength : List Int -> Int
              let generatedListLength xs decreases structural xs =
                  match xs
                    case Nil -> 0
                    case _ :: tail -> 1 + generatedListLength tail

              let item = 0

              generatedListLength [item, (item + 1), 0])
          case False -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_989

--! assertNoErrors
--! assertType generated_989 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
