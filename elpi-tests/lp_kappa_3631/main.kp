module main

generated_3631 : Set Int
let generated_3631 = {| yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) 0)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) |}

result : Set Int
let result = generated_3631

--! assertNoErrors
--! assertType generated_3631 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
