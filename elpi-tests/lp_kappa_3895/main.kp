module main

generated_3895 : Int
let generated_3895 =
    block
        let pair = (x = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0), y = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3895

--! assertNoErrors
--! assertType generated_3895 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
