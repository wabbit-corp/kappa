module main

generated_2325 : Int
let generated_2325 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            let pair = (x = (match (Some 0)
              case Some payload -> payload
              case None -> 0), y = ((match (Some 0)
              case Some payload -> payload
              case None -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

result : Int
let result = generated_2325

--! assertNoErrors
--! assertType generated_2325 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
