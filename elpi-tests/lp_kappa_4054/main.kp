module main

generated_4054 : Int
let generated_4054 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

result : Int
let result = generated_4054

--! assertNoErrors
--! assertType generated_4054 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
