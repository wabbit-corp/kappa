module main

generated_3116 : Int
let generated_3116 =
    block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))
        let values = { yield "item" : item }

        generatedMapCount values

result : Int
let result = generated_3116

--! assertNoErrors
--! assertType generated_3116 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
