module main

generated_4053 : Int
let generated_4053 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

result : Int
let result = generated_4053

--! assertNoErrors
--! assertType generated_4053 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
