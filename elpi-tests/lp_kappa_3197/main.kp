module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3197 : Int
let generated_3197 = ((.generatedEcho) ((match (Some (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) + 0))

result : Int
let result = generated_3197

--! assertNoErrors
--! assertType generated_3197 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
