module main

generated_3414 : List Int
let generated_3414 = [ yield ((match ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = 0, y = (0 + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) + 0) ]

result : List Int
let result = generated_3414

--! assertNoErrors
--! assertType generated_3414 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
