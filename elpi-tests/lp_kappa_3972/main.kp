module main

generated_3972 : Int
let generated_3972 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = ((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2))

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_3972

--! assertNoErrors
--! assertType generated_3972 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
