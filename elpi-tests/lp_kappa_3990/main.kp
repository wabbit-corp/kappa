module main

generated_3990 : IO Int
let generated_3990 =
    do
        value <- pure ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3990

--! assertNoErrors
--! assertType generated_3990 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
