module main

generated_407 : List Int
let generated_407 = [ for x in [1, 2, 3], order by x desc, yield (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_407

--! assertNoErrors
--! assertType generated_407 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
