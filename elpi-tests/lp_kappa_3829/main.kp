module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_3829 : Int
let generated_3829 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0)

result : Int
let result = generated_3829

--! assertNoErrors
--! assertType generated_3829 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
