module main

generated_2164 : Int
let generated_2164 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_2164

--! assertNoErrors
--! assertType generated_2164 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
