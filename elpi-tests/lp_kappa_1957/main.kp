module main

generated_1957 : Set Int
let generated_1957 = {| yield ((match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = 0
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0) + 0) |}

result : Set Int
let result = generated_1957

--! assertNoErrors
--! assertType generated_1957 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
