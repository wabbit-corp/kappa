module main

generated_1135 : Set Int
let generated_1135 = {| for x in [1, 2, 3], yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = x
        let values = {|item, (item + 1)|}

        generatedSetCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_1135

--! assertNoErrors
--! assertType generated_1135 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
