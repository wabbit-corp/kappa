module main

generated_2349 : Map String Int
let generated_2349 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = (match (Some x)
      case Some payload -> payload
      case None -> 0), y = ((match (Some x)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), on conflict keep first }

result : Map String Int
let result = generated_2349

--! assertNoErrors
--! assertType generated_2349 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
