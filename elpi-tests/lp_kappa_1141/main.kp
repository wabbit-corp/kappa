module main

generated_1141 : Int
let generated_1141 =
    block
        let pair = (x = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0), y = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_1141

--! assertNoErrors
--! assertType generated_1141 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
