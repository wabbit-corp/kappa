module main

generated_3891 : Int
let generated_3891 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

result : Int
let result = generated_3891

--! assertNoErrors
--! assertType generated_3891 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
