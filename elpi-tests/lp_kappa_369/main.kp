module main

generated_369 : Int
let generated_369 =
    let
        ranked : Int = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other))
in
        (ranked + 0)

result : Int
let result = generated_369

--! assertNoErrors
--! assertType generated_369 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
