module main

generated_3567 : Int
let generated_3567 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

result : Int
let result = generated_3567

--! assertNoErrors
--! assertType generated_3567 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
