module main

generated_930 : List Int
let generated_930 = [ yield (((\(n : Int) -> n) (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0])) + 0) ]

result : List Int
let result = generated_930

--! assertNoErrors
--! assertType generated_930 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
