module main

generated_3090 : List Int
let generated_3090 = [ yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) ]

result : List Int
let result = generated_3090

--! assertNoErrors
--! assertType generated_3090 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
