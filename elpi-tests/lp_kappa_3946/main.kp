module main

generated_3946 : Int
let generated_3946 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 0)

result : Int
let result = generated_3946

--! assertNoErrors
--! assertType generated_3946 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
