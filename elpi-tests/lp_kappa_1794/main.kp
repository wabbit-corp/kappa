module main

generated_1794 : List Int
let generated_1794 = [ yield ((match [(block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values), ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) ]

result : List Int
let result = generated_1794

--! assertNoErrors
--! assertType generated_1794 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
