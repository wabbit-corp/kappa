module main

generated_3470 : Map String Int
let generated_3470 = { yield "only" : (((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0) }

result : Map String Int
let result = generated_3470

--! assertNoErrors
--! assertType generated_3470 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
