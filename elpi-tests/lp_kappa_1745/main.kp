module main

generated_1745 : Int
let generated_1745 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1745

--! assertNoErrors
--! assertType generated_1745 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
