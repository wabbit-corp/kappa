module main

generated_2056 : Int
let generated_2056 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)

result : Int
let result = generated_2056

--! assertNoErrors
--! assertType generated_2056 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
