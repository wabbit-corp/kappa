module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_4315 : Int
let generated_4315 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0)

result : Int
let result = generated_4315

--! assertNoErrors
--! assertType generated_4315 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
