module main

generated_2154 : IO Int
let generated_2154 =
    do
        value <- pure ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2154

--! assertNoErrors
--! assertType generated_2154 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
