module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4076 : Int
let generated_4076 =
    block
        let width = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4076

--! assertNoErrors
--! assertType generated_4076 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
