module main

type GeneratedPair = (left : Int, right : Int)

generated_4264 : GeneratedPair
let generated_4264 = ((left = (block
    let pair = (x = (match (0 == 0)
      case True -> 0
      case False -> 0), y = ((match (0 == 0)
      case True -> 0
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)), right = 0)).{ right = ((block
    let pair = (x = (match (0 == 0)
      case True -> 0
      case False -> 0), y = ((match (0 == 0)
      case True -> 0
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1) }

result : GeneratedPair
let result = generated_4264

--! assertNoErrors
--! assertType generated_4264 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, signature, let, signature, let
