module main

generated_4484 : Map String Int
let generated_4484 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = ((Some (x + 1)) ?: (x + 2))

    generatedListLength [item, (item + 1), 0]) }

result : Map String Int
let result = generated_4484

--! assertNoErrors
--! assertType generated_4484 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
