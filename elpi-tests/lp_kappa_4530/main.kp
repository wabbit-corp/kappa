module main

generated_4530 : IO Int
let generated_4530 =
    do
        value <- pure ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_4530

--! assertNoErrors
--! assertType generated_4530 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
