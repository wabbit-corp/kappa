module main

generated_2334 : List Int
let generated_2334 = [ yield ((block
    let pair = (x = (match (Some 0)
      case Some payload -> payload
      case None -> 0), y = ((match (Some 0)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) ]

result : List Int
let result = generated_2334

--! assertNoErrors
--! assertType generated_2334 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
