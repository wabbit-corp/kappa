module main

generated_2817 : IO Int
let generated_2817 =
    do
        let staged = ((block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_2817

--! assertNoErrors
--! assertType generated_2817 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
