module main

generated_3800 : Int
let generated_3800 =
    block
        let (captured : Int) = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        captured

result : Int
let result = generated_3800

--! assertNoErrors
--! assertType generated_3800 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
