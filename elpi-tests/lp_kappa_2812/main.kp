module main

generated_2812 : Int
let generated_2812 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((((((((left = 0, right = (((left = (0 + 0), right = 0)).left + 0))).right + 0) + 0) + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_2812

--! assertNoErrors
--! assertType generated_2812 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
