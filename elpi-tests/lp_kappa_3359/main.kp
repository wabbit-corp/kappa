module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_3359 : Int
let generated_3359 = ((.generatedEcho) (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0))

result : Int
let result = generated_3359

--! assertNoErrors
--! assertType generated_3359 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
