module main

generated_4111 : Int
let generated_4111 =
    block
        let pair = (x = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0), y = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_4111

--! assertNoErrors
--! assertType generated_4111 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
