module main

generated_4833 : Map String Int
let generated_4833 = { for x in [1, 2, 3], yield "k" : ((\(n : Int) -> n) ((Some (x + 1)) ?: (x + 2))), on conflict keep first }

result : Map String Int
let result = generated_4833

--! assertNoErrors
--! assertType generated_4833 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
