module main

generatedEcho : forall (a : Type). (this x : a) -> a
let generatedEcho @a (this x : a) = x

generated_721 : Option Int
let generated_721 = (Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = 0

        generatedListLength [item, (item + 1), 0])
    let values = { yield "item" : item }

    generatedMapCount values) + 0))?.generatedEcho

result : Option Int
let result = generated_721

--! assertNoErrors
--! assertType generated_721 Option Int
--! assertType result Option Int
--! assertDeclKinds signature, let, signature, let, signature, let
