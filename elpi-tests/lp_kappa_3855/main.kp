module main

generated_3855 : Int
let generated_3855 =
    block
        let pair = (x = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0), y = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_3855

--! assertNoErrors
--! assertType generated_3855 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
