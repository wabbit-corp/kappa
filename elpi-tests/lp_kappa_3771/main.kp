module main

generated_3771 : Int
let generated_3771 =
    let
        ranked : Int = (block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))
in
        (ranked + 0)

result : Int
let result = generated_3771

--! assertNoErrors
--! assertType generated_3771 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
