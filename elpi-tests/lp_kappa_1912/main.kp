module main

type GeneratedTuple = (Int, Int)

generated_1912 : Int
let generated_1912 =
    block
        let pair : GeneratedTuple = ((((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0), (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_1912

--! assertNoErrors
--! assertType generated_1912 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
