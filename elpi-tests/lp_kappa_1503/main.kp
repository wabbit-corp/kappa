module main

generated_1503 : Int
let generated_1503 =
    let
        ranked : Int = ((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2))
in
        (ranked + 0)

result : Int
let result = generated_1503

--! assertNoErrors
--! assertType generated_1503 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
