module main

generated_2866 : Int
let generated_2866 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)) + 0)

result : Int
let result = generated_2866

--! assertNoErrors
--! assertType generated_2866 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
