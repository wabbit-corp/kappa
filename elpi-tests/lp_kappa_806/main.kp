module main

generated_806 : Int
let generated_806 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((((((((0 + (((left = 0, right = (0 + 0))).right + 0)) + 0) + 0) + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_806

--! assertNoErrors
--! assertType generated_806 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
