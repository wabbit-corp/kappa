module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_4801 : Int
let generated_4801 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 0)

result : Int
let result = generated_4801

--! assertNoErrors
--! assertType generated_4801 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
