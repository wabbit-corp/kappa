module main

generated_2189 : List Int
let generated_2189 = [ for x in [1, 2, 3], order by x desc, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (Some x)
      case Some payload -> payload
      case None -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_2189

--! assertNoErrors
--! assertType generated_2189 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
