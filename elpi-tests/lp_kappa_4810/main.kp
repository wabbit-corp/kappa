module main

generated_4810 : Int
let generated_4810 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 0)

result : Int
let result = generated_4810

--! assertNoErrors
--! assertType generated_4810 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
