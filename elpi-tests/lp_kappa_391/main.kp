module main

generated_391 : Set Int
let generated_391 = {| yield ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_391

--! assertNoErrors
--! assertType generated_391 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
