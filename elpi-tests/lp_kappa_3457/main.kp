module main

generated_3457 : Set Int
let generated_3457 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield ((Some ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) |}

result : Set Int
let result = generated_3457

--! assertNoErrors
--! assertType generated_3457 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
