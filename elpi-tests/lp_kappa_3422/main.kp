module main

generated_3422 : Int
let generated_3422 =
    block
        let (captured : Int) = ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0)

        captured

result : Int
let result = generated_3422

--! assertNoErrors
--! assertType generated_3422 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
