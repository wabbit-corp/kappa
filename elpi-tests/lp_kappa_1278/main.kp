module main

generated_1278 : Int
let generated_1278 =
    block
        scoped effect Choice =
            ω choose : Unit -> Bool

        let seed = (match (Some (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case Some payload -> payload
          case None -> 0)

        let comp : Eff <[Choice : Choice]> Int =
            do
                let b <- Choice.choose ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Choice comp with
                case return x -> pure x
                case choose () k ->
                    do
                        let a <- k True
                        let b <- k False
                        pure (a + b)

        runPure handled

result : Int
let result = generated_1278

--! assertNoErrors
--! assertType generated_1278 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
