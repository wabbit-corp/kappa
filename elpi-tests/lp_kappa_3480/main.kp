module main

generated_3480 : Int
let generated_3480 =
    match [(((Some ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1)) ?: ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 2)) + 0), ((((Some ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1)) ?: ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 2)) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_3480

--! assertNoErrors
--! assertType generated_3480 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
