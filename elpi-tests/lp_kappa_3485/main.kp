module main

generated_3485 : List Int
let generated_3485 = [ for x in [1, 2, 3], order by x desc, yield ((Some ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) ]

result : List Int
let result = generated_3485

--! assertNoErrors
--! assertType generated_3485 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
