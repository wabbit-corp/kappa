module main

generated_4550 : Map String Int
let generated_4550 = { yield "only" : ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((Some (0 + 1)) ?: (0 + 2))
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) }

result : Map String Int
let result = generated_4550

--! assertNoErrors
--! assertType generated_4550 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
