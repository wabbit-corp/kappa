module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_802 : Int
let generated_802 =
    block
        let (@x : Int) = (match [(block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]), ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]) + 1)]
          case head :: tail -> head
          case Nil -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_802

--! assertNoErrors
--! assertType generated_802 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
