module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3718 : Int
let generated_3718 =
    block
        let (@x : Int) = (match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0)

        if True then generatedNeed else 0

result : Int
let result = generated_3718

--! assertNoErrors
--! assertType generated_3718 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
