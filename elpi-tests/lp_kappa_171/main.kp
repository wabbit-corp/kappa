module main

generated_171 : IO Int
let generated_171 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_171

--! assertNoErrors
--! assertType generated_171 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
