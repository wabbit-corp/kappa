module main

generated_2540 : Map String Int
let generated_2540 = { for x in [1, 2, 3], if x > 0, yield "k" : (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)

    generatedListLength [item, (item + 1), 0]) }

result : Map String Int
let result = generated_2540

--! assertNoErrors
--! assertType generated_2540 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
