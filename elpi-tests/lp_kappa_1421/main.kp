module main

generated_1421 : Int
let generated_1421 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((\(n : Int) -> n) (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1421

--! assertNoErrors
--! assertType generated_1421 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
