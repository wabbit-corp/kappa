module main

generated_1594 : List Int
let generated_1594 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)

    generatedListLength [item, (item + 1), 0]) ]

result : List Int
let result = generated_1594

--! assertNoErrors
--! assertType generated_1594 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
