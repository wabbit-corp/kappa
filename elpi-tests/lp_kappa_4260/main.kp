module main

generated_4260 : IO Int
let generated_4260 =
    do
        value <- pure ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_4260

--! assertNoErrors
--! assertType generated_4260 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
