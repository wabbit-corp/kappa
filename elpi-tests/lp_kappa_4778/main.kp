module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4778 : Int
let generated_4778 =
    block
        let width = ((block
            let pair = (x = ((Some (0 + 1)) ?: (0 + 2)), y = (((Some (0 + 1)) ?: (0 + 2)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4778

--! assertNoErrors
--! assertType generated_4778 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
