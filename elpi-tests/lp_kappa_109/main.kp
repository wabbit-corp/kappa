module main

generated_109 : Set Int
let generated_109 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) |}

result : Set Int
let result = generated_109

--! assertNoErrors
--! assertType generated_109 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
