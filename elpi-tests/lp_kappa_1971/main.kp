module main

generated_1971 : Map String Int
let generated_1971 = { for x in [1, 2, 3], yield "k" : (match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = x
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0), on conflict keep first }

result : Map String Int
let result = generated_1971

--! assertNoErrors
--! assertType generated_1971 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
