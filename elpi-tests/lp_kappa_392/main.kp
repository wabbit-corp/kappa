module main

generated_392 : Map String Int
let generated_392 = { yield "only" : ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) }

result : Map String Int
let result = generated_392

--! assertNoErrors
--! assertType generated_392 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
