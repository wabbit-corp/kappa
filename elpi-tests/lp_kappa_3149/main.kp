module main

generated_3149 : Int
let generated_3149 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3149

--! assertNoErrors
--! assertType generated_3149 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
