module main

generated_3203 : Int
let generated_3203 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3203

--! assertNoErrors
--! assertType generated_3203 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
