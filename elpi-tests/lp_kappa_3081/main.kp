module main

generated_3081 : Int
let generated_3081 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

result : Int
let result = generated_3081

--! assertNoErrors
--! assertType generated_3081 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
