module main

generated_3349 : Set Int
let generated_3349 = {| for x in [1, 2, 3], let y = x + 1, yield ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))) |}

result : Set Int
let result = generated_3349

--! assertNoErrors
--! assertType generated_3349 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
