module main

generated_769 : Set Int
let generated_769 = {| yield ((match (Some (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]))
  case Some payload -> payload
  case None -> 0) + 0) |}

result : Set Int
let result = generated_769

--! assertNoErrors
--! assertType generated_769 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
