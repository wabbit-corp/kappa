module main

generated_703 : Set Int
let generated_703 = {| for x in [1, 2, 3], yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = x

        generatedListLength [item, (item + 1), 0])
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_703

--! assertNoErrors
--! assertType generated_703 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
