module main

generated_441 : IO Int
let generated_441 =
    do
        let staged = (((\(n : Int) -> n) 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_441

--! assertNoErrors
--! assertType generated_441 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
