module main

generated_4598 : Int
let generated_4598 =
    block
        let base : (x : Int) = (x = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0))
        let extended = base.{ y := ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_4598

--! assertNoErrors
--! assertType generated_4598 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
