module main

generated_1308 : List Int
let generated_1308 = [ yield ((match [(block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values), ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) ]

result : List Int
let result = generated_1308

--! assertNoErrors
--! assertType generated_1308 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
