module main

generated_3555 : Int
let generated_3555 =
    let
        ranked : Int = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)
in
        (ranked + 0)

result : Int
let result = generated_3555

--! assertNoErrors
--! assertType generated_3555 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
