module main

generated_3753 : Map String Int
let generated_3753 = { for x in [1, 2, 3], yield "k" : (match [((\(n : Int) -> n) x), (((\(n : Int) -> n) x) + 1)]
  case head :: tail -> head
  case Nil -> 0), on conflict keep first }

result : Map String Int
let result = generated_3753

--! assertNoErrors
--! assertType generated_3753 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
