module main

generated_3419 : Int
let generated_3419 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3419

--! assertNoErrors
--! assertType generated_3419 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
