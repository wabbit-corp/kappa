module main

generated_3404 : Map String Int
let generated_3404 = { for x in [1, 2, 3], if x > 0, yield "k" : (match ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = x, y = (x + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) }

result : Map String Int
let result = generated_3404

--! assertNoErrors
--! assertType generated_3404 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
