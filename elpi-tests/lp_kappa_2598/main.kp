module main

generated_2598 : Int
let generated_2598 =
    match (((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0) == ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = (match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0)
        let values = {|item, (item + 1)|}

        generatedSetCount values) + 0))
      case True -> ((block
          generatedSetCount : Set Int -> Int
          let generatedSetCount values = 2

          let item = (match [0, (0 + 1)]
            case head :: tail -> head
            case Nil -> 0)
          let values = {|item, (item + 1)|}

          generatedSetCount values) + 0)
      case False -> 0

result : Int
let result = generated_2598

--! assertNoErrors
--! assertType generated_2598 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
