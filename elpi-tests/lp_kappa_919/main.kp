module main

generated_919 : Set Int
let generated_919 = {| for x in [1, 2, 3], yield ((\(n : Int) -> n) (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0])) |}

result : Set Int
let result = generated_919

--! assertNoErrors
--! assertType generated_919 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
