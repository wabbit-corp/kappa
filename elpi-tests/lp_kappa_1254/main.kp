module main

generated_1254 : List Int
let generated_1254 = [ yield ((match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) + 0) ]

result : List Int
let result = generated_1254

--! assertNoErrors
--! assertType generated_1254 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
