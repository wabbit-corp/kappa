module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_4276 : IO Int
let generated_4276 =
    do
        let (@x : Int) = ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_4276

--! assertNoErrors
--! assertType generated_4276 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
