module main

generated_1972 : List Int
let generated_1972 = [ for x in [1, 2, 3], skip 1, take 2, yield (match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = x
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0) ]

result : List Int
let result = generated_1972

--! assertNoErrors
--! assertType generated_1972 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
