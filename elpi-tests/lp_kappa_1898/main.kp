module main

generated_1898 : Int
let generated_1898 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_1898

--! assertNoErrors
--! assertType generated_1898 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
