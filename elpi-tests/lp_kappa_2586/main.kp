module main

generated_2586 : IO Int
let generated_2586 =
    do
        value <- pure ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2586

--! assertNoErrors
--! assertType generated_2586 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
