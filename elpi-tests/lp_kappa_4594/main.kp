module main

generated_4594 : Int
let generated_4594 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_4594

--! assertNoErrors
--! assertType generated_4594 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
