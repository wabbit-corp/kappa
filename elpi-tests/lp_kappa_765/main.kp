module main

generated_765 : IO Int
let generated_765 =
    do
        let staged = ((match (Some (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_765

--! assertNoErrors
--! assertType generated_765 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
