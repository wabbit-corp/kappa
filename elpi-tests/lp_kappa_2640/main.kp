module main

generated_2640 : IO Int
let generated_2640 =
    do
        value <- pure ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2640

--! assertNoErrors
--! assertType generated_2640 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
