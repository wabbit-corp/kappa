module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_3883 : Int
let generated_3883 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

result : Int
let result = generated_3883

--! assertNoErrors
--! assertType generated_3883 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
