module main

generated_1668 : IO Int
let generated_1668 =
    do
        value <- pure ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                generatedMapCount : Map String Int -> Int
                let generatedMapCount values = 1

                let item = 0
                let values = { yield "item" : item }

                generatedMapCount values)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1668

--! assertNoErrors
--! assertType generated_1668 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
