module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_4320 : Int
let generated_4320 = ((generatedMakeConst (((\(n : Int) -> n) (match (0 == 0)
  case True -> 0
  case False -> 0)) + 0)) ())

result : Int
let result = generated_4320

--! assertNoErrors
--! assertType generated_4320 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
