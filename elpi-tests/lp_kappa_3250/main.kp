module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3250 : IO Int
let generated_3250 =
    do
        let (@x : Int) = ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3250

--! assertNoErrors
--! assertType generated_3250 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
