module main

generated_3734 : Int
let generated_3734 =
    block
        let base : (x : Int) = (x = ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0))
        let extended = base.{ y := ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3734

--! assertNoErrors
--! assertType generated_3734 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
