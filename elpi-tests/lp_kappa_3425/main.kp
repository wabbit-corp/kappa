module main

generated_3425 : Int
let generated_3425 =
    match (Some ((match ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) == (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)))
      case True -> (block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other))
      case False -> 0) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_3425

--! assertNoErrors
--! assertType generated_3425 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
