module main

generated_448 : Int
let generated_448 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = (((\(n : Int) -> n) 0) + 0)

        (aliased + 1)

result : Int
let result = generated_448

--! assertNoErrors
--! assertType generated_448 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
