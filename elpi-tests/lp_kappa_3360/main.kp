module main

generated_3360 : List Int
let generated_3360 = [ yield (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 0) ]

result : List Int
let result = generated_3360

--! assertNoErrors
--! assertType generated_3360 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
