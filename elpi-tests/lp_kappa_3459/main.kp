module main

generated_3459 : Int
let generated_3459 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)

result : Int
let result = generated_3459

--! assertNoErrors
--! assertType generated_3459 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
