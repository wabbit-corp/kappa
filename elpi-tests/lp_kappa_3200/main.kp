module main

generated_3200 : Map String Int
let generated_3200 = { yield "only" : ((match (Some (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) + 0) }

result : Map String Int
let result = generated_3200

--! assertNoErrors
--! assertType generated_3200 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
