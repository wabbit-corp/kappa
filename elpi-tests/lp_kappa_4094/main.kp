module main

generated_4094 : Int
let generated_4094 =
    block
        ranked : Int
        let ranked = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values)

        (ranked + 0)

result : Int
let result = generated_4094

--! assertNoErrors
--! assertType generated_4094 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
