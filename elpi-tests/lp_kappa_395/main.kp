module main

generated_395 : Int
let generated_395 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_395

--! assertNoErrors
--! assertType generated_395 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
