module main

generated_3403 : Set Int
let generated_3403 = {| for x in [1, 2, 3], if x > 0, yield (match ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = x, y = (x + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) |}

result : Set Int
let result = generated_3403

--! assertNoErrors
--! assertType generated_3403 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
