module main

generated_974 : Map String Int
let generated_974 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (((((((if True then (if True then x else 0) else 0) + 0) + 0) + 0) + 0) + 0) + 0) }

result : Map String Int
let result = generated_974

--! assertNoErrors
--! assertType generated_974 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
