module main

generated_3577 : Set Int
let generated_3577 = {| yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((\(n : Int) -> n) 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) |}

result : Set Int
let result = generated_3577

--! assertNoErrors
--! assertType generated_3577 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
