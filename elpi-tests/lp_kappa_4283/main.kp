module main

generated_4283 : Int
let generated_4283 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4283

--! assertNoErrors
--! assertType generated_4283 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
