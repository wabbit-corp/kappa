module main

generated_1341 : Int
let generated_1341 =
    let
        ranked : Int = (block
            let pair = (x = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values), y = ((block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other))
in
        (ranked + 0)

result : Int
let result = generated_1341

--! assertNoErrors
--! assertType generated_1341 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
