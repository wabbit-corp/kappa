module main

generated_1286 : Int
let generated_1286 =
    block
        ranked : Int
        let ranked = (match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0)

        (ranked + 0)

result : Int
let result = generated_1286

--! assertNoErrors
--! assertType generated_1286 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
