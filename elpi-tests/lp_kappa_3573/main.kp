module main

generated_3573 : IO Int
let generated_3573 =
    do
        let staged = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = ((\(n : Int) -> n) 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3573

--! assertNoErrors
--! assertType generated_3573 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
