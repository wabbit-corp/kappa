module main

generated_3234 : IO Int
let generated_3234 =
    do
        value <- pure ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3234

--! assertNoErrors
--! assertType generated_3234 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
