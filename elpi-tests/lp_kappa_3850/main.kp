module main

generated_3850 : Int
let generated_3850 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0)

        (aliased + 1)

result : Int
let result = generated_3850

--! assertNoErrors
--! assertType generated_3850 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
