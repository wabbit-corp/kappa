module main

generated_2046 : IO Int
let generated_2046 =
    do
        value <- pure ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2046

--! assertNoErrors
--! assertType generated_2046 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
