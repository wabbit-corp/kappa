module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1306 : IO Int
let generated_1306 =
    do
        let (@x : Int) = ((match [(block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values), ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure generatedNeed

result : IO Int
let result = generated_1306

--! assertNoErrors
--! assertType generated_1306 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
