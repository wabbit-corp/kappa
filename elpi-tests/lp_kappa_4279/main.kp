module main

generated_4279 : Set Int
let generated_4279 = {| yield ((block
    let pair = (x = (match (0 == 0)
      case True -> 0
      case False -> 0), y = ((match (0 == 0)
      case True -> 0
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_4279

--! assertNoErrors
--! assertType generated_4279 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
