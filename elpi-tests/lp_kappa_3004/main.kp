module main

generated_3004 : Int
let generated_3004 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let l = Ask

        let seed = ((Some ((match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1)) ?: ((match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0) + 2))

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- l.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle l comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_3004

--! assertNoErrors
--! assertType generated_3004 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
