module main

generatedResize : (image : Int) -> (width : Int) -> (height : Int) -> Int
let generatedResize image width height = image + width * 10 + height

generated_4535 : Int
let generated_4535 = generatedResize { image = ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((Some (0 + 1)) ?: (0 + 2))
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0), height = 3, width = 2 }

result : Int
let result = generated_4535

--! assertNoErrors
--! assertType generated_4535 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
