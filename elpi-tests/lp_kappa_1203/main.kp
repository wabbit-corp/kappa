module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_1203 : Int
let generated_1203 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = (block
              generatedSetCount : Set Int -> Int
              let generatedSetCount values = 2

              let item = 0
              let values = {|item, (item + 1)|}

              generatedSetCount values)
          let values = { yield "item" : item }

          generatedMapCount values) + 0)

result : Int
let result = generated_1203

--! assertNoErrors
--! assertType generated_1203 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
