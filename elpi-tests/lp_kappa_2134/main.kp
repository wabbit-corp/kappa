module main

generated_2134 : List Int
let generated_2134 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (Some x)
      case Some payload -> payload
      case None -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_2134

--! assertNoErrors
--! assertType generated_2134 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
