module main

generated_3619 : Set Int
let generated_3619 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) x)
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_3619

--! assertNoErrors
--! assertType generated_3619 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
