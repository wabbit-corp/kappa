module main

generated_3792 : List Int
let generated_3792 = [ yield ((block
    let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) ]

result : List Int
let result = generated_3792

--! assertNoErrors
--! assertType generated_3792 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
