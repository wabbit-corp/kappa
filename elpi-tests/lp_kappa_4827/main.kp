module main

generated_4827 : Int
let generated_4827 =
    block
        let pair = (x = (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 0), y = (((\(n : Int) -> n) ((Some (0 + 1)) ?: (0 + 2))) + 1))
        let whole@(x = captured, y = other) = pair

        (whole.x + (captured + other))

result : Int
let result = generated_4827

--! assertNoErrors
--! assertType generated_4827 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
