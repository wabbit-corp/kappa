module main

generated_1896 : Int
let generated_1896 =
    match ((((\(n : Int) -> n) (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)) + 0) == (((\(n : Int) -> n) (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)) + 0))
      case True -> (((\(n : Int) -> n) (block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = 0
          let values = { yield "item" : item }

          generatedMapCount values)) + 0)
      case False -> 0

result : Int
let result = generated_1896

--! assertNoErrors
--! assertType generated_1896 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
