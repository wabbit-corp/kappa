module main

generated_3565 : Set Int
let generated_3565 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((\(n : Int) -> n) x)
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_3565

--! assertNoErrors
--! assertType generated_3565 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
