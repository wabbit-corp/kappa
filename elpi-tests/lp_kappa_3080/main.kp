module main

generated_3080 : Map String Int
let generated_3080 = { for x in [1, 2, 3], yield "k" : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) }

result : Map String Int
let result = generated_3080

--! assertNoErrors
--! assertType generated_3080 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
