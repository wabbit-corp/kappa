module main

generated_1205 : Int
let generated_1205 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_1205

--! assertNoErrors
--! assertType generated_1205 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
