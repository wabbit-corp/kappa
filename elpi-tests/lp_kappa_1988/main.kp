module main

generated_1988 : Int
let generated_1988 =
    block
        ranked : Int
        let ranked = ((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2))

        (ranked + 0)

result : Int
let result = generated_1988

--! assertNoErrors
--! assertType generated_1988 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
