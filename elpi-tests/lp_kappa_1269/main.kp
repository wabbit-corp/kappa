module main

generated_1269 : Map String Int
let generated_1269 = { for x in [1, 2, 3], yield "k" : (match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0), on conflict keep first }

result : Map String Int
let result = generated_1269

--! assertNoErrors
--! assertType generated_1269 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
