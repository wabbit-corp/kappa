module main

generated_2604 : List Int
let generated_2604 = [ yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) ]

result : List Int
let result = generated_2604

--! assertNoErrors
--! assertType generated_2604 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
