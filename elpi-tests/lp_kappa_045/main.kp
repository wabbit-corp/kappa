module main

type GeneratedUnit = Unit

type GeneratedPair = (left : Int, right : Int)

type GeneratedTuple = (Int, Int)

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_45 : Bool
let generated_45 = if (1 == 1) then True else False

result : Bool
let result = generated_45

--! assertNoErrors
--! assertType generated_45 Bool
--! assertType result Bool
--! assertDeclKinds type, type, type, data, trait, instance, signature, let, signature, let
