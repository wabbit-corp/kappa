module main

type GeneratedTuple = (Int, Int)

generated_3856 : Int
let generated_3856 =
    block
        let pair : GeneratedTuple = ((((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 0), (((\(n : Int) -> n) ((\(n : Int) -> n) 0)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3856

--! assertNoErrors
--! assertType generated_3856 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
