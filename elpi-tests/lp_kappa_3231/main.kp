module main

generated_3231 : Int
let generated_3231 =
    let
        ranked : Int = (match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0)
in
        (ranked + 0)

result : Int
let result = generated_3231

--! assertNoErrors
--! assertType generated_3231 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
