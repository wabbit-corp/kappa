module main

generated_1370 : Int
let generated_1370 =
    block
        let (captured : Int) = ((block
            let pair = (x = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values), y = ((block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        captured

result : Int
let result = generated_1370

--! assertNoErrors
--! assertType generated_1370 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
