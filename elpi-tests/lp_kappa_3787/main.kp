module main

generated_3787 : Int
let generated_3787 =
    block
        let pair = (x = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), y = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3787

--! assertNoErrors
--! assertType generated_3787 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
