module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4022 : Int
let generated_4022 =
    block
        let width = ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4022

--! assertNoErrors
--! assertType generated_4022 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
