module main

generated_4328 : Int
let generated_4328 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_4328

--! assertNoErrors
--! assertType generated_4328 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
