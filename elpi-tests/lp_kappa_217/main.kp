module main

generated_217 : Set Int
let generated_217 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) |}

result : Set Int
let result = generated_217

--! assertNoErrors
--! assertType generated_217 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
