module main

type GeneratedTuple = (Int, Int)

generated_76 : Int
let generated_76 =
    block
        let pair : GeneratedTuple = ((0 + 0), (0 + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_76

--! assertNoErrors
--! assertType generated_76 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
