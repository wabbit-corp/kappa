module main

generated_3396 : IO Int
let generated_3396 =
    do
        value <- pure ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3396

--! assertNoErrors
--! assertType generated_3396 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
