module main

generated_4280 : Map String Int
let generated_4280 = { yield "only" : ((block
    let pair = (x = (match (0 == 0)
      case True -> 0
      case False -> 0), y = ((match (0 == 0)
      case True -> 0
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) }

result : Map String Int
let result = generated_4280

--! assertNoErrors
--! assertType generated_4280 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
