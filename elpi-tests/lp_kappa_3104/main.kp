module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3104 : Int
let generated_3104 =
    block
        let width = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3104

--! assertNoErrors
--! assertType generated_3104 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
