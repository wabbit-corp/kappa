module main

generated_1001 : List Int
let generated_1001 = [ for x in [1, 2, 3], order by x desc, yield (match ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]) == (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = x

    generatedListLength [item, (item + 1), 0]))
  case True -> (block
      generatedListLength : List Int -> Int
      let generatedListLength xs decreases structural xs =
          match xs
            case Nil -> 0
            case _ :: tail -> 1 + generatedListLength tail

      let item = x

      generatedListLength [item, (item + 1), 0])
  case False -> 0) ]

result : List Int
let result = generated_1001

--! assertNoErrors
--! assertType generated_1001 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
