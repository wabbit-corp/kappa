module main

generated_3141 : IO Int
let generated_3141 =
    do
        let staged = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3141

--! assertNoErrors
--! assertType generated_3141 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
