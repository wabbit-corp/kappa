module main

generated_3864 : Int
let generated_3864 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = ((\(n : Int) -> n) ((\(n : Int) -> n) 0))

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_3864

--! assertNoErrors
--! assertType generated_3864 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
