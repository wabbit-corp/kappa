module main

generated_3611 : List Int
let generated_3611 = [ for x in [1, 2, 3], if x > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((\(n : Int) -> n) x)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_3611

--! assertNoErrors
--! assertType generated_3611 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
