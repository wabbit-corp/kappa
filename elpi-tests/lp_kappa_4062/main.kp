module main

generated_4062 : List Int
let generated_4062 = [ yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (match (0 == 0)
      case True -> 0
      case False -> 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) ]

result : List Int
let result = generated_4062

--! assertNoErrors
--! assertType generated_4062 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
