module main

generated_4295 : List Int
let generated_4295 = [ for x in [1, 2, 3], order by x desc, yield (block
    let pair = (x = (match (x == x)
      case True -> x
      case False -> 0), y = ((match (x == x)
      case True -> x
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_4295

--! assertNoErrors
--! assertType generated_4295 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
