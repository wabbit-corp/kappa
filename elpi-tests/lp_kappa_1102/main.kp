module main

type GeneratedTuple = (Int, Int)

generated_1102 : Int
let generated_1102 =
    block
        let pair : GeneratedTuple = (((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)

            generatedListLength [item, (item + 1), 0]) + 0), ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)

            generatedListLength [item, (item + 1), 0]) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_1102

--! assertNoErrors
--! assertType generated_1102 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
