module main

generated_4486 : Int
let generated_4486 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((Some (0 + 1)) ?: (0 + 2))

            generatedListLength [item, (item + 1), 0]) + 0)

result : Int
let result = generated_4486

--! assertNoErrors
--! assertType generated_4486 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
