module main

generated_385 : Int
let generated_385 =
    block
        let pair = (x = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), y = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_385

--! assertNoErrors
--! assertType generated_385 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
