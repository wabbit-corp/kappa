module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_2047 : Int
let generated_2047 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)

            generatedListLength [item, (item + 1), 0]) + 0)

result : Int
let result = generated_2047

--! assertNoErrors
--! assertType generated_2047 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
