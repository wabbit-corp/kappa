module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_3482 : Int
let generated_3482 =
    block
        let width = (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_3482

--! assertNoErrors
--! assertType generated_3482 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
