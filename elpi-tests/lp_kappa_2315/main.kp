module main

generated_2315 : List Int
let generated_2315 = [ for x in [1, 2, 3], if x > 0, yield (block
    let pair = (x = (match (Some x)
      case Some payload -> payload
      case None -> 0), y = ((match (Some x)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_2315

--! assertNoErrors
--! assertType generated_2315 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
