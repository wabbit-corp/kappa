module main

generated_1485 : Map String Int
let generated_1485 = { for x in [1, 2, 3], yield "k" : (match ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) == (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case True -> (block
      generatedSetCount : Set Int -> Int
      let generatedSetCount values = 2

      let item = x
      let values = {|item, (item + 1)|}

      generatedSetCount values)
  case False -> 0), on conflict keep first }

result : Map String Int
let result = generated_1485

--! assertNoErrors
--! assertType generated_1485 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
