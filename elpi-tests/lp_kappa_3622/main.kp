module main

generated_3622 : Int
let generated_3622 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((\(n : Int) -> n) 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_3622

--! assertNoErrors
--! assertType generated_3622 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
