module main

generated_2636 : Int
let generated_2636 =
    block
        ranked : Int
        let ranked = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0)
            let values = { yield "item" : item }

            generatedMapCount values)

        (ranked + 0)

result : Int
let result = generated_2636

--! assertNoErrors
--! assertType generated_2636 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
