module main

generated_2015 : Int
let generated_2015 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox (((Some ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)) ?: ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 2)) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_2015

--! assertNoErrors
--! assertType generated_2015 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
