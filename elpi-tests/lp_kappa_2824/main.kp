module main

generated_2824 : Int
let generated_2824 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        (aliased + 1)

result : Int
let result = generated_2824

--! assertNoErrors
--! assertType generated_2824 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
