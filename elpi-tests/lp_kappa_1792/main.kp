module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1792 : IO Int
let generated_1792 =
    do
        let (@x : Int) = ((match [(block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values), ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)
        pure generatedNeed

result : IO Int
let result = generated_1792

--! assertNoErrors
--! assertType generated_1792 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
