module main

generated_931 : Set Int
let generated_931 = {| yield (((\(n : Int) -> n) (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0])) + 0) |}

result : Set Int
let result = generated_931

--! assertNoErrors
--! assertType generated_931 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
