module main

generated_3786 : Int
let generated_3786 =
    match (((block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0) == ((block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0))
      case True -> ((block
          let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 0)
      case False -> 0

result : Int
let result = generated_3786

--! assertNoErrors
--! assertType generated_3786 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
