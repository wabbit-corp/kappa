module main

generated_163 : Set Int
let generated_163 = {| for x in [1, 2, 3], if x > 0, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_163

--! assertNoErrors
--! assertType generated_163 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
