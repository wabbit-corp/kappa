module main

generated_2097 : Int
let generated_2097 =
    let
        ranked : Int = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)
in
        (ranked + 0)

result : Int
let result = generated_2097

--! assertNoErrors
--! assertType generated_2097 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
