module main

generated_3338 : Int
let generated_3338 =
    block
        ranked : Int
        let ranked = ((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))

        (ranked + 0)

result : Int
let result = generated_3338

--! assertNoErrors
--! assertType generated_3338 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
