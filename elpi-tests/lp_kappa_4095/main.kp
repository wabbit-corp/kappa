module main

generated_4095 : Int
let generated_4095 =
    let
        ranked : Int = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values)
in
        (ranked + 0)

result : Int
let result = generated_4095

--! assertNoErrors
--! assertType generated_4095 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
