module main

generated_4604 : Map String Int
let generated_4604 = { yield "only" : ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = ((Some (0 + 1)) ?: (0 + 2))
    let values = { yield "item" : item }

    generatedMapCount values) + 0) }

result : Map String Int
let result = generated_4604

--! assertNoErrors
--! assertType generated_4604 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
