module main

generated_3730 : Int
let generated_3730 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_3730

--! assertNoErrors
--! assertType generated_3730 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
