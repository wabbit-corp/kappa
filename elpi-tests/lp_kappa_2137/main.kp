module main

generated_2137 : Int
let generated_2137 =
    block
        scoped effect State (s : Type) =
            get : Unit -> s

        let seed = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)

        let comp : Eff <[State : State Int]> Int =
            do
                let n <- State.get ()
                n

        let handled : Eff <[ ]> Int =
            deep handle State comp with
                case return x -> pure x
                case get () k -> k seed

        runPure handled

result : Int
let result = generated_2137

--! assertNoErrors
--! assertType generated_2137 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
