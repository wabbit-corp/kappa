module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_4605 : Int
let generated_4605 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          generatedMapCount : Map String Int -> Int
          let generatedMapCount values = 1

          let item = ((Some (0 + 1)) ?: (0 + 2))
          let values = { yield "item" : item }

          generatedMapCount values) + 0)

result : Int
let result = generated_4605

--! assertNoErrors
--! assertType generated_4605 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
