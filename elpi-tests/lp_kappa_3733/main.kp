module main

generated_3733 : Int
let generated_3733 =
    block
        let pair = (x = ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0), y = ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)

result : Int
let result = generated_3733

--! assertNoErrors
--! assertType generated_3733 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
