module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3304 : IO Int
let generated_3304 =
    do
        let (@x : Int) = ((block
            let pair = (x = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)), y = ((block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other)) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3304

--! assertNoErrors
--! assertType generated_3304 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
