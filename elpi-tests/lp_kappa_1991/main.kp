module main

generated_1991 : List Int
let generated_1991 = [ for x in [1, 2, 3], yield ((Some ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 1)) ?: ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) + 2)) ]

result : List Int
let result = generated_1991

--! assertNoErrors
--! assertType generated_1991 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
