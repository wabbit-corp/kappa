module main

generated_3788 : Int
let generated_3788 =
    block
        let base : (x : Int) = (x = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0))
        let extended = base.{ y := ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3788

--! assertNoErrors
--! assertType generated_3788 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
