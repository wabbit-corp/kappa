module main

generated_3254 : Map String Int
let generated_3254 = { yield "only" : ((match [(block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)), ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0) }

result : Map String Int
let result = generated_3254

--! assertNoErrors
--! assertType generated_3254 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
