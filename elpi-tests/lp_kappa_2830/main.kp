module main

type GeneratedTuple = (Int, Int)

generated_2830 : Int
let generated_2830 =
    block
        let pair : GeneratedTuple = (((block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0), ((block
            let pair = (x = (match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0), y = ((match [0, (0 + 1)]
              case head :: tail -> head
              case Nil -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_2830

--! assertNoErrors
--! assertType generated_2830 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
