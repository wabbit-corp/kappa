module main

generated_190 : List Int
let generated_190 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_190

--! assertNoErrors
--! assertType generated_190 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
