module main

generated_2384 : Int
let generated_2384 =
    block
        let base : (x : Int) = (x = (((\(n : Int) -> n) (match (Some 0)
          case Some payload -> payload
          case None -> 0)) + 0))
        let extended = base.{ y := (((\(n : Int) -> n) (match (Some 0)
          case Some payload -> payload
          case None -> 0)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_2384

--! assertNoErrors
--! assertType generated_2384 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
