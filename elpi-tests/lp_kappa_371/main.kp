module main

generated_371 : List Int
let generated_371 = [ for x in [1, 2, 3], if x > 0, yield (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_371

--! assertNoErrors
--! assertType generated_371 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
