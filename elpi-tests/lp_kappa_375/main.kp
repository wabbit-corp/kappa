module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_375 : Int
let generated_375 =
    match (GeneratedLeft ((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0))
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)) + 0)

result : Int
let result = generated_375

--! assertNoErrors
--! assertType generated_375 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
