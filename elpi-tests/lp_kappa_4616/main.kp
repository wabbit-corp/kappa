module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4616 : Int
let generated_4616 =
    block
        let width = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4616

--! assertNoErrors
--! assertType generated_4616 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
