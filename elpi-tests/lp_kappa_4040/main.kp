module main

generated_4040 : Int
let generated_4040 =
    block
        ranked : Int
        let ranked = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values)

        (ranked + 0)

result : Int
let result = generated_4040

--! assertNoErrors
--! assertType generated_4040 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
