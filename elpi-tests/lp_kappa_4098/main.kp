module main

generated_4098 : IO Int
let generated_4098 =
    do
        value <- pure ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_4098

--! assertNoErrors
--! assertType generated_4098 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
