module main

generated_4322 : Map String Int
let generated_4322 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : ((\(n : Int) -> n) (match (x == x)
  case True -> x
  case False -> 0)) }

result : Map String Int
let result = generated_4322

--! assertNoErrors
--! assertType generated_4322 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
