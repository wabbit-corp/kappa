module main

generated_3504 : IO Int
let generated_3504 =
    do
        value <- pure ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((\(n : Int) -> n) 0)

            generatedListLength [item, (item + 1), 0]) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3504

--! assertNoErrors
--! assertType generated_3504 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
