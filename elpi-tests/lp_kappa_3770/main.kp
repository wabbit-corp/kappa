module main

generated_3770 : Int
let generated_3770 =
    block
        ranked : Int
        let ranked = (block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other))

        (ranked + 0)

result : Int
let result = generated_3770

--! assertNoErrors
--! assertType generated_3770 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
