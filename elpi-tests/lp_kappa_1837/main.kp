module main

generated_1837 : Set Int
let generated_1837 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    let pair = (x = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values), y = ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_1837

--! assertNoErrors
--! assertType generated_1837 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
