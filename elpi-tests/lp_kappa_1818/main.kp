module main

generated_1818 : Int
let generated_1818 =
    block
        scoped effect Choice =
            ω choose : Unit -> Bool

        let seed = (match [(block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values), ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) + 1)]
          case head :: tail -> head
          case Nil -> 0)

        let comp : Eff <[Choice : Choice]> Int =
            do
                let b <- Choice.choose ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Choice comp with
                case return x -> pure x
                case choose () k ->
                    do
                        let a <- k True
                        let b <- k False
                        pure (a + b)

        runPure handled

result : Int
let result = generated_1818

--! assertNoErrors
--! assertType generated_1818 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
