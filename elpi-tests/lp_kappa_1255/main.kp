module main

generated_1255 : Set Int
let generated_1255 = {| yield ((match (Some (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values))
  case Some payload -> payload
  case None -> 0) + 0) |}

result : Set Int
let result = generated_1255

--! assertNoErrors
--! assertType generated_1255 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
