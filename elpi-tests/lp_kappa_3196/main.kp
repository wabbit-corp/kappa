module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3196 : IO Int
let generated_3196 =
    do
        let (@x : Int) = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3196

--! assertNoErrors
--! assertType generated_3196 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
