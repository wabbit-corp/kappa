module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3790 : IO Int
let generated_3790 =
    do
        let (@x : Int) = ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3790

--! assertNoErrors
--! assertType generated_3790 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
