module main

generated_3743 : Int
let generated_3743 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match [((\(n : Int) -> n) 0), (((\(n : Int) -> n) 0) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3743

--! assertNoErrors
--! assertType generated_3743 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
