module main

generated_1506 : IO Int
let generated_1506 =
    do
        value <- pure (((Some ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 1)) ?: ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 2)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_1506

--! assertNoErrors
--! assertType generated_1506 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
