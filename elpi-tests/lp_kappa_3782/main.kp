module main

generated_3782 : Map String Int
let generated_3782 = { for x in [1, 2, 3], let y = x + 1, yield "k" : (block
    let pair = (x = ((\(n : Int) -> n) x), y = (((\(n : Int) -> n) x) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_3782

--! assertNoErrors
--! assertType generated_3782 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
