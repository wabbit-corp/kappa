module main

generated_1934 : Int
let generated_1934 =
    block
        ranked : Int
        let ranked = (match ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values) == (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case True -> (block
              generatedMapCount : Map String Int -> Int
              let generatedMapCount values = 1

              let item = 0
              let values = { yield "item" : item }

              generatedMapCount values)
          case False -> 0)

        (ranked + 0)

result : Int
let result = generated_1934

--! assertNoErrors
--! assertType generated_1934 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
