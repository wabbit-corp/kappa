module main

generated_132 : Int
let generated_132 =
    match [((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = 0

        generatedListLength [item, (item + 1), 0]) + 0), (((block
        generatedListLength : List Int -> Int
        let generatedListLength xs decreases structural xs =
            match xs
              case Nil -> 0
              case _ :: tail -> 1 + generatedListLength tail

        let item = 0

        generatedListLength [item, (item + 1), 0]) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_132

--! assertNoErrors
--! assertType generated_132 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
