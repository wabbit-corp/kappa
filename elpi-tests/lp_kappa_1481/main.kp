module main

generated_1481 : Int
let generated_1481 =
    match (Some ((match ((block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = 0
        let values = {|item, (item + 1)|}

        generatedSetCount values) == (block
        generatedSetCount : Set Int -> Int
        let generatedSetCount values = 2

        let item = 0
        let values = {|item, (item + 1)|}

        generatedSetCount values))
      case True -> (block
          generatedSetCount : Set Int -> Int
          let generatedSetCount values = 2

          let item = 0
          let values = {|item, (item + 1)|}

          generatedSetCount values)
      case False -> 0) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_1481

--! assertNoErrors
--! assertType generated_1481 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
