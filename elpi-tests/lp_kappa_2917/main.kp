module main

generated_2917 : Set Int
let generated_2917 = {| for x in [1, 2, 3], let y = x + 1, yield (match ((match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0) == (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0))
  case True -> (match [x, (x + 1)]
    case head :: tail -> head
    case Nil -> 0)
  case False -> 0) |}

result : Set Int
let result = generated_2917

--! assertNoErrors
--! assertType generated_2917 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
