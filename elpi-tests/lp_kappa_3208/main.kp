module main

type GeneratedTuple = (Int, Int)

generated_3208 : Int
let generated_3208 =
    block
        let pair : GeneratedTuple = (((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0), ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3208

--! assertNoErrors
--! assertType generated_3208 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
