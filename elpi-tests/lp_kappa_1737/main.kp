module main

generated_1737 : IO Int
let generated_1737 =
    do
        let staged = ((match (Some (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = 0
            let values = { yield "item" : item }

            generatedMapCount values))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_1737

--! assertNoErrors
--! assertType generated_1737 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
