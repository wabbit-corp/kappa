module main

generated_2727 : Map String Int
let generated_2727 = { for x in [1, 2, 3], yield "k" : (match (Some (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0))
  case Some payload -> payload
  case None -> 0), on conflict keep first }

result : Map String Int
let result = generated_2727

--! assertNoErrors
--! assertType generated_2727 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
