module main

generated_4274 : Int
let generated_4274 =
    block
        let base : (x : Int) = (x = ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0))
        let extended = base.{ y := ((block
            let pair = (x = (match (0 == 0)
              case True -> 0
              case False -> 0), y = ((match (0 == 0)
              case True -> 0
              case False -> 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_4274

--! assertNoErrors
--! assertType generated_4274 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
