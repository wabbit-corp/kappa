module main

generated_3416 : Map String Int
let generated_3416 = { yield "only" : ((match ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = 0, y = (0 + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0) + 0) }

result : Map String Int
let result = generated_3416

--! assertNoErrors
--! assertType generated_3416 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
