module main

generated_1406 : Map String Int
let generated_1406 = { for x in [1, 2, 3], let y = x + 1, yield "k" : ((\(n : Int) -> n) (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values)) }

result : Map String Int
let result = generated_1406

--! assertNoErrors
--! assertType generated_1406 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
