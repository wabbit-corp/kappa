module main

generated_1918 : List Int
let generated_1918 = [ for x in [1, 2, 3], skip 1, take 2, yield ((\(n : Int) -> n) (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values)) ]

result : List Int
let result = generated_1918

--! assertNoErrors
--! assertType generated_1918 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
