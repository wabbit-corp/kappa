module main

generated_3915 : Map String Int
let generated_3915 = { for x in [1, 2, 3], yield "k" : (match (((\(n : Int) -> n) x) == ((\(n : Int) -> n) x))
  case True -> ((\(n : Int) -> n) x)
  case False -> 0), on conflict keep first }

result : Map String Int
let result = generated_3915

--! assertNoErrors
--! assertType generated_3915 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
