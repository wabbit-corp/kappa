module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_797 : Int
let generated_797 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((((((((0 + (((left = 0, right = (0 + 0))).right + 0)) + 0) + 0) + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_797

--! assertNoErrors
--! assertType generated_797 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
