module main

generated_2673 : Map String Int
let generated_2673 = { for x in [1, 2, 3], yield "k" : (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0)
    let values = { yield "item" : item }

    generatedMapCount values), on conflict keep first }

result : Map String Int
let result = generated_2673

--! assertNoErrors
--! assertType generated_2673 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
