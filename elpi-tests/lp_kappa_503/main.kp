module main

generated_503 : Int
let generated_503 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((match (0 == 0)
          case True -> 0
          case False -> 0) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_503

--! assertNoErrors
--! assertType generated_503 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
