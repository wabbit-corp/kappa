module main

generated_2821 : Set Int
let generated_2821 = {| yield ((block
    let pair = (x = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0) |}

result : Set Int
let result = generated_2821

--! assertNoErrors
--! assertType generated_2821 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
