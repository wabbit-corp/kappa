module main

generated_3036 : List Int
let generated_3036 = [ yield ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))

    generatedListLength [item, (item + 1), 0]) + 0) ]

result : List Int
let result = generated_3036

--! assertNoErrors
--! assertType generated_3036 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
