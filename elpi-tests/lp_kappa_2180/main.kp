module main

generated_2180 : Int
let generated_2180 =
    block
        let (captured : Int) = ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        captured

result : Int
let result = generated_2180

--! assertNoErrors
--! assertType generated_2180 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
