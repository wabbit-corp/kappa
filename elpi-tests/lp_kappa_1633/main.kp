module main

generated_1633 : Set Int
let generated_1633 = {| yield ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0) |}

result : Set Int
let result = generated_1633

--! assertNoErrors
--! assertType generated_1633 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
