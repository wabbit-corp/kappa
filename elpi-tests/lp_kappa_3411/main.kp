module main

generated_3411 : IO Int
let generated_3411 =
    do
        let staged = ((match ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) == (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case True -> (block
              let pair = (x = 0, y = (0 + 1))
              let (x = captured, y = other) = pair

              (captured + other))
          case False -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3411

--! assertNoErrors
--! assertType generated_3411 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
