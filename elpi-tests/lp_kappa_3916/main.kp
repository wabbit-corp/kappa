module main

generated_3916 : List Int
let generated_3916 = [ for x in [1, 2, 3], skip 1, take 2, yield (match (((\(n : Int) -> n) x) == ((\(n : Int) -> n) x))
  case True -> ((\(n : Int) -> n) x)
  case False -> 0) ]

result : List Int
let result = generated_3916

--! assertNoErrors
--! assertType generated_3916 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
