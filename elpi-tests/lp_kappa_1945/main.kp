module main

generated_1945 : Set Int
let generated_1945 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = x
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0) |}

result : Set Int
let result = generated_1945

--! assertNoErrors
--! assertType generated_1945 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
