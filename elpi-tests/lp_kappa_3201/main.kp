module main

data GeneratedChoice : Type =
    GeneratedLeft (value : Int)
    GeneratedRight

generated_3201 : Int
let generated_3201 =
    match GeneratedRight
      case GeneratedLeft payload -> (payload + 0)
      case GeneratedRight -> ((match (Some (block
          let pair = (x = 0, y = (0 + 1))
          let (x = captured, y = other) = pair

          (captured + other)))
        case Some payload -> payload
        case None -> 0) + 0)

result : Int
let result = generated_3201

--! assertNoErrors
--! assertType generated_3201 Int
--! assertType result Int
--! assertDeclKinds data, signature, let, signature, let
