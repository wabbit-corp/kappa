module main

generated_908 : Int
let generated_908 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((((((((if True then ((0 + 0) + 0) else 0) + 0) + 0) + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_908

--! assertNoErrors
--! assertType generated_908 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
