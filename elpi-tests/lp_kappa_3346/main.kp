module main

type GeneratedPair = (left : Int, right : Int)

generated_3346 : GeneratedPair
let generated_3346 = ((left = ((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))), right = 0)).{ right = (((\(n : Int) -> n) (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other))) + 1) }

result : GeneratedPair
let result = generated_3346

--! assertNoErrors
--! assertType generated_3346 GeneratedPair
--! assertType result GeneratedPair
--! assertDeclKinds type, signature, let, signature, let
