module main

generated_772 : Int
let generated_772 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match (Some (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0]))
          case Some payload -> payload
          case None -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_772

--! assertNoErrors
--! assertType generated_772 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
