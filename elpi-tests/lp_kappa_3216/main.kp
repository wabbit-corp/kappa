module main

generated_3216 : Int
let generated_3216 =
    block
        scoped effect Ask =
            ask : Unit -> Bool

        let seed = (match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0)

        let comp : Eff <[Ask : Ask]> Int =
            do
                let b <- Ask.ask ()
                if b then seed else (seed + 1)

        let handled : Eff <[ ]> Int =
            deep handle Ask comp with
                case return x -> pure x
                case ask () k -> k True

        runPure handled

result : Int
let result = generated_3216

--! assertNoErrors
--! assertType generated_3216 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
