module main

generated_155 : List Int
let generated_155 = [ for x in [1, 2, 3], if x > 0, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = x
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_155

--! assertNoErrors
--! assertType generated_155 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
