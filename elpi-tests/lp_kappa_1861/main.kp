module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_1861 : Int
let generated_1861 = ((generatedMakeConst @Int ((block
    let pair = (x = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values), y = ((block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0)) ())

result : Int
let result = generated_1861

--! assertNoErrors
--! assertType generated_1861 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
