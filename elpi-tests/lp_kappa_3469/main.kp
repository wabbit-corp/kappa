module main

generated_3469 : Set Int
let generated_3469 = {| yield (((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0) |}

result : Set Int
let result = generated_3469

--! assertNoErrors
--! assertType generated_3469 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
