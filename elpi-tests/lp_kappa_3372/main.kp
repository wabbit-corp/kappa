module main

generated_3372 : Int
let generated_3372 =
    match [(((\(n : Int) -> n) (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))) + 0), ((((\(n : Int) -> n) (block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other))) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_3372

--! assertNoErrors
--! assertType generated_3372 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
