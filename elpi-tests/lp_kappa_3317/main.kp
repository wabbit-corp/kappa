module main

generated_3317 : Int
let generated_3317 =
    match (Some ((block
        let pair = (x = (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), y = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0))
      case Some payload -> payload
      case None -> 0

result : Int
let result = generated_3317

--! assertNoErrors
--! assertType generated_3317 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
