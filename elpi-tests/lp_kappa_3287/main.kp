module main

generated_3287 : List Int
let generated_3287 = [ for x in [1, 2, 3], yield (block
    let pair = (x = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) ]

result : List Int
let result = generated_3287

--! assertNoErrors
--! assertType generated_3287 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
