module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3204 : Int
let generated_3204 = generatedNeed @(((match (Some (block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0) + 0))

result : Int
let result = generated_3204

--! assertNoErrors
--! assertType generated_3204 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
