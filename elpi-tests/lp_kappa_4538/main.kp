module main

generated_4538 : Map String Int
let generated_4538 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((Some (x + 1)) ?: (x + 2))
    let values = {|item, (item + 1)|}

    generatedSetCount values) }

result : Map String Int
let result = generated_4538

--! assertNoErrors
--! assertType generated_4538 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
