module main

generated_3774 : IO Int
let generated_3774 =
    do
        value <- pure ((block
            let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)
        pure (value + 2)

result : IO Int
let result = generated_3774

--! assertNoErrors
--! assertType generated_3774 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
