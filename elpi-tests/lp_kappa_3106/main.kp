module main

generated_3106 : List Int
let generated_3106 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = {|item, (item + 1)|}

    generatedSetCount values) ]

result : List Int
let result = generated_3106

--! assertNoErrors
--! assertType generated_3106 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
