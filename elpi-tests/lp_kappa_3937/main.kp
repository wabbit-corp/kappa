module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_3937 : Int
let generated_3937 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((Some (((\(n : Int) -> n) 0) + 1)) ?: (((\(n : Int) -> n) 0) + 2)) + 0)

result : Int
let result = generated_3937

--! assertNoErrors
--! assertType generated_3937 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
