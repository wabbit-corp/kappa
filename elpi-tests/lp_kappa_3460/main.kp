module main

generated_3460 : Int
let generated_3460 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((Some ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)) ?: ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 2)) + 0)

result : Int
let result = generated_3460

--! assertNoErrors
--! assertType generated_3460 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
