module main

generated_4267 : Set Int
let generated_4267 = {| for x in [1, 2, 3], if x > 0, yield (block
    let pair = (x = (match (x == x)
      case True -> x
      case False -> 0), y = ((match (x == x)
      case True -> x
      case False -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_4267

--! assertNoErrors
--! assertType generated_4267 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
