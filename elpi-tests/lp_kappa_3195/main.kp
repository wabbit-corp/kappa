module main

generated_3195 : IO Int
let generated_3195 =
    do
        let staged = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_3195

--! assertNoErrors
--! assertType generated_3195 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
