module main

generated_4132 : List Int
let generated_4132 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_4132

--! assertNoErrors
--! assertType generated_4132 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
