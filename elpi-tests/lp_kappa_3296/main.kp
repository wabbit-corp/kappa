module main

generated_3296 : Map String Int
let generated_3296 = { for x in [1, 2, 3], yield "k" : (block
    let pair = (x = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)), y = ((block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) }

result : Map String Int
let result = generated_3296

--! assertNoErrors
--! assertType generated_3296 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
