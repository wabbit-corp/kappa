module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_3913 : Int
let generated_3913 = ((generatedMakeConst @Int ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
  case True -> ((\(n : Int) -> n) 0)
  case False -> 0) + 0)) ())

result : Int
let result = generated_3913

--! assertNoErrors
--! assertType generated_3913 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
