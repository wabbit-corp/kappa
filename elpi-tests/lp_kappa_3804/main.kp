module main

generated_3804 : Int
let generated_3804 =
    match [((block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0), (((block
        let pair = (x = ((\(n : Int) -> n) 0), y = (((\(n : Int) -> n) 0) + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_3804

--! assertNoErrors
--! assertType generated_3804 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
