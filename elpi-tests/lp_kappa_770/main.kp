module main

generated_770 : Map String Int
let generated_770 = { yield "only" : ((match (Some (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = 0

    generatedListLength [item, (item + 1), 0]))
  case Some payload -> payload
  case None -> 0) + 0) }

result : Map String Int
let result = generated_770

--! assertNoErrors
--! assertType generated_770 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
