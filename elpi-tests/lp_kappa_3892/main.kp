module main

generated_3892 : Int
let generated_3892 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

result : Int
let result = generated_3892

--! assertNoErrors
--! assertType generated_3892 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
