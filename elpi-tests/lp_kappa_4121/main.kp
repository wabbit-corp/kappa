module main

generated_4121 : Int
let generated_4121 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (match (0 == 0)
              case True -> 0
              case False -> 0)
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4121

--! assertNoErrors
--! assertType generated_4121 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
