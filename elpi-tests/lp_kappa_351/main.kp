module main

generated_351 : Map String Int
let generated_351 = { for x in [1, 2, 3], yield "k" : (match [x, (x + 1)]
  case head :: tail -> head
  case Nil -> 0), on conflict keep first }

result : Map String Int
let result = generated_351

--! assertNoErrors
--! assertType generated_351 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
