module main

generated_1125 : Int
let generated_1125 =
    let
        ranked : Int = (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                generatedSetCount : Set Int -> Int
                let generatedSetCount values = 2

                let item = 0
                let values = {|item, (item + 1)|}

                generatedSetCount values)
            let values = {|item, (item + 1)|}

            generatedSetCount values)
in
        (ranked + 0)

result : Int
let result = generated_1125

--! assertNoErrors
--! assertType generated_1125 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
