module main

generated_927 : IO Int
let generated_927 =
    do
        let staged = (((\(n : Int) -> n) (block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = 0

            generatedListLength [item, (item + 1), 0])) + 0)
        pure (staged + 1)

result : IO Int
let result = generated_927

--! assertNoErrors
--! assertType generated_927 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
