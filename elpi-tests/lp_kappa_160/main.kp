module main

generated_160 : Int
let generated_160 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((((((0 + 0) + 0) + 0) + 0) + 0) + 0) + 0)

result : Int
let result = generated_160

--! assertNoErrors
--! assertType generated_160 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
