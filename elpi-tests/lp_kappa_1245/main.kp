module main

generated_1245 : Int
let generated_1245 =
    block
        rankedHelper : (Int -> Int)
        let rankedHelper x = x + 1

        rankedHelper ((match (Some (block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = 0
            let values = {|item, (item + 1)|}

            generatedSetCount values))
          case Some payload -> payload
          case None -> 0) + 0)

result : Int
let result = generated_1245

--! assertNoErrors
--! assertType generated_1245 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
