module main

generated_3350 : Map String Int
let generated_3350 = { for x in [1, 2, 3], let y = x + 1, yield "k" : ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))) }

result : Map String Int
let result = generated_3350

--! assertNoErrors
--! assertType generated_3350 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
