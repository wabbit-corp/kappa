module main

generated_402 : Int
let generated_402 =
    match [((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0), (((block
        let pair = (x = 0, y = (0 + 1))
        let (x = captured, y = other) = pair

        (captured + other)) + 0) + 1)]
      case head :: tail -> head
      case Nil -> 0

result : Int
let result = generated_402

--! assertNoErrors
--! assertType generated_402 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
