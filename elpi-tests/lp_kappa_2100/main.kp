module main

generated_2100 : IO Int
let generated_2100 =
    do
        value <- pure ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (match (Some 0)
              case Some payload -> payload
              case None -> 0)
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure (value + 2)

result : IO Int
let result = generated_2100

--! assertNoErrors
--! assertType generated_2100 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let
