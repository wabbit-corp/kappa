module main

generated_3339 : Int
let generated_3339 =
    let
        ranked : Int = ((\(n : Int) -> n) (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
in
        (ranked + 0)

result : Int
let result = generated_3339

--! assertNoErrors
--! assertType generated_3339 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
