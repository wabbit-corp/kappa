module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_3456 : Int
let generated_3456 = ((generatedMakeConst (((Some ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = 0, y = (0 + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) + 0)) ())

result : Int
let result = generated_3456

--! assertNoErrors
--! assertType generated_3456 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
