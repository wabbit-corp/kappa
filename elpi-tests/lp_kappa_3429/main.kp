module main

generated_3429 : Map String Int
let generated_3429 = { for x in [1, 2, 3], yield "k" : (match ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) == (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case True -> (block
      let pair = (x = x, y = (x + 1))
      let (x = captured, y = other) = pair

      (captured + other))
  case False -> 0), on conflict keep first }

result : Map String Int
let result = generated_3429

--! assertNoErrors
--! assertType generated_3429 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
