module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_3088 : IO Int
let generated_3088 =
    do
        let (@x : Int) = ((block
            generatedSetCount : Set Int -> Int
            let generatedSetCount values = 2

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = {|item, (item + 1)|}

            generatedSetCount values) + 0)
        pure generatedNeed

result : IO Int
let result = generated_3088

--! assertNoErrors
--! assertType generated_3088 IO Int
--! assertType result IO Int
--! assertDeclKinds signature, let, signature, let, signature, let
