module main

generated_3136 : Int
let generated_3136 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

result : Int
let result = generated_3136

--! assertNoErrors
--! assertType generated_3136 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
