module main

generated_2552 : Map String Int
let generated_2552 = { yield "only" : ((block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = (match [0, (0 + 1)]
      case head :: tail -> head
      case Nil -> 0)

    generatedListLength [item, (item + 1), 0]) + 0) }

result : Map String Int
let result = generated_2552

--! assertNoErrors
--! assertType generated_2552 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
