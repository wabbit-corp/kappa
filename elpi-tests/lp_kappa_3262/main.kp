module main

type GeneratedTuple = (Int, Int)

generated_3262 : Int
let generated_3262 =
    block
        let pair : GeneratedTuple = (((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0), ((match [(block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)), ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 1)]
          case head :: tail -> head
          case Nil -> 0) + 1))
        let (left, right) = pair

        (left + right)

result : Int
let result = generated_3262

--! assertNoErrors
--! assertType generated_3262 Int
--! assertType result Int
--! assertDeclKinds type, signature, let, signature, let
