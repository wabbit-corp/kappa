module main

generated_1687 : Set Int
let generated_1687 = {| yield ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = 0
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = { yield "item" : item }

    generatedMapCount values) + 0) |}

result : Set Int
let result = generated_1687

--! assertNoErrors
--! assertType generated_1687 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
