module main

generated_425 : List Int
let generated_425 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield ((\(n : Int) -> n) x) ]

result : List Int
let result = generated_425

--! assertNoErrors
--! assertType generated_425 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
