module main

generated_3341 : List Int
let generated_3341 = [ for x in [1, 2, 3], let y = x + 1, yield ((\(n : Int) -> n) (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other))) ]

result : List Int
let result = generated_3341

--! assertNoErrors
--! assertType generated_3341 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
