module main

generated_3904 : Int
let generated_3904 =
    block
        type RankedAlias = Int

        let aliased : RankedAlias = ((match (((\(n : Int) -> n) 0) == ((\(n : Int) -> n) 0))
          case True -> ((\(n : Int) -> n) 0)
          case False -> 0) + 0)

        (aliased + 1)

result : Int
let result = generated_3904

--! assertNoErrors
--! assertType generated_3904 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
