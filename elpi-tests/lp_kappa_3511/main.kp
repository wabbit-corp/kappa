module main

generated_3511 : Set Int
let generated_3511 = {| for x in [1, 2, 3], yield (block
    generatedListLength : List Int -> Int
    let generatedListLength xs decreases structural xs =
        match xs
          case Nil -> 0
          case _ :: tail -> 1 + generatedListLength tail

    let item = ((\(n : Int) -> n) x)

    generatedListLength [item, (item + 1), 0]) |}

result : Set Int
let result = generated_3511

--! assertNoErrors
--! assertType generated_3511 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
