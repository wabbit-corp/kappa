module main

generated_4918 : Int
let generated_4918 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((Some (((Some (0 + 1)) ?: (0 + 2)) + 1)) ?: (((Some (0 + 1)) ?: (0 + 2)) + 2)) + 0)

result : Int
let result = generated_4918

--! assertNoErrors
--! assertType generated_4918 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
