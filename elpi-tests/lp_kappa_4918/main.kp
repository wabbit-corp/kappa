module main

generated_4918 : Map String Int
let generated_4918 = { for x in [1, 2, 3], let y = x + 1, if y > 0, yield (if y > 1 then "hi" else "lo") : (((((0 + (((if True then ((left = 0, right = (x + 0))).right else 0) + 0) + 0)) + 0) + 0) + 0) + 0) }

result : Map String Int
let result = generated_4918

--! assertNoErrors
--! assertType generated_4918 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
