module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_1314 : Int
let generated_1314 = generatedNeed @(((match [(block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values), ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = 0
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 1)]
  case head :: tail -> head
  case Nil -> 0) + 0))

result : Int
let result = generated_1314

--! assertNoErrors
--! assertType generated_1314 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
