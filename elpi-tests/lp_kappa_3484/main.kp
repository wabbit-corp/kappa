module main

generated_3484 : List Int
let generated_3484 = [ for x in [1, 2, 3], skip 1, take 2, yield ((Some ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 1)) ?: ((block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 2)) ]

result : List Int
let result = generated_3484

--! assertNoErrors
--! assertType generated_3484 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
