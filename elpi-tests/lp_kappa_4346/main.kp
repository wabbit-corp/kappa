module main

generatedUseWidth : (width : Int) -> Int
let generatedUseWidth width = width

generated_4346 : Int
let generated_4346 =
    block
        let width = (((\(n : Int) -> n) (match (0 == 0)
          case True -> 0
          case False -> 0)) + 0)

        generatedUseWidth { width }

result : Int
let result = generated_4346

--! assertNoErrors
--! assertType generated_4346 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
