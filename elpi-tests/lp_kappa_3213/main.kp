module main

generated_3213 : Map String Int
let generated_3213 = { for x in [1, 2, 3], yield "k" : (match (Some (block
    let pair = (x = x, y = (x + 1))
    let (x = captured, y = other) = pair

    (captured + other)))
  case Some payload -> payload
  case None -> 0), on conflict keep first }

result : Map String Int
let result = generated_3213

--! assertNoErrors
--! assertType generated_3213 Map String Int
--! assertType result Map String Int
--! assertDeclKinds signature, let, signature, let
