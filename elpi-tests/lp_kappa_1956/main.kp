module main

generated_1956 : List Int
let generated_1956 = [ yield ((match ((block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values) == (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = 0
    let values = { yield "item" : item }

    generatedMapCount values))
  case True -> (block
      generatedMapCount : Map String Int -> Int
      let generatedMapCount values = 1

      let item = 0
      let values = { yield "item" : item }

      generatedMapCount values)
  case False -> 0) + 0) ]

result : List Int
let result = generated_1956

--! assertNoErrors
--! assertType generated_1956 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
