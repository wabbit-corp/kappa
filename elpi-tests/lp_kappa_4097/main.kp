module main

generated_4097 : List Int
let generated_4097 = [ for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (match (x == x)
      case True -> x
      case False -> 0)
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_4097

--! assertNoErrors
--! assertType generated_4097 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
