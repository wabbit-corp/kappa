module main

generated_3527 : Int
let generated_3527 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedListLength : List Int -> Int
            let generatedListLength xs decreases structural xs =
                match xs
                  case Nil -> 0
                  case _ :: tail -> 1 + generatedListLength tail

            let item = ((\(n : Int) -> n) 0)

            generatedListLength [item, (item + 1), 0]) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_3527

--! assertNoErrors
--! assertType generated_3527 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
