module main

generated_92 : Int
let generated_92 =
    block
        trait RankedScore a =
            rank : a -> Int

        instance RankedScore Int =
            let rank x = x

        let dict = summon (RankedScore Int)

        dict.rank (((0 + 0) + 0) + 0)

result : Int
let result = generated_92

--! assertNoErrors
--! assertType generated_92 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
