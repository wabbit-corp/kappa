module main

generated_1757 : List Int
let generated_1757 = [ for x in [1, 2, 3], order by x desc, yield (match (Some (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = x
    let values = { yield "item" : item }

    generatedMapCount values))
  case Some payload -> payload
  case None -> 0) ]

result : List Int
let result = generated_1757

--! assertNoErrors
--! assertType generated_1757 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
