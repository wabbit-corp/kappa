module main

generated_3123 : Int
let generated_3123 =
    let
        ranked : Int = (block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = (block
                let pair = (x = 0, y = (0 + 1))
                let (x = captured, y = other) = pair

                (captured + other))
            let values = { yield "item" : item }

            generatedMapCount values)
in
        (ranked + 0)

result : Int
let result = generated_3123

--! assertNoErrors
--! assertType generated_3123 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
