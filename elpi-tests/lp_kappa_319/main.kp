module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_319 : Int
let generated_319 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score ((match [0, (0 + 1)]
          case head :: tail -> head
          case Nil -> 0) + 0)

result : Int
let result = generated_319

--! assertNoErrors
--! assertType generated_319 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
