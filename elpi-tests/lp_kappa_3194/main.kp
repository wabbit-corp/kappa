module main

generated_3194 : Int
let generated_3194 =
    block
        let base : (x : Int) = (x = ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 0))
        let extended = base.{ y := ((match (Some (block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)))
          case Some payload -> payload
          case None -> 0) + 1) }

        (extended.x + extended.y)

result : Int
let result = generated_3194

--! assertNoErrors
--! assertType generated_3194 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
