module main

generatedNeed : (@x : Int) -> Int
let generatedNeed (@x : Int) = x

generated_2340 : Int
let generated_2340 = generatedNeed @(((block
    let pair = (x = (match (Some 0)
      case Some payload -> payload
      case None -> 0), y = ((match (Some 0)
      case Some payload -> payload
      case None -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) + 0))

result : Int
let result = generated_2340

--! assertNoErrors
--! assertType generated_2340 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
