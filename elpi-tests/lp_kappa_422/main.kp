module main

generated_422 : Int
let generated_422 =
    block
        ranked : Int
        let ranked = ((\(n : Int) -> n) 0)

        (ranked + 0)

result : Int
let result = generated_422

--! assertNoErrors
--! assertType generated_422 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
