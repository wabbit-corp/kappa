module main

generated_398 : Int
let generated_398 =
    block
        let (captured : Int) = ((block
            let pair = (x = 0, y = (0 + 1))
            let (x = captured, y = other) = pair

            (captured + other)) + 0)

        captured

result : Int
let result = generated_398

--! assertNoErrors
--! assertType generated_398 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
