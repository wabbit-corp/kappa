module main

generated_2809 : Set Int
let generated_2809 = {| for x in [1, 2, 3], let y = x + 1, if y > 0, yield (block
    let pair = (x = (match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0), y = ((match [x, (x + 1)]
      case head :: tail -> head
      case Nil -> 0) + 1))
    let (x = captured, y = other) = pair

    (captured + other)) |}

result : Set Int
let result = generated_2809

--! assertNoErrors
--! assertType generated_2809 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
