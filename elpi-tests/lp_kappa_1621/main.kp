module main

generated_1621 : Set Int
let generated_1621 = {| for x in [1, 2, 3], let y = x + 1, yield (block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = (block
        generatedMapCount : Map String Int -> Int
        let generatedMapCount values = 1

        let item = x
        let values = { yield "item" : item }

        generatedMapCount values)
    let values = {|item, (item + 1)|}

    generatedSetCount values) |}

result : Set Int
let result = generated_1621

--! assertNoErrors
--! assertType generated_1621 Set Int
--! assertType result Set Int
--! assertDeclKinds signature, let, signature, let
