module main

generatedMakeConst : forall (a : Type). a -> Unit -> a
let generatedMakeConst value = \() -> value

generated_3589 : Int
let generated_3589 = ((generatedMakeConst @Int ((block
    generatedSetCount : Set Int -> Int
    let generatedSetCount values = 2

    let item = ((\(n : Int) -> n) 0)
    let values = {|item, (item + 1)|}

    generatedSetCount values) + 0)) ())

result : Int
let result = generated_3589

--! assertNoErrors
--! assertType generated_3589 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let, signature, let
