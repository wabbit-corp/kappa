module main

generated_3160 : List Int
let generated_3160 = [ for x in [1, 2, 3], skip 1, take 2, yield (block
    generatedMapCount : Map String Int -> Int
    let generatedMapCount values = 1

    let item = (block
        let pair = (x = x, y = (x + 1))
        let (x = captured, y = other) = pair

        (captured + other))
    let values = { yield "item" : item }

    generatedMapCount values) ]

result : List Int
let result = generated_3160

--! assertNoErrors
--! assertType generated_3160 List Int
--! assertType result List Int
--! assertDeclKinds signature, let, signature, let
