module main

generated_4607 : Int
let generated_4607 =
    block
        data RankedBox : Type =
            RankedBox (value : Int)

        let boxed = RankedBox ((block
            generatedMapCount : Map String Int -> Int
            let generatedMapCount values = 1

            let item = ((Some (0 + 1)) ?: (0 + 2))
            let values = { yield "item" : item }

            generatedMapCount values) + 0)

        match boxed
          case RankedBox value -> (value + 1)

result : Int
let result = generated_4607

--! assertNoErrors
--! assertType generated_4607 Int
--! assertType result Int
--! assertDeclKinds signature, let, signature, let
