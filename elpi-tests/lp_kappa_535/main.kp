module main

trait GeneratedScore a =
    score : a -> Int

instance GeneratedScore Int =
    let score x = x

generated_535 : Int
let generated_535 =
    let
        dict = summon (GeneratedScore Int)
in
        dict.score (((Some (0 + 1)) ?: (0 + 2)) + 0)

result : Int
let result = generated_535

--! assertNoErrors
--! assertType generated_535 Int
--! assertType result Int
--! assertDeclKinds trait, instance, signature, let, signature, let
