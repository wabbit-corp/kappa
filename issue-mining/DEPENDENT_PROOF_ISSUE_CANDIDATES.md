# Dependent Proof / Elaboration Issue Candidates For Kappa

Mined from:

- `../datatron/data-scala-issues/Idris2-issues.json`

Direct reads completed on 2026-04-26:

- `Idris2#13-#17`
- `Idris2#18-#22`
- `Idris2#23-#27`
- `Idris2#28-#32`
- `Idris2#33-#37`
- `Idris2#38-#42`
- `Idris2#43-#47`
- `Idris2#48-#52`
- `Idris2#53-#57`
- `Idris2#58-#62`
- `Idris2#63-#67`
- `Idris2#68-#72`
- `Idris2#73-#77`
- `Idris2#78-#82` (only `#78` present in this snapshot; `#79-#82` absent)
- `Idris2#95-#99` (`#96` absent in this snapshot)
- `Idris2#100-#104` (only `#101` and `#104` present in this snapshot; `#100`, `#102`, `#103` absent)
- `Idris2#108-#112` (only `#108`, `#110`, and `#112` present in this snapshot; `#109` and `#111` absent)
- `Idris2#114-#118` (only `#114` and `#116` present in this snapshot; `#115`, `#117`, and `#118` absent)
- `Idris2#125-#129` (only `#125` present in this snapshot; `#126`, `#127`, `#128`, and `#129` absent)
- `Idris2#132-#136` (only `#132` and `#135` present in this snapshot; `#133`, `#134`, and `#136` absent)
- `Idris2#140-#144` (only `#140` and `#141` present in this snapshot; `#142`, `#143`, and `#144` absent)
- `Idris2#163-#167` (only `#163`, `#164`, and `#166` present in this snapshot; `#165` and `#167` absent)
- `Idris2#173-#177` (only `#173`, `#174`, and `#175` present in this snapshot; `#176` and `#177` absent)
- `Idris2#179-#183` (only `#179` and `#180` present in this snapshot; `#181`, `#182`, and `#183` absent)
- `Idris2#184-#188`
- `Idris2#189-#193` (only `#189` present in this snapshot; `#190`, `#191`, `#192`, and `#193` absent)
- `Idris2#194-#198` (only `#194` and `#195` present in this snapshot; `#196`, `#197`, and `#198` absent)
- `Idris2#199-#203` (only `#202` present in this snapshot; `#199`, `#200`, `#201`, and `#203` absent)
- `Idris2#204-#208` (only `#208` present in this snapshot; `#204`, `#205`, `#206`, and `#207` absent)
- `Idris2#209-#213` (only `#209`, `#211`, and `#213` present in this snapshot; `#210` and `#212` absent)
- `Idris2#214-#218` (only `#214` and `#215` present in this snapshot; `#216`, `#217`, and `#218` absent)
- `Idris2#221-#225` (only `#221` and `#225` present in this snapshot; `#222`, `#223`, and `#224` absent)
- `Idris2#226-#230` (only `#228` and `#230` present in this snapshot; `#226`, `#227`, and `#229` absent)
- `Idris2#231-#235` (only `#234` present in this snapshot; `#231`, `#232`, `#233`, and `#235` absent)
- `Idris2#236-#240` (only `#240` present in this snapshot; `#236`, `#237`, `#238`, `#239` absent)
- `Idris2#241-#245` (only `#243` present in this snapshot; `#241`, `#242`, `#244`, and `#245` absent)
- `Idris2#251-#255` (only `#253` and `#255` present in this snapshot; `#251`, `#252`, and `#254` absent)
- `Idris2#256-#260` (only `#258` and `#259` present in this snapshot; `#256`, `#257`, and `#260` absent)
- `Idris2#261-#265` (only `#261` and `#264` present in this snapshot; `#262`, `#263`, and `#265` absent)
- `Idris2#266-#270` (only `#268` and `#270` present in this snapshot; `#266`, `#267`, and `#269` absent)
- `Idris2#271-#275` (only `#271` present in this snapshot; `#272`, `#273`, `#274`, and `#275` absent)
- `Idris2#276-#280` (only `#276` present in this snapshot; `#277`, `#278`, `#279`, and `#280` absent)
- `Idris2#298-#302` (only `#300` and `#301` present in this snapshot; `#298`, `#299`, and `#302` absent)
- `Idris2#293-#297` (only `#293`, `#294`, `#295`, and `#297` present in this snapshot; `#296` absent)
- `Idris2#303-#307` (only `#304`, `#305`, and `#307` present in this snapshot; `#303` and `#306` absent)
- `Idris2#308-#312` (only `#308` and `#311` present in this snapshot; `#309`, `#310`, and `#312` absent)
- `Idris2#313-#317` (only `#317` present in this snapshot; `#313`, `#314`, `#315`, and `#316` absent)
- `Idris2#331-#340` (only `#331` and `#338` present in this snapshot; `#332`, `#333`, `#334`, `#335`, `#336`, `#337`, `#339`, and `#340` absent)
- `Idris2#341-#345` (`#341-#345` absent in this snapshot)
- `Idris2#346-#350` (only `#346` present in this snapshot; `#347`, `#348`, `#349`, and `#350` absent)
- `Idris2#351-#355` (`#351-#355` absent in this snapshot)
- `Idris2#356-#360` (only `#356` and `#360` present in this snapshot; `#357`, `#358`, and `#359` absent)
- `Idris2#361-#365` (only `#361` and `#364` present in this snapshot; `#362`, `#363`, and `#365` absent)
- `Idris2#366-#370` (only `#367` present in this snapshot; `#366`, `#368`, `#369`, and `#370` absent)
- `Idris2#371-#375` (only `#371` and `#374` present in this snapshot; `#372`, `#373`, and `#375` absent)
- `Idris2#376-#380` (only `#377` and `#378` present in this snapshot; `#376`, `#379`, and `#380` absent)
- `Idris2#381-#385` (`#381-#385` absent in this snapshot)
- `Idris2#394-#398` (only `#394`, `#395`, `#397`, and `#398` present in this snapshot; `#396` absent)
- `Idris2#399-#405` (only `#399`, `#402`, `#403`, and `#405` present in this snapshot; `#400`, `#401`, and `#404` absent)
- `Idris2#406-#410` (only `#407` and `#409` present in this snapshot; `#406`, `#408`, and `#410` absent)
- `Idris2#411-#415` (only `#413` present in this snapshot; `#411`, `#412`, `#414`, and `#415` absent)
- `Idris2#421-#425` (only `#421` and `#423` present in this snapshot; `#422`, `#424`, and `#425` absent)
- `Idris2#426-#430` (only `#429` and `#430` present in this snapshot; `#426`, `#427`, and `#428` absent)
- `Idris2#440-#444` (only `#440`, `#441`, and `#443` present in this snapshot; `#442` and `#444` absent)
- `Idris2#445-#449` (only `#446` and `#447` present in this snapshot; `#445`, `#448`, and `#449` absent)
- `Idris2#450-#454` (only `#454` present in this snapshot; `#450`, `#451`, `#452`, and `#453` absent)
- `Idris2#659-#663` (only `#660` and `#661` present in this snapshot; `#659`, `#662`, and `#663` absent)
- `Idris2#664-#668` (only `#664` and `#666` present in this snapshot; `#665`, `#667`, and `#668` absent)
- `Idris2#669-#673` (only `#670` and `#672` present in this snapshot; `#669`, `#671`, and `#673` absent)
- `Idris2#674-#678` (only `#676` and `#677` present in this snapshot; `#674`, `#675`, and `#678` absent)
- `Idris2#684-#688` (only `#685` and `#686` present in this snapshot; `#684`, `#687`, and `#688` absent)
- `Idris2#694-#698` (only `#694`, `#696`, and `#698` present in this snapshot; `#695` and `#697` absent)
- `Idris2#704-#708` (only `#704`, `#705`, and `#706` present in this snapshot; `#707` and `#708` absent)
- `Idris2#709-#713` (only `#710` and `#711` present in this snapshot; `#709`, `#712`, and `#713` absent)
- `Idris2#714-#718` (only `#714` present in this snapshot; `#715`, `#716`, `#717`, and `#718` absent)
- `Idris2#723-#727` (only `#723`, `#724`, and `#725` present in this snapshot; `#726` and `#727` absent)
- `Idris2#728-#732` (only `#728` and `#732` present in this snapshot; `#729`, `#730`, and `#731` absent)
- `Idris2#733-#737` (only `#733`, `#735`, and `#737` present in this snapshot; `#734` and `#736` absent)
- `Idris2#738-#742` (only `#738` and `#742` present in this snapshot; `#739`, `#740`, and `#741` absent)
- `Idris2#743-#747` (only `#743` and `#745` present in this snapshot; `#744`, `#746`, and `#747` absent)
- `Idris2#748-#752` (only `#751` present in this snapshot; `#748`, `#749`, `#750`, and `#752` absent)
- `Idris2#758-#762` (only `#758`, `#759`, and `#762` present in this snapshot; `#760` and `#761` absent)
- `Idris2#763-#767` (only `#763` and `#764` present in this snapshot; `#765`, `#766`, and `#767` absent)
- `Idris2#768-#772` (only `#768`, `#769`, and `#770` present in this snapshot; `#771` and `#772` absent)
- `Idris2#773-#777` (only `#773`, `#775`, `#776`, and `#777` present in this snapshot; `#774` absent)
- `Idris2#778-#782` (only `#778` and `#779` present in this snapshot; `#780`, `#781`, and `#782` absent)
- `Idris2#783-#787` (only `#783`, `#784`, and `#785` present in this snapshot; `#786` and `#787` absent)
- `Idris2#788-#792` (only `#788`, `#791`, and `#792` present in this snapshot; `#789` and `#790` absent)
- `Idris2#793-#797` (only `#794`, `#796`, and `#797` present in this snapshot; `#793` and `#795` absent)
- `Idris2#798-#802` (only `#798`, `#800`, `#801`, and `#802` present in this snapshot; `#799` absent)

Keyword scan completed on 2026-04-26 against `Idris2-issues.json` titles and bodies:

- `implicit`, `metavariable`, `elaboration`, `hole`, `coverage`, `normalization`, `erased`, `universe`, `transport`, `equality`, `with`, `total`, `termination`

Notes:

- The earliest available issue numbers in the local Idris2 archive start at `#13`; no `#1-#12` issues are present in
  this snapshot.
- After `Idris2#78`, the next available issue number in this local snapshot is `Idris2#95`; `#79-#94` are absent here.
- After `Idris2#104`, the next available issue number in this local snapshot is `Idris2#108`; `#105-#107` are absent here.
- After `Idris2#112`, the next available issue number in this local snapshot is `Idris2#114`; `#113` is absent here.
- After `Idris2#118`, the next available issue number in this local snapshot is `Idris2#125`; `#119-#124` are absent here.
- After `Idris2#129`, the next available issue number in this local snapshot is `Idris2#132`; `#130-#131` are absent here.
- After `Idris2#136`, the next available issue number in this local snapshot is `Idris2#140`; `#137-#139` are absent here.
- After `Idris2#144`, the next available issue number in this local snapshot is `Idris2#163`; `#145-#162` are absent here.
- After `Idris2#167`, the next available issue number in this local snapshot is `Idris2#173`; `#168-#172` are absent here.
- After `Idris2#177`, the next available issue number in this local snapshot is `Idris2#179`; `#178` is absent here.
- After `Idris2#180`, the next available issue number in this local snapshot is `Idris2#184`; `#181-#183` are absent here.
- After `Idris2#189`, the next available issue number in this local snapshot is `Idris2#194`; `#190-#193` are absent here.
- After `Idris2#195`, the next available issue number in this local snapshot is `Idris2#202`; `#196-#201` are absent here.
- After `Idris2#202`, the next available issue number in this local snapshot is `Idris2#204`; `#203` is absent here.
- After `Idris2#208`, the next available issue number in this local snapshot is `Idris2#209`; `#204-#207` are absent here.
- After `Idris2#215`, the next available issue number in this local snapshot is `Idris2#221`; `#216-#220` are absent here.
- After `Idris2#225`, the next available issue number in this local snapshot is `Idris2#228`; `#226-#227` are absent here.
- After `Idris2#230`, the next available issue number in this local snapshot is `Idris2#234`; `#231-#233` are absent here.
- After `Idris2#234`, the next available issue number in this local snapshot is `Idris2#240`; `#235-#239` are absent here.
- After `Idris2#240`, the next available issue number in this local snapshot is `Idris2#243`; `#241-#242` are absent here.
- After `Idris2#243`, the next available issue number in this local snapshot is `Idris2#253`; `#244-#252` are absent here.
- After `Idris2#255`, the next available issue number in this local snapshot is `Idris2#258`; `#256-#257` are absent here.
- After `Idris2#259`, the next available issue number in this local snapshot is `Idris2#261`; `#260` is absent here.
- After `Idris2#264`, the next available issue number in this local snapshot is `Idris2#268`; `#265-#267` are absent here.
- After `Idris2#270`, the next available issue number in this local snapshot is `Idris2#271`; `#269` is absent here.
- After `Idris2#271`, the next available issue number in this local snapshot is `Idris2#276`; `#272-#275` are absent here.
- After `Idris2#276`, the next available issue number in this local snapshot is `Idris2#293`; `#277-#292` are absent here.
- After `Idris2#297`, the next available issue number in this local snapshot is `Idris2#300`; `#298-#299` are absent here.
- After `Idris2#301`, the next available issue number in this local snapshot is `Idris2#304`; `#302-#303` are absent here.
- After `Idris2#307`, the next available issue number in this local snapshot is `Idris2#308`; `#306` is absent here.
- After `Idris2#311`, the next available issue number in this local snapshot is `Idris2#317`; `#312-#316` are absent here.
- After `Idris2#317`, the next available issue number in this local snapshot is `Idris2#331`; `#318-#330` are absent here.
- After `Idris2#331`, the next available issue number in this local snapshot is `Idris2#338`; `#332-#337` are absent here.
- After `Idris2#338`, the next available issue number in this local snapshot is `Idris2#346`; `#339-#345` are absent here.
- After `Idris2#346`, the next available issue number in this local snapshot is `Idris2#356`; `#347-#355` are absent here.
- After `Idris2#356`, the next available issue number in this local snapshot is `Idris2#360`; `#357-#359` are absent here.
- After `Idris2#360`, the next available issue number in this local snapshot is `Idris2#361`.
- After `Idris2#361`, the next available issue number in this local snapshot is `Idris2#364`; `#362-#363` are absent here.
- After `Idris2#364`, the next available issue number in this local snapshot is `Idris2#367`; `#365-#366` are absent here.
- After `Idris2#367`, the next available issue number in this local snapshot is `Idris2#371`; `#368-#370` are absent here.
- After `Idris2#371`, the next available issue number in this local snapshot is `Idris2#374`; `#372-#373` are absent here.
- After `Idris2#374`, the next available issue number in this local snapshot is `Idris2#377`; `#375-#376` are absent here.
- After `Idris2#378`, the next available issue number in this local snapshot is `Idris2#394`; `#379-#393` are absent here.
- After `Idris2#395`, the next available issue number in this local snapshot is `Idris2#397`; `#396` is absent here.
- After `Idris2#399`, the next available issue number in this local snapshot is `Idris2#402`; `#400-#401` are absent here.
- After `Idris2#403`, the next available issue number in this local snapshot is `Idris2#405`; `#404` is absent here.
- After `Idris2#405`, the next available issue number in this local snapshot is `Idris2#407`; `#406` is absent here.
- After `Idris2#407`, the next available issue number in this local snapshot is `Idris2#409`; `#408` is absent here.
- After `Idris2#409`, the next available issue number in this local snapshot is `Idris2#413`; `#410-#412` are absent here.
- After `Idris2#413`, the next available issue number in this local snapshot is `Idris2#421`; `#414-#420` are absent here.
- After `Idris2#423`, the next available issue number in this local snapshot is `Idris2#429`; `#424-#428` are absent here.
- After `Idris2#430`, the next available issue number in this local snapshot is `Idris2#440`; `#431-#439` are absent here.
- After `Idris2#443`, the next available issue number in this local snapshot is `Idris2#446`; `#444-#445` are absent here.
- After `Idris2#447`, the next available issue number in this local snapshot is `Idris2#454`; `#448-#453` are absent here.
- After `Idris2#454`, the next available issue number in this local snapshot is `Idris2#457`; `#455-#456` are absent here.
- After `Idris2#458`, the next available issue number in this local snapshot is `Idris2#460`; `#459` is absent here.
- After `Idris2#461`, the next available issue number in this local snapshot is `Idris2#468`; `#462-#467` are absent here.
- After `Idris2#468`, the next available issue number in this local snapshot is `Idris2#470`; `#469` is absent here.
- After `Idris2#475`, the next available issue number in this local snapshot is `Idris2#484`; `#476-#483` are absent here.
- After `Idris2#486`, the next available issue number in this local snapshot is `Idris2#489`; `#487-#488` are absent here.
- After `Idris2#493`, the next available issue number in this local snapshot is `Idris2#495`; `#494` is absent here.
- After `Idris2#496`, the next available issue number in this local snapshot is `Idris2#499`; `#497-#498` are absent here.

Purpose: collect Idris2 dependent-proof, elaboration, hole, and normalization issues that map onto Kappa's explicit
implicit-parameter, elaboration, and dependent-typing rules.

Idris-specific triage policy:

- A non-direct-fit Idris2 issue should not be treated as disposable background by default.
- If it looks plausibly useful for Kappa's future language surface, tooling surface, proof-search story, or incremental
  compilation behavior, it should usually become a `SPEC_ADDITIONS.md` feature/tooling requirement note rather than
  being parked as noise.
- "Background / Poor Fits" below therefore means "not a current direct conformance fixture," not "irrelevant."

## Strongest Kappa Fits

| source | bucket | spec_refs | fit | current_coverage | fixture_kind | expected_outcome | proposed_fixture_root | status | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Idris2#14` | `implicit_lambda_binders` | `§7.2.1`, `§7.3` | `direct` | `none` | `single-file` | `positive-run` | `expressions.implicit_parameters.positive_lambda_binds_implicit_argument_in_context` | `accepted-now` | Staged in `new-tests/`; a lambda used in expression context should be allowed to introduce an implicit binder explicitly and then elaborate under the contextual function type. |
| `Idris2#20` | `local_let_layout_without_ascription` | `§3.4`, `§6.3.1` | `direct` | `partial` | `single-file` | `positive-run` | `declarations.let_in.positive_multiline_initializer_without_ascription` | `accepted-now` | Staged in `new-tests/`; a local `let` without a separate local signature should still accept a multiline indented initializer and then bind the resulting value in the surrounding block. |
| `Idris2#27` | `residual_catch_all_constructor_refinement` | `§7.4.1`, `§7.5.4`, `§2.8.3` | `direct` | `none` | `single-file` | `positive-run` | `expressions.match.positive_residual_catch_all_derives_remaining_constructor` | `accepted-now` | Staged in `new-tests/`; after an unguarded `None` branch fails, the residual catch-all branch may use the remaining `Some`-like constructor refinement for projection. |
| `Idris2#31` | `local_value_signature_layout` | `§3.4`, `§6.3.1` | `direct` | `partial` | `single-file` | `positive-run` | `declarations.let_in.positive_local_value_signature_indented_definition` | `accepted-now` | Staged in `new-tests/`; a local signature followed by an indented local `let` definition and final expression should parse as one block-scoped declaration sequence. |
| `Idris2#41` | `range_driven_list_comprehension` | `§10.2`, `§10.3`, `§10.10.1` | `direct` | `none` | `single-file` | `positive-run` | `queries.for_clauses.positive_range_generator_nested_list_comprehension` | `accepted-now` | Staged in `new-tests/`; a list comprehension may iterate a portable `Rangeable` scalar range and build nested list/tuple structure without spurious range-instance inference failure. |
| `Idris2#42` | `portable_filter_prelude_surface` | `§2.6`, `§10.10.1`, `§17.1` | `direct` | `none` | `single-file` | `positive-run` | `modules.prelude.contents.positive_filter_term` | `accepted-now` | Staged in `new-tests/`; the portable prelude surface includes `filter`, and importing it explicitly should allow direct list filtering. |
| `Idris2#50` | `nested_record_projection_read` | `§2.8.3`, `§5.5.5` | `direct` | `partial` | `single-file` | `positive-run` | `types.records.field_projection.positive_nested_field_read` | `accepted-now` | Staged in `new-tests/`; Idris's record-extraction sugar maps directly to ordinary dotted nested record projection in Kappa. |
| `Idris2#56` | `default_member_sees_all_supertraits` | `§12.1`, `§12.2.1` | `direct` | `partial` | `single-file` | `positive-run` | `traits.members.positive_default_member_uses_multiple_supertraits` | `accepted-now` | Staged in `new-tests/`; a default trait member should typecheck using all declared supertrait members under the same type argument. |
| `Idris2#60` | `multi_parameter_trait_instance_preserves_argument_order` | `§12.1`, `§12.2.2` | `direct` | `none` | `single-file` | `positive-run` | `traits.instances.positive_multi_parameter_trait_instance_preserves_argument_order` | `accepted-now` | Staged in `new-tests/`; a higher-kinded multi-parameter trait instance should preserve the declared argument order through member types during implementation checking. |
| `Idris2#62` | `lambda_body_match_expression` | `§7.2.1`, `§7.5` | `direct` | `partial` | `single-file` | `positive-run` | `expressions.lambdas.positive_lambda_body_is_match_expression` | `accepted-now` | Staged in `new-tests/`; a lambda body may be a `match` expression and then be applied immediately without spurious delay/type artifacts. |
| `Idris2#63` | `mixed_local_signature_and_inferred_binding` | `§6.3.1` | `direct` | `none` | `single-file` | `positive-run` | `declarations.let_in.positive_mixed_local_signature_and_inferred_binding` | `accepted-now` | Staged in `new-tests/`; a local block may mix one signed local definition with another inferred local binding without forcing every local item to carry a separate signature. |
| `Idris2#66` | `higher_kinded_trait_instance_resolves_without_explicit_head_argument` | `§12.2.2`, `§12.3.1` | `direct` | `none` | `single-file` | `positive-run` | `traits.instances.positive_higher_kinded_instance_resolves_without_explicit_head_argument` | `accepted-now` | Staged in `new-tests/`; a higher-kinded trait instance should be found from the result type without forcing the head constructor argument to be written explicitly at use sites. |
| `Idris2#67` | `trait_member_signature_accepts_implicit_parameter` | `§12.1`, `§7.3` | `direct` | `none` | `single-file` | `positive-run` | `traits.members.positive_member_signature_accepts_implicit_parameter` | `accepted-now` | Staged in `new-tests/`; a trait member signature may itself declare an implicit parameter and instances may implement it normally. |
| `Idris2#72` | `supertrait_projection_satisfies_weaker_helper_constraint` | `§7.3.3`, `§12.1.1`, `§12.3.1` | `direct` | `partial` | `single-file` | `positive-run` | `expressions.implicit_parameters.positive_supertrait_projection_for_weaker_helper_constraint` | `accepted-now` | Staged in `new-tests/`; an in-scope stronger trait dictionary should satisfy helper calls that require a declared supertrait rather than forcing the weaker evidence to be passed explicitly. |
| `Idris2#73` | `function_type_quantity_affects_definitional_equality` | `§5.1.5`, `§14.3` | `direct` | `real-fixture` | `single-file` | `negative-compile` | `types.universes.quantities.negative_function_type_quantity_equality` | `already-covered` | Already covered in the main fixture suite; function types that differ only by binder quantity are not definitionally equal. |
| `Idris2#78` | `portable_bytes_prelude_surface` | `§2.6`, `§5.1.2` | `direct` | `none` | `single-file` | `positive-run` | `modules.prelude.contents.positive_bytes_type` | `accepted-now` | Staged in `new-tests/`; the portable prelude exports `Bytes` as a first-class ordinary type. |
| `Idris2#110` | `constructor_pattern_binder_shadows_function_name` | `§7.6.1`, `§7.6.2` | `direct` | `none` | `single-file` | `positive-run` | `expressions.patterns.positive_constructor_pattern_binder_shadows_function_name` | `accepted-now` | Staged in `new-tests/`; a constructor-pattern binder may shadow the surrounding function name without confusing elaboration or name resolution. |
| `Idris2#112` | `integer_literal_case_arms_distinguish_values` | `§4.1`, `§7.5`, `§7.6.1` | `direct` | `none` | `single-file` | `positive-run` | `expressions.match.runtime_positive_integer_literal_cases_distinguish_values` | `accepted-now` | Staged in `new-tests/`; integer literal case arms must distinguish decimal/hex/octal values by their actual numeric meaning rather than collapsing to the first branch. |
| `Idris2#116` | `do_block_final_alias_preserves_tail_expression_type` | `§8.2.5`, `§14.1.1` | `direct` | `none` | `single-file` | `positive-run` | `effects.do_blocks.runtime_positive_final_alias_preserves_tail_expression` | `accepted-now` | Staged in `new-tests/`; replacing a final `do` expression with a local alias and then returning that alias must preserve typing and execution. |
| `Idris2#164` | `local_impossible_case_uses_same_coverage_reasoning_as_top_level` | `§6.3.1`, `§7.5.2A`, `§17.3.6` | `direct` | `none` | `single-file` | `positive-run` | `declarations.let_in.positive_local_impossible_case_preserves_coverage` | `accepted-now` | Staged in `new-tests/`; a local signed helper over a refined indexed type should accept `case impossible` using the same coverage reasoning as an equivalent top-level helper. |
| `Idris2#188` | `tuple_components_accept_quantified_function_types` | `§4.5`, `§5.1.5`, `§5.5` | `direct` | `partial` | `single-file` | `positive-run` | `types.tuples.positive_quantity_annotated_function_components` | `accepted-now` | Staged in `new-tests/`; tuple component binders may carry quantity annotations even when the component type is itself a function type. |
| `Idris2#189` | `identity_preserves_linear_function_argument_quantities` | `§7.1.3`, `§14.3` | `direct` | `none` | `single-file` | `positive-run` | `types.universes.quantities.qtt.positive_identity_preserves_linear_function_argument_quantity` | `accepted-now` | Staged in `new-tests/`; expected linear binder quantities on function arguments should be preserved through ordinary identity-style wrappers in argument position. |
| `Idris2#670` | `polymorphic_identity_specializes_to_linear_function_transformer` | `§7.1.3`, `§14.3` | `direct` | `staged-fixture` | `single-file` | `positive-run` | `types.universes.quantities.qtt.positive_identity_preserves_linear_function_argument_quantity` | `already-covered` | Covered by the staged linear-identity fixture: ordinary polymorphic identity remains usable at a demanded higher-order linear-function type without requiring explicit type application or eta-expansion from the caller. |
| `Idris2#202` | `indexed_match_must_reject_missing_reachable_constructor` | `§7.5.1`, `§7.5.1A`, `§17.3.6` | `direct` | `none` | `single-file` | `negative-check` | `expressions.match.negative_indexed_missing_reachable_constructor` | `accepted-now` | Staged in `new-tests/`; after index refinement removes impossible constructors, a match must still reject omission of any remaining reachable constructor. |
| `Idris2#208` | `irrefutable_tuple_destructure_preserves_definitional_equality` | `§6.1.2`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_irrefutable_tuple_let_normalizes` | `accepted-now` | Staged in `new-tests/`; definitional equality should see through an irrefutable tuple destructure used to define an equivalent tuple-producing function. |
| `Idris2#214` | `equality_refinement_does_not_collapse_distinct_linear_binders` | `§5.1.5`, `§5.6.1`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `types.universes.quantities.qtt.positive_equality_match_preserves_distinct_linear_binders` | `accepted-now` | Staged in `new-tests/`; matching on erased equality evidence may refine types, but it must not merge two distinct linear binder obligations into one use-counted runtime variable. |
| `Idris2#225` | `implicit_function_binder_hygiene_avoids_outer_name_clash` | `§5.3.3`, `§7.3`, `§17.3.4` | `direct` | `none` | `single-file` | `positive-check` | `expressions.implicit_parameters.positive_alias_implicit_binder_avoids_signature_name_clash` | `accepted-now` | Staged in `new-tests/`; an implicit binder introduced by a type alias must alpha-separate cleanly from the same spelling in an enclosing signature instead of being mistaken for a repeated pattern/name binder. |
| `Idris2#230` | `alpha_renamed_if_codomains_remain_definitionally_equal` | `§5.6.1`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_alpha_renamed_if_codomains_equal` | `accepted-now` | Staged in `new-tests/`; definitional equality must treat `(c : Bool) -> if c then a else b` and `(y : Bool) -> if y then a else b` as the same type up to alpha-renaming of the binder. |
| `Idris2#243` | `type_level_if_and_match_reduce_in_signatures` | `§7.5`, `§14.3`, `§14.3A` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_type_level_if_and_match_reduce_in_signatures` | `accepted-now` | Staged in `new-tests/`; a dependent type alias defined by `if` or `match` must normalize to the selected branch when used in a signature, rather than collapsing to the first branch. |
| `Idris2#255` | `local_signed_let_does_not_lift_sibling_binders` | `§6.3.1`, `§14.1.1` | `direct` | `staged-fixture` | `single-file` | `positive-run` | `declarations.let_in.positive_mixed_local_signature_and_inferred_binding` | `already-covered` | Covered by the strengthened staged local-`let` fixture: a signed local definition inside a block does not force sibling inferred locals before or after it to be treated as top-level declarations. |
| `Idris2#270` | `ambiguous_local_implicit_candidates_rejected_with_structured_ambiguity_diagnostic` | `§7.3.3`, `§17.2.4A.3` | `direct` | `real-fixture` | `single-file` | `negative-check` | `implicit_reject_same_scope_ambiguous_candidates` | `already-covered` | Already covered in the main fixture suite; when one lexical implicit scope contains two valid candidates for one ordinary implicit goal, elaboration must reject the call as ambiguous, and the standardized diagnostic family is `kappa.implicit.ambiguous` rather than an implementation-internal search artifact. |
| `Idris2#276` | `package_mode_module_header_matches_path_relative_to_source_root` | `§2.1` | `direct` | `real-fixture` | `single-file` | `negative-check` | `modules.files.negative_header_mismatch` | `already-covered` | Already covered in the main fixture suite; package-mode module-header checking is defined relative to the selected source root, so diagnostics must compare the header against the path-derived module name rather than the raw filesystem path. |
| `Idris2#307` | `unused_local_match_initializer_does_not_affect_result_inference` | `§6.3.1`, `§7.5` | `direct` | `none` | `single-file` | `positive-run` | `declarations.let_in.positive_unused_match_initializer_does_not_affect_result` | `accepted-now` | Staged in `new-tests/`; an unused local binding initialized by a `match` expression should typecheck normally and not force the enclosing block result through the local initializer's type. |
| `Idris2#338` | `duplicate_constructor_declarations_rejected_in_one_data_block` | `§2.8`, `§11.1` | `direct` | `fuzz-pending fixture` | `single-file` | `negative-check` | `data_types.data_declarations.negative_duplicate_constructor_declaration` | `accepted-now` | Staged in `new-tests/`; two constructors of one data declaration must not reuse the same constructor spelling, even when the rest of the declaration remains well-typed. |
| `Idris2#364` | `trait_member_chain_resolves_single_dictionary_from_result_type` | `§7.3.3`, `§12.3.1` | `direct` | `partial` | `single-file` | `positive-run` | `expressions.implicit_parameters.positive_trait_member_chain_resolves_from_result_type` | `accepted-now` | Staged in `new-tests/`; when an expression is typed under `Trait a => a`, one resolved dictionary should consistently drive all member uses in the chain even if the result type is the only source of `a`. |
| `Idris2#367` | `parenthesized_equality_operator_name_accepted_in_type_positions` | `§3.5.1`, `§5.6.1` | `direct` | `none` | `single-file` | `positive-check` | `types.propositions.positive_parenthesized_equality_operator_in_type_positions` | `accepted-now` | Staged in `new-tests/`; a parenthesized operator name such as `(=)` must parse and elaborate as an ordinary type-level name in direct type arguments and eta-reduced type aliases. |
| `Idris2#402` | `multiple_linear_indexed_singletons_cover_in_clause_heads` | `§5.1.5`, `§7.5.1A`, `§17.3.6` | `direct` | `none` | `single-file` | `positive-run` | `expressions.patterns.positive_linear_indexed_singletons_cover_clause_heads` | `accepted-now` | Staged in `new-tests/`; when each linear argument is an indexed singleton family, a direct clause-head definition matching all singleton constructors should count as exhaustive without requiring a synthetic wildcard residue. |
| `Idris2#409` | `free_lowercase_identifier_generalized_when_first_used_inside_type_level_match` | `§5.3.3`, `§7.5`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `declarations.signatures.positive_implicit_generalization_inside_type_level_match` | `accepted-now` | Staged in `new-tests/`; declaration-level universalization should still introduce a binder when a free lowercase identifier first appears only inside a type-level `match` in the signature. |
| `Idris2#421` | `record_update_may_explicitly_repair_implicit_compile_time_field` | `§5.5.5`, `§5.5.9` | `direct` | `real-fixture` | `single-file` | `positive-check` | `types.records.update.positive_implicit_compile_time_field_repair` | `already-covered` | Already covered in the main fixture suite; record update may explicitly repair an implicit compile-time field with `@label = expr`, and omitted dependent fields are then rechecked against the updated witness. |
| `Idris2#429` | `record_update_rejects_stale_implicit_dependent_field_after_witness_change` | `§5.5.5`, `§5.5.9`, `§17.2.13.1` | `direct` | `real-fixture` | `single-file` | `negative-check` | `types.records.update.negative_stale_implicit_compile_time_dependent_field` | `already-covered` | Already covered in the main fixture suite; updating only an implicit compile-time witness while omitting a dependent payload field must fail when the copied payload no longer matches the repaired witness. |
| `Idris2#441` | `parameter_name_does_not_capture_record_field_projection` | `§5.5.4`, `§7.1.1` | `direct` | `none` | `single-file` | `positive-run` | `types.records.field_projection.positive_parameter_name_does_not_capture_field_selector` | `accepted-now` | Staged in `new-tests/`; a function parameter may share a spelling with a record field name, and `record.field` must still resolve as ordinary field projection rather than re-parsing or capturing the parameter binder. |
| `Idris2#457` | `record_update_projection_normalizes_without_manual_destructure` | `§5.5.5`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_record_update_projection_normalizes` | `accepted-now` | Staged in `new-tests/`; ordinary record update must expose the updated field projection to definitional equality exactly as the full-record reconstruction rule promises. |
| `Idris2#484` | `final_case_impossible_must_not_mask_remaining_reachable_cases` | `§7.5.2A`, `§17.3.6` | `direct` | `none` | `single-file` | `negative-check` | `expressions.match.negative_impossible_does_not_mask_missing_reachable_constructor` | `accepted-now` | Staged in `new-tests/`; a final `case impossible` is rejected whenever a reachable constructor remains uncovered rather than accidentally short-circuiting the coverage check. |
| `Idris2#496` | `local_instance_does_not_escape_lexical_block_scope` | `§6.3.1`, `§7.3.3`, `§12.3`, `§14.1.1` | `direct` | `none` | `single-file` | `negative-check` | `traits.instances.negative_local_instance_does_not_escape_block_scope` | `accepted-now` | Staged in `new-tests/`; a local instance may resolve implicit goals inside its declaring block but must not enter the global instance environment afterward. |
| `Idris2#499` | `explicit_erased_witness_pattern_on_transparent_package` | `§5.5.9`, `§7.3.3`, `§7.6.1` | `direct` | `none` | `single-file` | `positive-run` | `types.records.dependent.positive_explicit_erased_witness_pattern_on_package` | `accepted-now` | Staged in `new-tests/`; an erased proof field of a transparent dependent package may be matched explicitly with an ordinary pattern, and the explicitly bound witness still participates in the branch-local implicit context. |
| `Idris2#504` | `same_spelling_type_and_constructor_binding_group` | `§2.8.1`, `§2.8.2` | `direct` | `none` | `single-file` | `positive-run` | `data_types.data_declarations.positive_same_spelling_type_and_constructor` | `accepted-now` | Staged in `new-tests/`; a `data` declaration may expose one same-spelling type facet and constructor facet, with type positions resolving the type and term/pattern positions resolving the constructor. |
| `Idris2#518` | `local_signature_accepts_unparenthesized_equality_type` | `§5.6.1`, `§6.3.1` | `direct` | `none` | `single-file` | `positive-check` | `declarations.let_in.positive_local_signature_equality_type_without_parentheses` | `accepted-now` | Staged in `new-tests/`; a block-local signature may place an equality type directly after `:` without requiring extra parentheses around `x = y`. |
| `Idris2#550` | `nested_list_head_patterns_cover_all_real_argument_pairs` | `§7.5.1`, `§7.5.1A`, `§14.3A` | `direct` | `none` | `single-file` | `positive-run` | `expressions.match.positive_nested_list_head_patterns_cover_all_pairs` | `accepted-now` | Staged in `new-tests/`; once the empty-head, empty-list, and non-empty-head combinations are all covered, the coverage checker must not invent a residual missing `(_ :: _) :: _` pair case. |
| `Idris2#553` | `named_constructor_application_by_field_names` | `§7.1.3C`, `§11.1.1` | `direct` | `real-fixture` | `single-file` | `positive-run` | `data_types.constructor_defaults.positive_named_defaults` | `already-covered` | Already covered in the staged/real fixture set; constructors whose explicit parameters have names may be instantiated with record-like named arguments such as `User { name = ..., age = ... }`, including reordered fields and preserved constructor metadata. |
| `Idris2#574` | `large_literal_constructor_equality_refinement_normalizes_for_coverage` | `§5.6.1`, `§7.5.1`, `§14.3.1` | `direct` | `none` | `single-file` | `positive-run` | `expressions.match.positive_large_literal_constructor_equality_refinement` | `accepted-now` | Staged in `new-tests/`; matching on equality evidence that fixes a constructor payload to a larger Nat literal must still accept the sole `refl` branch instead of inventing a bogus non-covering residual path. |
| `Idris2#589` | `function_projection_preserves_dependent_package_proof_field` | `§5.5.9`, `§6.3.1`, `§14.3` | `direct` | `none` | `single-file` | `positive-run` | `types.records.dependent.positive_function_projection_preserves_dependent_proof_field` | `accepted-now` | Staged in `new-tests/`; projecting a function field from a transparent dependent package and reusing it via a local alias must preserve the dependent proof-field type without forcing a manual ascription. |
| `Idris2#601` | `float_signed_zeroes_not_definitionally_equal` | `§4.1.3`, `§4.1.4`, `§5.6.1` | `direct` | `none` | `single-file` | `negative-check` | `types.propositions.negative_float_signed_zero_raw_bit_equality` | `accepted-now` | Staged in `new-tests/`; propositional equality on floating-point values follows raw-bit equality, so `-0.0` and `0.0` must not be accepted as equal via `refl`. |
| `Idris2#621` | `top_level_constants_reducing_to_same_literal_are_defeq` | `§5.6.1`, `§6.4.1`, `§14.3` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_top_level_constant_aliases_reduce_for_refl` | `accepted-now` | Staged in `new-tests/`; two transparent top-level constants that both reduce to the same literal remain definitionally equal, so `refl` is accepted at their equality type. |
| `Idris2#628` | `constructor_named_fields_do_not_become_record_update_paths` | `§5.5.5`, `§11.1.1` | `direct` | `none` | `single-file` | `negative-check` | `types.records.update.negative_constructor_field_path` | `accepted-now` | Staged in `new-tests/`; named fields on an ordinary `data` constructor do not make values of that datatype valid record-update scrutinees. |
| `Idris2#647` | `higher_kinded_constraint_method_solves_inside_if_and_case` | `§7.3.3`, `§7.4`, `§12.3.1` | `direct` | `none` | `single-file` | `positive-run` | `expressions.implicit_parameters.positive_constraint_method_inside_if_and_case` | `accepted-now` | Staged in `new-tests/`; a constrained higher-kinded method call should solve its hidden type parameters the same way under direct `if`/`match` use as when first bound to a local name. |
| `Idris2#661` | `erased_package_witness_constructor_match_allowed_when_runtime_field_forces_shape` | `§5.5.9`, `§7.5.1B`, `§14.3A` | `direct` | `partial` | `single-file` | `positive-run` | `types.records.dependent.positive_erased_witness_pattern_refines_with_forcing_field` | `accepted-now` | Staged in `new-tests/`; when a transparent dependent package carries both a runtime field and an erased singleton witness indexed by that field, matching the erased witness constructor is allowed because the runtime field already forces the constructor shape. |
| `Idris2#657` | `default_trait_member_uses_current_associated_static_member` | `§12.2`, `§12.2.1` | `direct` | `none` | `single-file` | `positive-run` | `traits.members.positive_default_member_uses_associated_static_member` | `accepted-now` | Staged in `new-tests/`; a default trait member should typecheck with access to the current trait parameter and the current associated static member. |
| `Idris2#658` | `later_trait_member_signature_may_mention_earlier_member_in_implicit_binder` | `§7.3.3`, `§12.2`, `§12.2.1` | `direct` | `none` | `single-file` | `positive-run` | `traits.members.positive_member_signature_uses_prior_trait_member_in_implicit_argument` | `accepted-now` | Staged in `new-tests/`; a later trait member signature may mention an earlier trait member inside an implicit proof binder, and the instance implementation should elaborate normally. |
| `Idris2#694` | `local_match_binder_shadows_surrounding_local_definition` | `§2.8.1`, `§6.3.1`, `§7.5` | `direct` | `none` | `single-file` | `positive-run` | `declarations.let_in.positive_local_match_binder_shadows_local_definition` | `accepted-now` | Staged in `new-tests/`; a catch-all `match` binder inside a local recursive-capable definition should shadow the surrounding local definition name rather than accidentally referring back to the definition itself. |
| `Idris2#696` | `local_type_helper_reduces_when_alias_used_in_later_signature` | `§5.1.4.2`, `§6.3.1`, `§14.3`, `§14.3A` | `direct` | `none` | `single-file` | `positive-check` | `definitional_equality.positive_local_type_helper_reduces_in_later_signature` | `accepted-now` | Staged in `new-tests/`; a transparent local helper inside a type alias body must still normalize when a later signature instantiates that alias and branch checking needs the selected result type. |
| `Idris2#706` | `paired_transparent_dependent_packages_share_explicit_index` | `§5.5.9`, `§14.1.1` | `direct` | `none` | `single-file` | `positive-check` | `types.records.dependent.positive_two_packages_share_explicit_index` | `accepted-now` | Staged in `new-tests/`; two transparent dependent packages parameterized by the same explicit index should both project fields at that same index without spuriously diverging into unrelated witnesses. |
| `Idris2#711` | `line_comment_may_contain_block_comment_closer_text` | `§3.3` | `direct` | `none` | `single-file` | `positive-run` | `lexical.comments.positive_line_comment_with_block_close_text` | `accepted-now` | Staged in `new-tests/`; the token sequence `--}` starts an ordinary line comment and must not be misparsed as a stray block-comment terminator. |
| `Idris2#714` | `duplicate_compile_time_syntax_in_quote_helper_is_not_linear_overuse` | `§5.1.4.1`, `§5.8.1`, `§5.8.2` | `direct` | `none` | `single-file` | `positive-run` | `macros.elab_phase.positive_duplicate_compile_time_syntax_in_local_quote_helper` | `accepted-now` | Staged in `new-tests/`; duplicating a compile-time `Syntax` value inside a local quote helper is meta-level duplication only and must not be rejected as repeated use of a linear object-language binder. |
| `Idris2#733` | `indexed_witness_constructor_refines_outer_arguments_and_hidden_indices` | `§5.6.1`, `§7.5.1A`, `§14.3A` | `direct` | `none` | `single-file` | `positive-run` | `expressions.match.positive_indexed_witness_refines_outer_arguments` | `accepted-now` | Staged in `new-tests/`; matching an indexed witness constructor must refine the outer arguments and hidden indices strongly enough that branch-local equalities such as `xs = Nil` and `ys = Nil` reduce by `refl`. |
| `Idris2#743` | `unrestricted_function_value_not_assignable_to_linear_function_type` | `§5.1.5`, `§7.1.3`, `§14.3` | `direct` | `none` | `single-file` | `negative-check` | `types.universes.quantities.qtt.negative_unrestricted_function_value_not_assignable_to_linear` | `accepted-now` | Staged in `new-tests/`; outermost-arrow subsumption is confined to application boundaries, so an unrestricted function value is not implicitly assignable to a linear function type at an ordinary definition boundary. |
| `Idris2#758` | `monadic_bind_preserves_linear_function_binder_annotation` | `§5.1.5`, `§8.2` | `direct` | `real-fixture` | `single-file` | `positive-check` | `effects.do_blocks.positive_monadic_bind_preserves_linearity` | `already-covered` | Already covered in the main fixture suite; extracting a linear function through a `do`-block bind must preserve the function binder's linear quantity so applying it to a linear argument remains well-typed. |
| `Idris2#763` | `bare_overloaded_member_passes_as_constrained_polymorphic_function_value` | `§7.3.3`, `§12.2.1` | `direct` | `none` | `single-file` | `positive-run` | `expressions.implicit_parameters.positive_pass_member_as_constrained_polymorphic_value` | `accepted-now` | Staged in `new-tests/`; passing a bare overloaded trait member as a higher-rank constrained function value must not trigger premature trait search at the passing site. |
| `Idris2#764` | `do_bound_value_remains_match_scrutinee_without_redundant_annotation` | `§7.5`, `§8.2`, `§14.1.1` | `direct` | `none` | `single-file` | `positive-run` | `effects.do_blocks.positive_do_bound_scrutinee_match_infers_type` | `accepted-now` | Staged in `new-tests/`; a value bound by `do` must remain a valid `match` scrutinee without forcing a redundant local type annotation. |
| `Idris2#802` | `typed_array_context_forces_integer_literals_to_double` | `§4.1.5`, `§10.2` | `direct` | `none` | `single-file` | `positive-check` | `literals.numeric_literals.positive_integer_literals_in_array_double_context` | `accepted-now` | Staged in `new-tests/`; an explicitly typed `Array Double` should contextually elaborate unsuffixed integer literals as `Double` values without needing per-element coercion annotations. |

## Background / Poor Fits

- `Idris2#13`
  - tracker / repo-management issue only. It is not a language or compiler-semantics candidate.

- `Idris2#15`
  - dependent elaboration / metavariable-cycle robustness background only for now. It points at solver/elaboration
    fragility, but the issue body does not yet provide a compact transferable source rule for a Kappa conformance
    fixture.

- `Idris2#16`
  - runtime complexity / performance background only. This is useful as implementation pressure, not as a new Kappa
    source-level acceptance rule.

- `Idris2#17`
  - Windows build / FFI-platform background only.

- `Idris2#18`
  - REPL code-generation / derive-definition tooling background only. It is not a direct source-semantics obligation for Kappa.

- `Idris2#19`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a direct totality / termination fit; keep it cross-linked there rather than duplicating the staging logic in this note.

- `Idris2#21`
  - hole-display / REPL tooling robustness background only. It reinforces the importance of elaboration tooling quality, but does not add a current conformance fixture.

- `Idris2#22`
  - imported-definition normalization / ambiguous-name reduction background only. The transferable concern is real, but the issue is phrased as REPL evaluation behavior rather than a compact current Kappa source rule.

- `Idris2#23`
  - REPL reload timestamp / file-watch tooling background only.

- `Idris2#24`
  - import-order-sensitive elaboration / normalization background only for now. It is a real implementation-stability concern, but the issue body does not isolate a compact current Kappa source rule.

- `Idris2#25`
  - library API ergonomics background only. It is about `Either` branch convention, not a current Kappa source-semantics obligation.

- `Idris2#26`
  - good evidence for a future spec-clarity note around branch-local normalization under ruled-out patterns, but not yet a clean
    current-spec direct-fit fixture. Kappa explicitly specifies residual negative constructor evidence and remaining-constructor
    refinement, but it does not yet spell out the stronger definitional-equality normalization that this Idris2 issue wants.

- `Idris2#28`
  - nested helper-lowering / dependent-pattern propagation background for now. It is closest to the already-covered
    `with`-lowering / dependent-relation family, but the issue body is still too Idris-specific to stage as a compact
    Kappa source fixture.

- `Idris2#29`
  - numeric-library / portable-prelude surface evidence only. It is relevant as a possible future `Float` / `Double`
    constants note, not as a dependent-proof fixture.

- `Idris2#30`
  - compiler implementation / temporary-file security background only.

- `Idris2#32`
  - prelude install / file I/O build background only.

- `Idris2#33`
  - native Windows build / packaging background only.

- `Idris2#34`
  - elaboration / implicit-search ambiguity background only for now. It is a real search-quality problem, but the issue
    body does not isolate a compact Kappa source rule beyond general ambiguity diagnostics already tracked elsewhere.

- `Idris2#35`
  - diagnostic pretty-printing background only. It reinforces the existing need for collision-avoiding binder names in
    diagnostics, but does not add a new acceptance test in this dependent-proof tranche.

- `Idris2#36`
  - FFI nested-struct layout background only in this note. Keep it visible for the separate FFI/runtime wave rather than
    staging it as a dependent-proof fixture here.

- `Idris2#37`
  - literate-file / build-path tooling background only.

- `Idris2#38`
  - AddClause indentation / REPL editor tooling background only.

- `Idris2#39`
  - AddClause multi-clause generation / REPL editor tooling background only.

- `Idris2#40`
  - AddClause operator-name rendering / REPL editor tooling background only.

- `Idris2#43`
  - file-resolution / missing-build-artifact tooling background only.

- `Idris2#44`
  - WSL install / shared-library test-environment background only.

- `Idris2#45`
  - Windows linker / runtime-library background only.

- `Idris2#46`
  - Idris-specific pattern-matching-anonymous-function syntax background for current v0.1, but it is now kept alive as
    a future ergonomic requirement candidate in `SPEC_ADDITIONS.md` rather than treated as dead noise.

- `Idris2#47`
  - backend architecture / project-planning background only.

- `Idris2#48`
  - backend debug-dump / compile-expression tooling background only.

- `Idris2#49`
  - shared-library test-environment / installation background only.

- `Idris2#51`
  - stale prelude cache / TTC-version mismatch background only.

- `Idris2#52`
  - browsable-code / documentation tooling background only.

- `Idris2#53`
  - autogenerated documentation / browsable-source tooling background only.

- `Idris2#54`
  - REPL line-editing / command-history tooling background only.

- `Idris2#55`
  - Idris-specific irrelevance model background for current v0.1, but it now feeds an explicit future feature note in
    `SPEC_ADDITIONS.md` because Kappa may want a stronger proof-only / irrelevant-definition surface later.

- `Idris2#57`
  - Idris-specific named-implementation `using` surface background for current v0.1, but it now feeds an explicit
    future feature note in `SPEC_ADDITIONS.md` for named instance-selection / local search-space control.

- `Idris2#58`
  - already-covered explicit-implicit / missing-evidence behavior. Kappa does not split Idris's auto vs non-auto
    implicit modes, and existing missing-implicit coverage already checks that unresolved implicit evidence is rejected
    rather than treated as a deferred runtime hole.

- `Idris2#59`
  - duplicate-pattern diagnostic-quality background only.

- `Idris2#61`
  - useful ergonomics request rather than current-spec gap. Kappa already accepts constructor patterns with explicit `_`
    wildcards, but does not currently specify a constructor-only pattern shorthand.

- `Idris2#64`
  - record-update inference background only for now. Kappa already treats record update as ordinary expression typing over
    a typed scrutinee and existing nested-update coverage exercises the surface more directly than the Idris hole-shaped
    inference complaint.

- `Idris2#65`
  - expression-inference / scrutinee-normalization background only for now. Kappa already accepts complex expressions as
    `match` scrutinees, and the remaining transferable concern is elaborator quality rather than a distinct source rule.

- `Idris2#68`
  - AddClause / generated-clause tooling background only. It is editor-client code-generation behavior, not a surface
    semantics obligation for Kappa.

- `Idris2#69`
  - Idris-specific `%hint` / proof-search-database background for current v0.1, but it now feeds an explicit
    proof-search feature/tooling note in `SPEC_ADDITIONS.md`.

- `Idris2#70`
  - additional evidence for the existing branch-local normalization follow-up. The transferable concern is that indexed
    branch reasoning must neither lose justified refinements nor manufacture contradictory equalities, but the exact
    Idris `with`-lowering shape is not a direct Kappa surface fixture.

- `Idris2#71`
  - Idris-specific `%search` soundness background for current v0.1, but it now feeds an explicit proof-search
    feature/tooling note in `SPEC_ADDITIONS.md`.

- `Idris2#74`
  - private-definition case-split / editor tooling background for current v0.1, but it now feeds an explicit tooling
    conformance note in `SPEC_ADDITIONS.md`.

- `Idris2#75`
  - linear-hole-context / tooling diagnostic background for current v0.1, but it now feeds an explicit tooling
    conformance note in `SPEC_ADDITIONS.md`.

- `Idris2#76`
  - backend-specific FFI callback / `%World` background only. Keep it visible for the FFI/runtime wave rather than the
    dependent-proof tranche.

- `Idris2#173`
  - additional evidence for the existing branch-local normalization / definitional-simplification follow-up. The
    transferable point is that trivial proof reduction should be allowed to use refinements established by any matched
    clause argument, not just the first one.

- `Idris2#174`
  - indexed hole-context tooling evidence. It now feeds a `SPEC_ADDITIONS.md` requirement that hole-goal queries preserve
    the same refined indexed equalities and specialized argument facts that the checker is using internally.

- `Idris2#175`
  - unnamed function/Pi binder ergonomics evidence. It now feeds a `SPEC_ADDITIONS.md` note for allowing `_` as an
    explicit unnamed binder in types when the binder name itself is not referenced.

- `Idris2#179`
  - useful defaulting-design evidence rather than a direct current-spec fixture. It now feeds a `SPEC_ADDITIONS.md` note
    asking whether numeric-literal defaulting should propagate through explicitly typed overloaded aliases or remain
    limited to literal elaboration sites.

- `Idris2#180`
  - import-alias / name-resolution diagnostic evidence rather than a direct fixture. Kappa already has clear alias
    semantics, but this issue usefully motivates a stronger diagnostic requirement around module-alias/type-name
    collisions.

- `Idris2#184`
  - case-split edit-generation tooling evidence. It now feeds the `SPEC_ADDITIONS.md` note requiring generated edits to
    be valid source or fail precisely, rather than emitting malformed binders.

- `Idris2#185`
  - lemma-extraction tooling evidence. It strengthens the same generated-edit note: editor-produced helper signatures must
    transport every required erased or implicit binder needed for the lemma to typecheck.

- `Idris2#186`
  - add-clause / definition-generation tooling evidence. It also feeds the generated-edit tooling note rather than a
    direct source-semantics fixture.

- `Idris2#187`
  - syntax-policy / operator-precedence background only. Kappa already assigns a fixed function-type grammar and does not
    need to import Idris's particular `->` precedence debate as a current spec change.

- `Idris2#194`
  - bootstrap / install-path background only.

- `Idris2#195`
  - standard-library line-reading semantics evidence. It is not a dependent-proof fixture, but it now feeds a
    `SPEC_ADDITIONS.md` portability note so the console/file line-reading story stays explicit if `std.io` grows that
    surface.

- `Idris2#208`
  - additional evidence for the normalization/definitional-equality story around irrefutable destructuring. It is staged
    directly because the Kappa analogue is compact and current-spec.

- `Idris2#209`
  - standard-library binary-I/O surface evidence rather than a direct dependent-proof fixture. It now feeds a
    `SPEC_ADDITIONS.md` portability note for reusable chunked binary file reads instead of forcing every large-file read
    through whole-file `Bytes` materialization.

- `Idris2#211`
  - literate-source parsing / performance background only. It is useful if Kappa later standardizes a literate-source
    format, but it does not currently imply a direct fixture or an immediate spec-addition note.

- `Idris2#213`
  - indexed-uninhabited diagnostic-quality evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    requirement that impossible indexed proofs should report the real contradiction rather than leaking synthetic
    implicit-binder failures from the elaborator.

- `Idris2#215`
  - recursive-family normalization / inductive-proof evidence rather than a compact direct fixture for this tranche. It
    now feeds a `SPEC_ADDITIONS.md` note that recursive transparent family reduction and equality transport should
    preserve instantiated branch-local equalities through inductive proofs.

- `Idris2#221`
  - macOS bootstrap / shell-tool portability background only. It is useful build-system evidence, but it does not
    currently imply a Kappa language, elaboration, or tooling-surface requirement.

- `Idris2#228`
  - hole/expression-search tooling evidence rather than a direct source-semantics fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that hole search and finite-inhabitant suggestion queries should be bounded or streamed,
    rather than eagerly materializing huge search spaces.

- `Idris2#234`
  - declaration-layout ergonomics evidence rather than a direct fixture. Kappa does not use Idris-style `where`
    blocks for data declarations, but the issue is still useful as a future note about whether block-introducing
    declarations may break before their line-start `=` without losing layout clarity.

- `Idris2#240`
  - declaration-head sort-inference ergonomics evidence rather than a direct fixture. Kappa currently leans toward
    explicit binder sorts, but this issue is worth preserving as a future decision about whether obviously type-level
    header binders may omit `: Type` when later uses force the sort unambiguously.

- `Idris2#253`
  - dependent-pattern / branch-local witness-access evidence rather than a compact direct fixture with today's Kappa
    surface. It now feeds a `SPEC_ADDITIONS.md` note that transparent family equalities established by constructor
    choice should preserve access to residual witnesses such as an appended-list suffix.

- `Idris2#258`
  - Idris-specific parser/precedence background only. It is about parenthesis requirements around Idris sigma-style
    `(**)` witness syntax and non-associative `=`, not a compact Kappa direct fit.

- `Idris2#259`
  - installation / bundled-package permission background only.

- `Idris2#261`
  - totality / helper-lowering evidence rather than a new direct fixture here. It folds into the existing
    `SPEC_ADDITIONS.md` note that elaboration-introduced helpers must preserve the evidence needed for termination
    checking, including nested helper chains.

- `Idris2#264`
  - test-harness / unsupported-feature execution evidence rather than a direct source fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that unsupported or not-yet-implemented behavior must fail deterministically instead of
    hanging harness-driven execution.

- `Idris2#268`
  - bootstrap / RTS heap-allocation / installation background only.

- `Idris2#270`
  - already-covered direct fit. The existing fixture `implicit_reject_same_scope_ambiguous_candidates` already checks
    that same-scope implicit ambiguity is rejected, and the current spec now goes further by standardizing the
    `kappa.implicit.ambiguous` diagnostic family and required payload.

- `Idris2#271`
  - backend/bootstrap / platform-build background only. It is a Racket-codegen-on-NixOS bootstrap failure rather than a
    compact Kappa source or tooling requirement candidate.

- `Idris2#276`
  - already-covered direct fit. The existing `modules.files.negative_header_mismatch` fixture already checks that a
    package-mode module header is validated against the path-derived module name relative to the source root, not
    against the raw filesystem path printed by a tooling entrypoint.

- `Idris2#293`
  - tracker / contribution-process meta issue only.

- `Idris2#294`
  - installation-triage template background only.

- `Idris2#295`
  - generic language-bug issue template background only.

- `Idris2#297`
  - nested helper-lowering / dependent-pattern sugar background only. It is another instance of the existing
    `with`-lowering family where sugared dependent-pair destructures must preserve parent-pattern identity, but Kappa
    does not currently expose Idris-style `with` clauses or that exact pair-pattern surface.

- `Idris2#300`
  - better tracked in `TOTALITY_ISSUE_CANDIDATES.md` as an alias-preserved structural-descent fit. The existing staged
    `declarations.totality.positive_let_alias_preserves_decrease_evidence` fixture already captures the Kappa
    obligation that transparent aliases around a decreasing recursive argument must not destroy totality checking.

- `Idris2#301`
  - Idris-specific quantity / inaccessible-elaboration background only. It was closed as expected behavior and is
    phrased around Idris's `Bind` constructor surface plus inaccessible-case restrictions rather than a compact Kappa
    source rule.

- `Idris2#304`
  - nested helper-lowering / dependent-pattern underscore-binder background only. It is another Idris `with`-subcase
    binder-preservation failure rather than a compact Kappa source rule, but it should stay visible alongside the other
    helper-lowering / dependent-pattern imports.

- `Idris2#305`
  - diagnostic-quality evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that unification
    diagnostics should not render hidden-structure mismatches as a bogus "constraint between T and T" when the real
    difference lies in generalized, implicit, or provenance-hidden structure.

- `Idris2#307`
  - direct current-spec fit. It is now staged as `declarations.let_in.positive_unused_match_initializer_does_not_affect_result`.

- `Idris2#308`
  - indexed hole-context / branch-refinement tooling evidence rather than a direct fixture. It reinforces the existing
    `SPEC_ADDITIONS.md` requirement that hole-goal reporting preserve the specialized branch-local equalities and
    instantiated argument types that checking is actually using.

- `Idris2#311`
  - semantic-reflection / reification stability evidence rather than a direct fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that semantically equivalent elaborated terms must reify canonically through the public
    reflection API instead of diverging based on environment-sensitive construction paths.

- `Idris2#317`
  - installation / documentation background only. It is a Homebrew-formula docs request rather than a language,
    elaboration, or tooling-semantics candidate for Kappa.

- `Idris2#331`
  - Idris-specific `with`-lowering / linearity-preservation evidence rather than a direct current-spec fixture. Kappa
    does not expose Idris's `with` clause surface, but this is exactly the kind of elaboration-preservation bug that
    should be carried forward as a future requirement, so it now feeds a `SPEC_ADDITIONS.md` note that
    elaboration-introduced helpers must preserve binder quantities and ownership obligations.

- `Idris2#346`
  - portable-prelude / library-surface evidence rather than a direct dependent-proof fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that Kappa should say more clearly whether basic sequence/cardinality helpers such as
    `length` are part of the guaranteed `std.prelude` surface or remain collection-specific imports.

- `Idris2#356`
  - spec-clarity evidence around quantity checking on compile-time alias parameters rather than a direct fixture for
    now. It now feeds a `SPEC_ADDITIONS.md` note that transparent type aliases and nested type formers must not change
    how quantity checking counts uses of compile-time binders such as `(1 A : Type)` or `(0 A : Type)`.

- `Idris2#360`
  - better tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later mutual-interface / dictionary-recursion totality read.
    The transferable concern is real, but it belongs with the next totality tranche rather than as a separate
    dependent-proof fixture here.

- `Idris2#361`
  - future policy-surface evidence rather than a direct current-spec fixture. Kappa does not currently standardize
    Idris-style source directives such as `%default total`, but this is useful evidence for a future rule that any
    source-level checking-policy directives should scope forward lexically rather than retroactively rewriting the
    checking mode of earlier declarations.

- `Idris2#364`
  - direct current-spec fit. It is now staged as `expressions.implicit_parameters.positive_trait_member_chain_resolves_from_result_type`.

- `Idris2#367`
  - direct current-spec fit. It is now staged as
    `types.propositions.positive_parenthesized_equality_operator_in_type_positions`.

- `Idris2#371`
  - useful future ergonomics evidence rather than a current-spec direct fixture. Kappa deliberately does not support
    ordinary default implicit parameters today, but this issue strengthens the existing `SPEC_ADDITIONS.md` note for an
    optional/default implicit-argument surface if that feature is ever added.

- `Idris2#374`
  - better tracked in `TOTALITY_ISSUE_CANDIDATES.md` as later well-founded-relation / implicit-resolution evidence for
  the standard `WellFounded` surface rather than as a separate dependent-proof fixture here.

- `Idris2#377`
  - installation / bootstrap background only. It is an OpenBSD Racket bootstrap failure rather than a language,
    elaboration, or tooling-semantics candidate for Kappa.

- `Idris2#378`
  - useful spec-clarity evidence rather than a direct fixture for now. It now feeds a `SPEC_ADDITIONS.md` note that
    dependent signature elaboration should solve straightforward index constraints induced by earlier binders and
    transparent family applications.

- `Idris2#394`
  - useful tooling-protocol evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that any
    machine-readable interactive/compiler-protocol channel must remain protocol-framed on EOF, interruption, and similar
    shutdown paths rather than printing ad hoc human text on the protocol stream.

- `Idris2#395`
  - useful suspension/elaboration evidence rather than a direct fixture. Kappa keeps laziness explicit today, but this
    issue now feeds a `SPEC_ADDITIONS.md` note that any future broadening of expected-type-directed suspension insertion
    must wait for ordinary inference/constraint solving rather than forcing `Thunk` / `Need` mismatches too early.

- `Idris2#397`
  - useful future transform-surface evidence rather than a current direct fixture. Kappa does not currently specify
    Idris-style `%transform` rules, but this issue now feeds a `SPEC_ADDITIONS.md` note that any future source-level
    rewrite/optimization-rule surface must preserve nested left-hand-side binder scope into the replacement template.

- `Idris2#398`
  - useful future transform-surface evidence rather than a current direct fixture. It now feeds the same
    `SPEC_ADDITIONS.md` note as `Idris2#397`, with extra emphasis that explicit implicit instantiations and obviously
    determined left-hand-side variables must unify canonically enough for the right-hand side to refer to them without
    accidental metavariable skew.

- `Idris2#399`
  - Windows/msys2 test-harness cleanup background only. It is useful repository/runtime evidence, but not a direct
    source-semantics or standardized tooling-surface requirement for Kappa.

- `Idris2#403`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a direct totality / helper-lowering fit; keep it cross-linked
    there rather than duplicating the staging logic in this note.

- `Idris2#405`
  - REPL command-parser / documentation-sync background only. It is useful implementation hygiene evidence, but Kappa
    does not currently standardize an Idris-style command REPL surface closely enough to justify a separate conformance
    note here.

- `Idris2#407`
  - useful hole-goal / tooling-rendering evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    that hole-goal and semantic-query output should render stable user-oriented names for generated branch helpers
    instead of leaking random compiler-generated helper identifiers that change with unrelated declaration order.

- `Idris2#409`
  - direct current-spec fit. It is now staged as
    `declarations.signatures.positive_implicit_generalization_inside_type_level_match`.

- `Idris2#413`
  - custom-codegen / build background only. It is useful backend packaging evidence, but not a direct Kappa source,
    elaboration, or standardized tooling requirement.

- `Idris2#421`
  - already covered in `tests/Kappa.Compiler.Tests/Fixtures/types.records.update.positive_implicit_compile_time_field_repair`.
    Kappa already specifies that implicit record fields may be updated with `@label = expr` and that omitted dependent
    fields are copied and rechecked under the repaired witness.

- `Idris2#423`
  - prebuilt-package REPL loading background only. It is useful implementation hygiene evidence, but Kappa does not
    currently standardize an Idris-style package REPL boot mode closely enough to justify a new conformance note here.

- `Idris2#429`
  - already covered in `tests/Kappa.Compiler.Tests/Fixtures/types.records.update.negative_stale_implicit_compile_time_dependent_field`.
    Kappa already requires record-update repair failure when a changed implicit witness would leave an omitted copied
    payload field stale.

- `Idris2#430`
  - useful coverage-checking evidence rather than a compact direct fixture with today's surface. It now feeds a
    `SPEC_ADDITIONS.md` note that coverage checking over transparent dependent packages / witness-carrying records
    should respect the finite witness family instead of inventing impossible residual payload shapes.

- `Idris2#440`
  - useful future clause-head-pattern evidence rather than a current direct fixture. Kappa does not currently expose
    Idris-style multi-equation function clauses with refutable literal patterns in parameter position, but this issue
    now feeds a `SPEC_ADDITIONS.md` note that any future clause-head pattern sugar must preserve the same linearity
    accounting as the equivalent explicit `match`.

- `Idris2#441`
  - direct current-spec fit. It is now staged as
    `types.records.field_projection.positive_parameter_name_does_not_capture_field_selector`.

- `Idris2#443`
  - useful semantic-reflection evidence rather than a separate direct fixture. It strengthens the existing
    `SPEC_ADDITIONS.md` note that semantically equivalent reflection results must reify canonically and remain stable
    across module boundaries, not only within one source file.

- `Idris2#446`
  - backend/library cleanup background only. It is useful evidence for ongoing `%foreign` migration and backend parity,
    but not a direct Kappa source-level or standardized tooling requirement.

- `Idris2#447`
  - runtime/backend evidence rather than a new direct fixture. It strengthens the existing `SPEC_ADDITIONS.md` note
    about stack-safe eager aggregate helpers and backend realizations of standard-library folds.

- `Idris2#454`
  - useful CLI/harness robustness evidence rather than a direct fixture. It strengthens the existing
    `SPEC_ADDITIONS.md` note that unsupported or invalid tooling inputs should fail deterministically with ordinary
    diagnostics instead of crashing.

- `Idris2#458`
  - better tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a totality-checker termination / assertion-safety fit; the
    transferable concern is that a recursively defined term used in types must still fail deterministically instead of
    making elaboration diverge just because it was marked total.

- `Idris2#460`
  - backend feature / runtime-portability background only. It is useful implementation pressure, but not a current
    Kappa source-level conformance fixture.

- `Idris2#461`
  - useful future ergonomics evidence for any later composition-helper or composition-operator surface. The
    transferable concern is that eta-equivalent forms should continue to type the same even when the intermediate
    function consumes an erased, irrelevant, or uninhabited input, but Kappa does not currently standardize an exact
    analogue of Idris's dot-composition surface.

- `Idris2#468`
  - useful tooling/documentation-surface evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that any future doc-comment attachment rules must behave consistently around visibility/export modifiers across
    functions, type constructors, and nested constructors.

- `Idris2#470`
  - compiler-internals / package-loading background only. Importing Idris compiler modules to build a backend is not a
    direct analogue of any current Kappa source-level conformance rule.

- `Idris2#471`
  - useful future clause/elaboration ergonomics evidence rather than a direct fixture. Kappa does not currently promise
    Idris-style availability of signature-bound explicit names inside separate function clauses, but this issue is good
    pressure for any later clause-style or signature-directed binder-reuse surface.

- `Idris2#472`
  - useful documentation-surface evidence rather than a direct fixture. It now feeds `SPEC_ADDITIONS.md` notes that a
    future Kappa comment/doc-comment surface should both document the basic spellings plainly and specify deterministic
    attachment around declaration modifiers.

- `Idris2#475`
  - useful backend/build-artifact evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note for a
    possible backend library-artifact generation mode that does not require a source-level `main`.

- `Idris2#486`
  - useful numeric-portability evidence rather than a direct fixture. Kappa currently leaves ordinary `Int` overflow and
    representation backend-dependent, but this issue is good pressure to specify any future exact-width signed integer
    surface coherently across literal conversion, shifts, and primitive arithmetic.

- `Idris2#489`
  - useful future Unicode-surface evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    any expansion of Unicode support should treat source identifiers/operators and runtime text classification as one
    coherent policy rather than as unrelated backend quirks.

- `Idris2#490`
  - useful future pattern-surface evidence rather than a direct fixture. Kappa does not currently standardize
    Idris-style as-patterns, but this issue is good pressure that any future pattern-alias surface must preserve binder
    quantity/relevance instead of implicitly forcing introduced binders to quantity `0`.

- `Idris2#493`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` and already covered by the staged
    `declarations.totality.positive_let_alias_preserves_decrease_evidence` fixture. Keep it cross-linked there rather
    than duplicating the totality staging logic in this note.

- `Idris2#495`
  - useful future pattern/implicit-syntax evidence rather than a direct fixture. Kappa permits infix constructor
    patterns, but it does not currently standardize a surface for explicitly introducing hidden constructor binders
    there, so this is better preserved as a future ergonomic requirement.

- `Idris2#505`
  - hello-world build-output background only. It is useful packaging/CLI evidence, but it does not map to a
    language-spec or dependent-elaboration conformance rule.

- `Idris2#510`
  - useful reflection-API robustness evidence rather than a new direct fixture. Kappa already has a reflection
    conformance note for canonical reification and stable semantic reflection; this issue strengthens the same family
    from the lvalue/unquote side.

- `Idris2#511`
  - installation / package-environment background only.

- `Idris2#512`
  - underscore-generalization background only for this wave. Kappa does not currently standardize Idris-style `_` use in
  recursive type arguments as a direct source-level generalization surface, so this is not a clean current-spec fit.

- `Idris2#514`
  - REPL / dynamic-library path background only. It is useful CLI/package-environment evidence, but it does not map to a
    language-spec or dependent-elaboration conformance rule.

- `Idris2#517`
  - useful reflection-API robustness evidence rather than a new direct fixture. It strengthens the same reflection note
    as `Idris2#510`, this time from a misleading linearly-checking path during quote/unquote elaboration.

- `Idris2#524`
  - positivity / totality soundness evidence. It belongs in `TOTALITY_ISSUE_CANDIDATES.md` rather than this note,
    because the transferable Kappa obligation is that non-strictly-positive families must not re-enter the trusted
    totality fragment through ordinary recursion.

- `Idris2#528`
  - diagnostic-quality evidence rather than a direct fixture. It now strengthens the existing `SPEC_ADDITIONS.md` note
    that hidden-structure unification failures must expose a real explanation instead of collapsing to an empty or
    content-free fallback.

- `Idris2#529`
  - installation / documentation background only.

- `Idris2#532`
  - parser-performance background only in this wave. It is useful implementation pressure if Kappa later standardizes a
    literate or heavily parenthesized proof/program surface, but it is not a compact current-spec conformance item.

- `Idris2#539`
  - useful future ergonomics evidence for the existing composition-helper / composition-operator note. It adds the
    accessibility/generalization angle to the same core requirement that helper syntax not be stricter than the
    eta-expanded ordinary lambda form.

- `Idris2#542`
  - useful future literate-source semantics evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that any standardized literate format must specify executable versus non-executable regions precisely and match
    the compiler's real behavior.

- `Idris2#544`
  - useful future elaboration-automation evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about a controlled recovery/backtracking surface for `Elab`.

- `Idris2#545`
  - useful numeric-portability evidence rather than a direct fixture. It now strengthens the existing exact-width
    integer semantics note with cast/widening-conversion pressure.

- `Idris2#546`
  - admin/FAQ background only. The underlying ambiguity is useful compiler ergonomics evidence, but this issue body is
    a help request rather than a compact transferable Kappa conformance case.

- `Idris2#549`
  - backend/FFI exact-width runtime background only for this wave. It is good pressure on the existing numeric/ABI notes,
    but it is primarily a backend foreign-boundary bug rather than a compact current-spec conformance fixture.

- `Idris2#555`
  - already covered by the current spec and fixture suite. Kappa already standardizes prefixed-string interpolation,
    `${...}` / `$name` syntax, raw interpolated strings, and elaboration through `InterpolatedMacro`.

- `Idris2#556`
  - useful future ergonomics evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note about allowing `_` as an explicit unnamed binder in function and Pi types with quantity
    annotations.

- `Idris2#557`
  - useful helper-lowering / erased-binder-preservation evidence rather than a direct fixture. It now strengthens the
    existing `SPEC_ADDITIONS.md` note that elaboration-introduced helpers must preserve binder quantities and linearity
    obligations.

- `Idris2#566`
  - tooling/case-split evidence rather than a direct fixture. It now strengthens the existing `SPEC_ADDITIONS.md` note
    that generated edits and clauses must use unique, reloadable generated names and holes.

- `Idris2#567`
  - helper-lowering / generated-name tooling evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that lowered helper names shown to users must be stable and non-colliding.

- `Idris2#582`
  - tooling/elaboration evidence rather than a direct fixture. It now strengthens the existing `SPEC_ADDITIONS.md`
    note that hole-goal reporting and placeholder elaboration should preserve the expected higher-order type,
    including erased binders, instead of failing first with an inaccessible-name artifact.

- `Idris2#586`
  - useful declaration-group elaboration evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that mutual/local recursive groups must preserve binder quantities and relevance
    obligations independent of declaration order.

- `Idris2#587`
  - backend line-reading background only for this wave. It usefully reinforces the existing portable line-reading
    semantics note, but it is not a compact new source-level Kappa fixture.

- `Idris2#588`
  - useful nested-declaration scoping evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about future local type/data declarations establishing scope deterministically before sibling local terms elaborate.

- `Idris2#591`
  - performance background only for this wave. It is a reminder that string/list conversion helpers should be
    implemented efficiently, but it does not add a new source-level Kappa conformance rule.

- `Idris2#592`
  - useful numeric-library ergonomics evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about whether the portable numeric surface should standardize integer exponentiation and its overflow/domain rules.

- `Idris2#596`
  - backend/runtime-library evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that basic string/character sequence conversion helpers should be stack-safe and usable on
    large inputs across backends.

- `Idris2#597`
  - useful proof-library / erasure-policy evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that standard constructor-injectivity helpers should erase the constructor arguments they only mention in the
    result proof.

- `Idris2#599`
  - syntax/background only for this wave. Kappa already specifies its own declaration/modifier grammar, so Idris's
    same-line `export public interface` preference is not a compelling direct analogue here.

- `Idris2#600`
  - admin/CI background only. It is good project-health evidence, but not a language or compiler-semantics candidate.

- `Idris2#605`
  - already-covered binder-hygiene / alpha-separation evidence rather than a new fixture. The current staged suite
    already covers the transferable Kappa requirement through `expressions.implicit_parameters.positive_alias_implicit_binder_avoids_signature_name_clash`
    (`Idris2#225`), `definitional_equality.positive_alpha_renamed_if_codomains_equal` (`Idris2#230`), and
    `types.records.field_projection.positive_parameter_name_does_not_capture_field_selector` (`Idris2#441`).

- `Idris2#606`
  - useful implicit-search semantics evidence rather than a current direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note about whether ordinary implicit proof goals over transparent total computations should normalize before search,
    beyond Kappa's current explicitly specified boolean/equality special cases in §7.3.3.

- `Idris2#613`
  - useful linear-resource API design evidence rather than a current direct fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that any future linear array / buffer builder surface should return owned resources only
    through an explicit scoped or resource-threaded form, not through an unrestricted callback result that can smuggle
    the linear value out of its intended scope.

- `Idris2#616`
  - syntax / namespaced-do background only for this wave. Kappa does not currently specify Idris-style qualified `do`
    syntax or namespace-selected bind operators, so this is not a direct current-spec analogue.

- `Idris2#620`
  - syntax/background only for this wave. Kappa's trait/declaration grammar does not use Idris's empty-interface
    trailing-`where` surface, so this is not a compelling direct import.

- `Idris2#622`
  - useful helper-lowering ergonomics evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about whether any future `with`-style sugar should support telescopic abstraction over several linked witnesses or
    intermediate expressions in one step.

- `Idris2#623`
  - useful floating-point portability evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about whether portable `Float` / `Double` arithmetic must round to binary64 at each operation boundary rather than
    depending on excess-precision backend accidents.

- `Idris2#625`
  - useful reflection-API parity evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md`
    note that public reflection surfaces should stay aligned with the full standardized source-language construct set
    instead of lagging behind newer declaration or FFI forms.

- `Idris2#626`
  - syntax / ergonomic background only for this wave. Kappa already has its own record literal/update syntax and does
    not need to import Idris's proposed implied-constructor `.{ ... }` surface directly.

- `Idris2#627`
  - useful elaboration-environment stability evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that `Elab` declarations should extend the local environment without perturbing unrelated later type inference
    or causing stray unsolved constraints in surrounding code.

- `Idris2#629`
  - useful range-library evidence rather than a direct fixture. It now strengthens the existing `SPEC_ADDITIONS.md`
    note that the portable range surface should say more clearly which scalar types have standard `Rangeable` support
    and which named constructors/helpers are guaranteed portably.

- `Idris2#632`
  - namespace/bootstrap ambiguity background only for this wave. Kappa already specifies explicit import kind selectors,
    lexical shadowing, and stable name-resolution rules; this Idris namespace-migration regression is not a strong new
    direct analogue.

- `Idris2#633`
  - useful function-pattern soundness evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    that any future function-valued pattern surface must not treat lambda bodies as constructor-like discriminators for
    contradiction or coverage reasoning.

- `Idris2#634`
  - useful tooling/case-split-generation evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that generated case splits should preserve the actual constructor spelling instead of rewriting a user-defined
    constructor into unrelated tuple/list sugar that changes the scrutinee type.

- `Idris2#636`
  - syntax/background only for this wave. Kappa's dependent package and record surfaces do not use Idris `(**)` syntax,
    so this parenthesization regression is not a direct current-spec analogue.

- `Idris2#637`
  - useful helper-lowering / layout-consistency evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that any future `with`-style sugar should have one context-independent layout rule instead
    of behaving differently between top-level and nested scopes.

- `Idris2#639`
  - install/tooling background only for this wave. It is useful project ergonomics evidence, but not a language or
    compiler-semantics candidate.

- `Idris2#642`
  - syntax / RFC background only for now. Kappa already has distinct named-argument, implicit-argument, and
    record-patch surfaces, so this Idris proposal to fuse them is not a direct current-spec import.

- `Idris2#643`
  - useful backend-lowering hygiene evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    generated temporaries introduced during backend lowering must remain hygienically fresh and non-capturing.

- `Idris2#645`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later totality-checker performance / pessimism read. Keep it
    visible there rather than duplicating totality-wave reasoning in this note.

- `Idris2#649`
  - syntax / elaboration-internals background only for this wave. It depends on Idris dot-pattern machinery that Kappa
    does not currently expose directly, so this is not a clean current-spec analogue.

- `Idris2#650`
  - useful type-object reduction evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    any future compile-time branching/classification over type objects should treat built-in and user-defined type
    constructors uniformly.

- `Idris2#651`
  - useful tooling/query-surface evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    reserved and built-in names should remain queriable uniformly through help/documentation surfaces.

- `Idris2#653`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later helper-lowering / nontermination-performance read.
    Keep it visible there rather than duplicating totality-wave reasoning in this note.

- `Idris2#654`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later partiality / helper-lowering policy read. Keep it
    visible there rather than duplicating totality-wave reasoning in this note.

- `Idris2#660`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later positivity-check precision read. Keep it visible
    there rather than duplicating totality-wave reasoning in this note.

- `Idris2#664`
  - useful future composition-surface evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that any future composition helper/operator must remain eta-equivalent to the explicit
    lambda form even when relevance-sensitive wrappers and dependent proof fields participate.

- `Idris2#666`
  - installation / bootstrap-tooling background only for this wave.

- `Idris2#672`
  - extension-gating / elaborator-entry background only for this wave. Kappa does not currently use Idris-style
    per-module language directives to enable `Elab`, so this is not a direct current-spec fixture.

- `Idris2#676`
  - useful future applicative-sugar evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    that if Kappa later adds idiom-bracket or equivalent applicative-expression sugar, tuple/pair spellings and
    explicit constructor spellings must elaborate uniformly.

- `Idris2#677`
  - useful future pattern-lambda hygiene evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note for anonymous pattern-lambda shorthand so constructor names like `refl` remain
    constructors rather than silently becoming binder names.

- `Idris2#685`
  - useful package/import-order stability evidence rather than a direct fixture. It now strengthens the existing
    `SPEC_ADDITIONS.md` note that interface-visible artifact identity and library selection should not depend on
    accidental package ordering.

- `Idris2#686`
  - useful build-policy / warning-surface evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note about configurable warnings or hard errors for residual holes and explicitly partial declarations.

- `Idris2#698`
  - JavaScript-backend `Bool` rendering bug only. Useful backend conformance pressure, but not a current Kappa
    source-level dependent-proof or elaboration fixture.

- `Idris2#704`
  - useful warning-policy evidence rather than a direct fixture. Kappa's lexical shadowing rules are already explicit,
    but this issue is a good reminder that an optional suspicious-shadowing lint for top-level names could still be
    valuable.

- `Idris2#705`
  - Idris clause-head matching on type arguments is not a clean current Kappa analogue. It is better treated as
    type-object / clause-lowering background than forced into an uncertain direct fixture.

- `Idris2#710`
  - namespace/name-resolution background only for this wave. Kappa already specifies lowercase-friendly lexical names,
    explicit imports, and stable dotted lookup, so this Idris namespace regression is not a strong new conformance
    obligation.

- `Idris2#723`
  - useful future declaration-group evidence rather than a direct current fixture. If Kappa later broadens mutual
    recursive groups to include richer trait/interface-style declarations, later members in the same SCC should already
    be visible while earlier member signatures are checked.

- `Idris2#724`
  - optimization / backend performance background only. It is useful implementation pressure, not a source-level
    dependent-proof requirement.

- `Idris2#725`
  - already covered in simpler Kappa form by the existing explicit-implicit application negatives. Bogus explicit
    implicit arguments are already rejected rather than silently treated as ignorable holes.

- `Idris2#728`
  - already covered in simpler Kappa form by the existing explicit-implicit application negatives. Bogus named or
    explicit implicit arguments in patterns/applications are already rejected rather than silently treated as wildcard
    holes.

- `Idris2#732`
  - useful hole-lowering hygiene / tooling evidence rather than a direct current-spec fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that holes inside helper-lowered forms must remain distinct per source site instead of
    colliding under generated helper names.

- `Idris2#735`
  - future descriptor-identity / first-class trait-object background only for now. Current Kappa does not pattern match
    on Idris-style interface descriptors as first-class values, so this is not yet a direct conformance fixture.

- `Idris2#737`
  - useful installation-layout evidence rather than a direct language fixture. It now feeds a `SPEC_ADDITIONS.md` note
    about separating readonly toolchain prefixes from writable user package roots in immutable installations.

- `Idris2#738`
  - useful proof-search scoping evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md` note
    that any future hint surface must respect lexical scope and private visibility instead of leaking globally.

- `Idris2#742`
  - installation / runtime-toolchain background only for now. It is useful deployment evidence, but not a current
    language or compiler-semantics fixture.

- `Idris2#745`
  - useful declaration-group scope evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md`
    note that constructors introduced by one member of a mutual group must already be visible in sibling signatures
    checked inside that same SCC.

- `Idris2#751`
  - useful future `with`-lowering / later-case-split evidence rather than a direct fixture. It now strengthens the
    `SPEC_ADDITIONS.md` note that any future `with`-style sugar must preserve the same later refinement and case-split
    behavior as the equivalent explicit nested matches on the original indexed scrutinee.

- `Idris2#759`
  - useful Unicode-display evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md` note that
    diagnostics and REPL displays should render Unicode text with one stable visible/canonical escaping policy instead
    of ad hoc decimal escapes.

- `Idris2#762`
  - useful future forced-pattern requirement rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    any future forced/inaccessible-pattern surface should apply saturation checks according to whether the position is
    actually discriminating.

- `Idris2#77`
  - name-resolution ambiguity / build tooling background only in this note. Kappa already specifies ordinary lexical
    shadowing and ambiguity diagnostics, so this is not the best place to stage another import/name-resolution fixture.

- `Idris2#95`
  - incremental-build / interface-hash background for current v0.1, but it now feeds an explicit semantic-hash
    stability note in `SPEC_ADDITIONS.md`.

- `Idris2#97`
  - IDE-mode protocol / documentation-sync background only.

- `Idris2#98`
  - macOS build / Chez bootstrap background only.

- `Idris2#99`
  - self-hosted install / packaging background only.

- `Idris2#101`
  - incremental-build / resolved-name hashing background for current v0.1, but it now feeds an explicit semantic-hash
    stability note in `SPEC_ADDITIONS.md`.

- `Idris2#104`
  - command-line `--find-ipkg` / parse-error tooling background only.

- `Idris2#108`
  - Fedora/Chez bootstrap installation background only.

- `Idris2#114`
  - backend specialization / optimizer background for current v0.1, but it now feeds an explicit future optimizer-conformance note in `SPEC_ADDITIONS.md` for representation-class specialization across single-constructor wrappers.

- `Idris2#125`
  - bootstrap-build versus bootstrap-test target separation background only. It is useful for repository automation, but it does not currently imply a Kappa language, elaboration, or tooling-surface requirement.

- `Idris2#132`
  - eager-prelude aggregate performance background for current v0.1, but it now feeds an explicit future note in
    `SPEC_ADDITIONS.md` about stack-safe eager aggregate helpers such as `sum` and `product`.

- `Idris2#135`
  - IntelliJ syntax-highlighting / editor-integration background only. It is useful tooling ecosystem evidence, but it
    does not currently imply a Kappa language, elaboration, or standardized semantic-query requirement.

- `Idris2#140`
  - strong evidence for Kappa's existing provider-collision rule rather than a new feature request. The transferable
    concern is that two providers of the same effective module name must be rejected at compile time rather than being
    allowed to link into a runtime mismatch, but staging that exactly will likely require a heavier package/provider
    harness setup than the current `new-tests/` tranche.

- `Idris2#141`
  - Windows runtime-library / installation background only.

- `Idris2#163`
  - better tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a totality-checker termination / performance fit; keep it
    cross-linked there rather than duplicating the staging logic in this dependent/elaboration note.

- `Idris2#166`
  - bootstrap C-support rename / build-compatibility background only. It is useful CI/build evidence, but it does not
    currently imply a Kappa language, elaboration, or tooling-surface requirement.

- `Idris2#768`
  - useful tooling-protocol evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md` note that
    machine-readable interactive/compiler protocol channels should return structured typed payloads for semantic queries
    and generated edits rather than formatted strings that clients must scrape.

- `Idris2#769`
  - useful semantic-highlighting / editor-query evidence rather than a direct fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that any standardized semantic-token or highlighting surface should classify repeated
    occurrences of the same resolved symbol consistently across declaration heads and later clause bodies.

- `Idris2#770`
  - websocket/library-usage background only. It is useful standard-library ecosystem evidence, but it does not
    currently imply a direct Kappa language, elaboration, or tooling-surface requirement.

- `Idris2#773`
  - useful generated-edit tooling evidence rather than a direct fixture. It now strengthens the `SPEC_ADDITIONS.md`
    note that add-clause/case-split-style editor actions must target the correct declaration even inside mixed
    mutual/interface blocks instead of emitting a valid clause for the wrong definition.

- `Idris2#775`
  - useful numeric/runtime-conversion evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    that runtime conversion from signed integers into `Nat` must have one explicit nonnegative-result rule shared across
    backends.

- `Idris2#776`
  - useful future proof-sugar evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that any
    future source-level `rewrite`-by-equality surface should judge rewrite progress after ordinary normalization rather
    than by raw pre-normalization surface syntax alone.

- `Idris2#777`
  - useful future trait-header evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note about
    explicit evidence-binder / telescopic trait headers for certified-wrapper style interfaces.

- `Idris2#778`
  - useful certified-trait / proof-hypothesis evidence rather than a direct fixture. It now strengthens the
    `SPEC_ADDITIONS.md` note that future evidence-binder / telescopic trait headers must let proof hypotheses involving
    default boolean-valued members normalize against the chosen superclass witness inside certified-wrapper members.

- `Idris2#779`
  - useful cross-module constructor-identity evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that forward declarations, split definition modules, and re-exports must not make constructor pattern heads
    resolve as linear/local names or other non-constructor entities.

- `Idris2#783`
  - useful instance-resolution performance evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that repeated structural trait obligations should avoid pathological duplicate search work on obvious
    subgoals.

- `Idris2#784`
  - useful diagnostic-range evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    declaration-level diagnostics should keep their primary source span anchored to the user-written declaration being
    rejected.

- `Idris2#785`
  - useful literate interactive-tooling evidence rather than a direct fixture. It now strengthens the
    `SPEC_ADDITIONS.md` note that any standardized literate-source format must keep interactive tooling aligned with the
    same visible-code classification that batch checking uses.

- `Idris2#788`
  - useful literate-source semantics evidence rather than a direct fixture. It now further strengthens the
    `SPEC_ADDITIONS.md` note that any standardized literate-source format must say exactly which marker families are
    executable code and which are comments/documentation only.

- `Idris2#791`
  - useful interactive-evaluation robustness evidence rather than a direct fixture. It now feeds a
    `SPEC_ADDITIONS.md` note that REPL / interpret evaluation should return source-level results or ordinary diagnostics
    instead of leaking elaborated internals or crashing.

- `Idris2#792`
  - useful hidden-structure diagnostic evidence rather than a direct fixture. It now strengthens the
    `SPEC_ADDITIONS.md` note that unification failures involving implicitly bound parameters must not render as a bogus
    surface `n` versus `n` mismatch.

- `Idris2#794`
  - already tracked in `TOTALITY_ISSUE_CANDIDATES.md` as a later totality / coverage-precision read around normalized
    list views and append-shaped indices; keep it cross-linked there rather than duplicating a dependent-proof fixture
    here.

- `Idris2#796`
  - useful FFI-boundary evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that
    compile-time-only type parameters and other erased elaboration arguments must not become hidden runtime foreign-call
    arguments.

- `Idris2#797`
  - useful backend/codegen stability evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note
    that equivalent pattern-dispatch clause sets should not cause order-of-magnitude generated-size or startup swings
    solely from harmless case ordering.

- `Idris2#798`
  - useful local-elaboration evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md` note that an
    explicitly ascribed block-local dependent helper should solve straightforward hidden arguments from its declared
    codomain after transparent normalization.

- `Idris2#800`
  - useful foreign-declaration ergonomics evidence rather than a direct fixture. It now feeds a `SPEC_ADDITIONS.md`
    note that any future inline foreign-declaration descriptor surface should say clearly whether imported compile-time
    string helpers are admitted or whether the argument is literal-only.

- `Idris2#801`
  - JavaScript-backend numeric-representation background only. The issue is useful as backend conformance pressure, but
    Kappa's current source semantics already distinguish `Int` from arbitrary-precision `Integer`, so this is not a new
    direct fixture or spec-gap by itself.

- `Idris2#802`
  - accepted-now direct fit. It is now staged as
    `new-tests/literals.numeric_literals.positive_integer_literals_in_array_double_context`, covering contextual
    elaboration of unsuffixed integer literals under an explicit `Array Double` type.

## Current Coverage Map

- `new-tests/expressions.implicit_parameters.positive_lambda_binds_implicit_argument_in_context`
  - source: `Idris2#14`; a lambda nested inside another expression may still introduce an explicit implicit binder and
    elaborate against a contextual higher-order function type.

- `new-tests/declarations.let_in.positive_multiline_initializer_without_ascription`
  - source: `Idris2#20`; a block-local `let` should accept a multiline continued initializer without needing a local type declaration solely to disambiguate layout.

- `new-tests/expressions.match.positive_residual_catch_all_derives_remaining_constructor`
  - source: `Idris2#27`; a residual unguarded catch-all branch after `None` may use the remaining visible `Some`-like
    constructor refinement for payload projection.

- `new-tests/declarations.let_in.positive_local_value_signature_indented_definition`
  - source: `Idris2#31`; a block-local value signature followed by an indented local definition should parse and scope as
    one local declaration sequence inside the surrounding block.

- `new-tests/queries.for_clauses.positive_range_generator_nested_list_comprehension`
  - source: `Idris2#41`; a list comprehension may iterate an inclusive integer range and yield nested list/tuple
    structure.

- `new-tests/modules.prelude.contents.positive_filter_term`
  - source: `Idris2#42`; the portable prelude exports `filter`, and an explicit import of that term supports ordinary
    list filtering.

- `new-tests/types.records.field_projection.positive_nested_field_read`
  - source: `Idris2#50`; nested record access is an ordinary dotted projection chain in Kappa rather than a special
    record-extraction form.

- `new-tests/traits.members.positive_default_member_uses_multiple_supertraits`
  - source: `Idris2#56`; a default member may refer to members from each declared supertrait under the same trait
    parameter.

- `new-tests/traits.instances.positive_multi_parameter_trait_instance_preserves_argument_order`
  - source: `Idris2#60`; a multi-parameter higher-kinded trait instance should keep its written parameter order through
    member signatures and implementation checking.

- `new-tests/expressions.lambdas.positive_lambda_body_is_match_expression`
  - source: `Idris2#62`; a lambda body may itself be a `match` expression and elaborate normally.

- `new-tests/declarations.let_in.positive_mixed_local_signature_and_inferred_binding`
  - source: `Idris2#63`; a local block may mix one signed binding and one inferred binding in the same local declaration
    cluster.

- `new-tests/traits.instances.positive_higher_kinded_instance_resolves_without_explicit_head_argument`
  - source: `Idris2#66`; a higher-kinded trait instance resolves from the demanded result type without an explicit head
    constructor argument.

- `new-tests/traits.members.positive_member_signature_accepts_implicit_parameter`
  - source: `Idris2#67`; a trait member may quantify its own implicit parameter.

- `new-tests/expressions.implicit_parameters.positive_supertrait_projection_for_weaker_helper_constraint`
  - source: `Idris2#72`; an in-scope stronger trait dictionary may satisfy a helper call that requires a declared
    supertrait, so helper functions need not receive the weaker evidence explicitly.

- `tests/Kappa.Compiler.Tests/Fixtures/types.universes.quantities.negative_function_type_quantity_equality`
  - source: `Idris2#73`; function types that differ only by binder quantity are not definitionally equal.

- `new-tests/modules.prelude.contents.positive_bytes_type`
  - source: `Idris2#78`; the portable prelude exports `Bytes` as a first-class ordinary type.

- `new-tests/expressions.patterns.positive_constructor_pattern_binder_shadows_function_name`
  - source: `Idris2#110`; a constructor-pattern binder may shadow the surrounding function name and still elaborate as
    an ordinary lexical binder.

- `new-tests/expressions.match.runtime_positive_integer_literal_cases_distinguish_values`
  - source: `Idris2#112`; integer literal case arms distinguish values by numeric meaning, including hex and octal
    spellings.

- `new-tests/effects.do_blocks.runtime_positive_final_alias_preserves_tail_expression`
  - source: `Idris2#116`; replacing the final `do` expression with a local alias and then returning that alias should
    preserve the same `IO` result type and runtime behavior.

- `new-tests/declarations.let_in.positive_local_impossible_case_preserves_coverage`
  - source: `Idris2#164`; a local signed helper may end in `case impossible` when the remaining refined index space is
    empty, and local coverage checking should accept it just as it would for a top-level helper.

- `new-tests/types.tuples.positive_quantity_annotated_function_components`
  - source: `Idris2#188`; tuple components may be quantity-annotated even when their field types are themselves function
    types.

- `new-tests/types.universes.quantities.qtt.positive_identity_preserves_linear_function_argument_quantity`
  - source: `Idris2#189`, `Idris2#670`; expected linear binder quantities on a higher-order function argument should
    survive passage through an ordinary identity wrapper before the value is consumed, including the case where `id`
    itself is used at a demanded linear-function transformer type.

- `new-tests/expressions.match.negative_indexed_missing_reachable_constructor`
  - source: `Idris2#202`; indexed refinement may remove impossible constructors, but a match that still omits another
    reachable constructor must be rejected as non-exhaustive.

- `new-tests/definitional_equality.positive_irrefutable_tuple_let_normalizes`
  - source: `Idris2#208`; definitional equality should see through an irrefutable tuple destructure when comparing two
    extensionally identical tuple-producing terms.

- `new-tests/types.universes.quantities.qtt.positive_equality_match_preserves_distinct_linear_binders`
  - source: `Idris2#214`; matching on `refl` may refine two linear binders to the same type-level value, but it must
    still allow each binder to be used exactly once as its own runtime ownership obligation.

- `new-tests/expressions.implicit_parameters.positive_alias_implicit_binder_avoids_signature_name_clash`
  - source: `Idris2#225`; an implicit binder introduced by a type alias should be alpha-renamed away from an outer
    signature binder of the same spelling rather than triggering a bogus repeated-name/non-linear-pattern failure.

- `new-tests/definitional_equality.positive_alpha_renamed_if_codomains_equal`
  - source: `Idris2#230`; alpha-renaming a dependent function binder should not change the type `(flag : Bool) -> if flag then a else b`.

- `new-tests/definitional_equality.positive_type_level_if_and_match_reduce_in_signatures`
  - source: `Idris2#243`; a type alias defined by `if` or `match` should reduce to the correct branch when a later
    signature demands the instantiated result type.

- `new-tests/declarations.let_in.positive_mixed_local_signature_and_inferred_binding`
  - source: `Idris2#63`, `Idris2#255`; a local block may contain inferred bindings both before and after a signed local
    definition without lifting the siblings to top level or requiring extra signatures.

- `tests/Kappa.Compiler.Tests/Fixtures/implicit_reject_same_scope_ambiguous_candidates`
  - source: `Idris2#270`; when one lexical implicit scope contains multiple candidates of the required type, implicit
    resolution must reject the call as ambiguous rather than arbitrarily picking one candidate or surfacing an internal
    search helper name.

- `tests/Kappa.Compiler.Tests/Fixtures/modules.files.negative_header_mismatch`
  - source: `Idris2#276`; in package mode, the module header must match the path-derived module name relative to the
    selected source root rather than the raw on-disk path used by a loader or IDE entrypoint.

- `new-tests/declarations.let_in.positive_unused_match_initializer_does_not_affect_result`
  - source: `Idris2#307`; a block-local binding whose initializer is a `match` expression may remain unused without
    poisoning the enclosing block result type or triggering a spurious unresolved placeholder.

- `new-tests/data_types.data_declarations.negative_duplicate_constructor_declaration`
  - source: `Idris2#338`; one `data` block must not declare the same constructor spelling twice, even if both
    constructor signatures would otherwise typecheck independently.

- `new-tests/expressions.implicit_parameters.positive_trait_member_chain_resolves_from_result_type`
  - source: `Idris2#364`; one ordinary trait dictionary should resolve consistently across a member chain under a
    `Trait a => a` signature when the enclosing result type is the only source of `a`.

- `new-tests/types.propositions.positive_parenthesized_equality_operator_in_type_positions`
  - source: `Idris2#367`; a parenthesized operator name such as `(=)` should be accepted as an ordinary type-level name
    in direct type arguments and eta-reduced alias bodies.

- `new-tests/expressions.patterns.positive_linear_indexed_singletons_cover_clause_heads`
  - source: `Idris2#402`; simultaneous clause-head singleton matches over two linear indexed arguments should count as
    exhaustive without demanding a residual wildcard arm.

- `new-tests/declarations.signatures.positive_implicit_generalization_inside_type_level_match`
  - source: `Idris2#409`; a free lowercase identifier first mentioned only inside a type-level `match` in a signature
    should still be implicitly universalized rather than rejected as unbound.

- `tests/Kappa.Compiler.Tests/Fixtures/types.records.update.positive_implicit_compile_time_field_repair`
  - source: `Idris2#421`; a record update may explicitly repair an implicit compile-time field, and omitted dependent
    fields are rechecked under the repaired witness.

- `tests/Kappa.Compiler.Tests/Fixtures/types.records.update.negative_stale_implicit_compile_time_dependent_field`
  - source: `Idris2#429`; changing an implicit compile-time witness without simultaneously repairing a copied dependent
    payload field must fail.

- `new-tests/types.records.field_projection.positive_parameter_name_does_not_capture_field_selector`
  - source: `Idris2#441`; a parameter name that matches a record field spelling must not interfere with ordinary
    dotted field projection from another value.

- `new-tests/definitional_equality.positive_record_update_projection_normalizes`
  - source: `Idris2#457`; ordinary record update must normalize far enough that the updated field projection is
    definitionally equal to the replacement expression without manually destructuring the original record.

- `new-tests/expressions.match.negative_impossible_does_not_mask_missing_reachable_constructor`
  - source: `Idris2#484`; a final `case impossible` must not be accepted when a reachable constructor remains
    uncovered.

- `new-tests/traits.instances.negative_local_instance_does_not_escape_block_scope`
  - source: `Idris2#496`; a local instance may resolve implicit goals inside its lexical block, but it must not remain
    available to later global implicit resolution.

- `new-tests/types.records.dependent.positive_explicit_erased_witness_pattern_on_package`
  - source: `Idris2#499`; a transparent dependent package may explicitly pattern-match an erased witness field with a
    constructor pattern, and the named witness binder remains usable as ordinary local implicit evidence.

- `new-tests/data_types.data_declarations.positive_same_spelling_type_and_constructor`
  - source: `Idris2#504`; one `data` declaration may contribute a same-spelling type facet and constructor facet, with
    type positions selecting the type and term/pattern positions selecting the constructor.

- `new-tests/declarations.let_in.positive_local_signature_equality_type_without_parentheses`
  - source: `Idris2#518`; a local signature should accept an unparenthesized equality type directly after `:` rather
    than misparsing it as a definition boundary.

- `new-tests/expressions.match.positive_nested_list_head_patterns_cover_all_pairs`
  - source: `Idris2#550`; nested list-head discrimination across two arguments should count as exhaustive once every
    real pair-shape is covered, without inventing a residual `(_ :: _) :: _` case.

- `new-tests/expressions.match.positive_large_literal_constructor_equality_refinement`
  - source: `Idris2#574`; matching on equality evidence that fixes a constructor payload to a larger Nat literal must
    still accept the sole `refl` branch instead of reporting a bogus non-covering residual path.

- `new-tests/types.records.dependent.positive_function_projection_preserves_dependent_proof_field`
  - source: `Idris2#589`; projecting a function field from a transparent dependent package and reusing it through a
    local alias must preserve the dependent proof-field type without requiring a manual ascription.

- `new-tests/types.propositions.negative_float_signed_zero_raw_bit_equality`
  - source: `Idris2#601`; raw-bit-based propositional equality on floating-point values must reject `refl` for
    `-0.0 = 0.0`.

- `new-tests/definitional_equality.positive_top_level_constant_aliases_reduce_for_refl`
  - source: `Idris2#621`; two transparent top-level constants that both reduce to the same literal remain
    definitionally equal, so `refl` is accepted at `a1 = a2`.

- `new-tests/types.records.update.negative_constructor_field_path`
  - source: `Idris2#628`; a value of an ordinary datatype with named constructor fields is not a record-update
    scrutinee, so `value.{ field = ... }` must be rejected.

- `new-tests/expressions.implicit_parameters.positive_constraint_method_inside_if_and_case`
  - source: `Idris2#647`; a higher-kinded constrained method call should solve its hidden type parameters uniformly in a
    `let`, directly in `if`, and directly in `match`.

- `new-tests/types.records.dependent.positive_erased_witness_pattern_refines_with_forcing_field`
  - source: `Idris2#661`; a transparent dependent package may match an erased singleton witness constructor explicitly
    when a non-erased field of the same package already forces that constructor shape.

- `new-tests/traits.members.positive_default_member_uses_associated_static_member`
  - source: `Idris2#657`; a default trait member should elaborate with access to the current associated static member
    and the current trait parameter.

- `new-tests/traits.members.positive_member_signature_uses_prior_trait_member_in_implicit_argument`
  - source: `Idris2#658`; a later trait member signature may mention an earlier trait member inside an implicit proof
    binder, and the instance implementation plus explicit `@refl` call should elaborate normally.

- `new-tests/declarations.let_in.positive_local_match_binder_shadows_local_definition`
  - source: `Idris2#694`; a catch-all `match` binder inside a block-local definition should shadow that local
    definition's own name instead of accidentally elaborating as a self-reference.

- `new-tests/definitional_equality.positive_local_type_helper_reduces_in_later_signature`
  - source: `Idris2#696`; a transparent local helper inside a type alias body should still reduce when a later
    signature instantiates that alias and branch checking needs the selected type family result.

- `new-tests/types.records.dependent.positive_two_packages_share_explicit_index`
  - source: `Idris2#706`; two transparent dependent packages parameterized by one explicit index should both project
    their dependent fields at that same index without introducing unrelated witnesses.

- `new-tests/lexical.comments.positive_line_comment_with_block_close_text`
  - source: `Idris2#711`; `--}` must be treated as ordinary line-comment text rather than as a malformed block-comment
    terminator.

- `new-tests/macros.elab_phase.positive_duplicate_compile_time_syntax_in_local_quote_helper`
  - source: `Idris2#714`; duplicating a compile-time `Syntax` value in a local quote helper or from a top-level
    `Syntax` constant must remain meta-level duplication only and should not trigger linear-overuse errors by itself.

- `new-tests/expressions.match.positive_indexed_witness_refines_outer_arguments`
  - source: `Idris2#733`; matching an indexed witness constructor should refine outer arguments and hidden indices
    strongly enough for branch-local equality proofs like `xs = Nil` and `ys = Nil` to close by `refl`.

- `new-tests/types.universes.quantities.qtt.negative_unrestricted_function_value_not_assignable_to_linear`
  - source: `Idris2#743`; an unrestricted function value must not be implicitly accepted at a plain definition boundary
    where the surrounding type demands a linear function.

- `tests/Kappa.Compiler.Tests/Fixtures/effects.do_blocks.positive_monadic_bind_preserves_linearity`
  - source: `Idris2#758`; extracting a linear function from a monadic bind must preserve the function binder's linear
    annotation through `do`-block elaboration.

- `new-tests/expressions.implicit_parameters.positive_pass_member_as_constrained_polymorphic_value`
  - source: `Idris2#763`; a bare overloaded trait member should pass as a higher-rank constrained function value
    without premature trait search at the passing site.

- `new-tests/effects.do_blocks.positive_do_bound_scrutinee_match_infers_type`
  - source: `Idris2#764`; a `do`-bound local should remain a valid `match` scrutinee without a redundant type
    annotation.

## Next Focused Pass

- `Idris2#803-#807`

Notes for the next pass:

- `Idris2#26` should stay visible as a spec-clarity / normalization follow-up even though it is not yet staged as a current-spec test.
- `Idris2#28` should stay visible as a nested helper-lowering / dependent-pattern background reference if a later
  Kappa analogue appears in the totality wave.
- `Idris2#36` should be easy to cross-link later if the FFI wave needs more nested-layout evidence.
- The local archive confirms the `Idris2#467-#471` slice is complete: `#468`, `#470`, and `#471` are present, while
  `#467` and `#469` are absent.
- The local archive confirms the `Idris2#484-#488` slice is complete: `#484` and `#486` are present, while `#485`,
  `#487`, and `#488` are absent.
- The local archive confirms the `Idris2#489-#493` slice is complete: `#489`, `#490`, and `#493` are present, while
  `#491-#492` are absent.
- The local archive confirms the `Idris2#494-#498` slice is complete: `#495` and `#496` are present, while `#494`,
  `#497`, and `#498` are absent.
- The local archive confirms the `Idris2#499-#503` slice is complete: `#499` is present, while `#500-#503` are absent.
- The local archive confirms the `Idris2#504-#508` slice is complete: `#504` and `#505` are present, while `#506-#508`
  are absent.
- The local archive confirms the `Idris2#509-#513` slice is complete: `#510`, `#511`, and `#512` are present, while
  `#509` and `#513` are absent.
- The local archive confirms the `Idris2#514-#518` slice is complete: `#514`, `#517`, and `#518` are present, while
  `#515-#516` are absent.
- The local archive confirms the `Idris2#524-#528` slice is complete: `#524` and `#528` are present, while `#525-#527`
  are absent.
- The local archive confirms the `Idris2#529-#533` slice is complete: `#529` and `#532` are present, while `#530-#531`
  and `#533` are absent.
- The local archive confirms the `Idris2#534-#538` slice is complete: `#536` is present, while `#534-#535` and
  `#537-#538` are absent.
- The local archive confirms the `Idris2#539-#543` slice is complete: `#539` and `#542` are present, while
  `#540-#541` and `#543` are absent.
- The local archive confirms the `Idris2#544-#548` slice is complete: `#544`, `#545`, and `#546` are present, while
  `#547-#548` are absent.
- The local archive confirms the `Idris2#549-#553` slice is complete: `#549`, `#550`, and `#553` are present, while
  `#551-#552` are absent.
- The local archive confirms the `Idris2#554-#558` slice is complete: `#555`, `#556`, and `#557` are present, while
  `#554` and `#558` are absent.
- The local archive confirms the `Idris2#564-#568` slice is complete: `#566` and `#567` are present, while
  `#564-#565` and `#568` are absent.
- The local archive confirms the `Idris2#574-#578` slice is complete: `#574` is present, while `#575-#578` are
  absent.
- The local archive confirms the `Idris2#579-#583` slice is complete: `#582` is present, while `#579-#581` and
  `#583` are absent.
- The local archive confirms the `Idris2#584-#588` slice is complete: `#586`, `#587`, and `#588` are present, while
  `#584-#585` are absent.
- The local archive confirms the `Idris2#589-#593` slice is complete: `#589`, `#591`, and `#592` are present, while
  `#590` and `#593` are absent.
- The local archive confirms the `Idris2#594-#598` slice is complete: `#596` and `#597` are present, while `#594`,
  `#595`, and `#598` are absent.
- The local archive confirms the `Idris2#599-#603` slice is complete: `#599`, `#600`, and `#601` are present, while
  `#602` and `#603` are absent.
- The local archive confirms the `Idris2#604-#608` slice is complete: `#605` and `#606` are present, while `#604`,
  `#607`, and `#608` are absent.
- The local archive confirms the `Idris2#609-#613` slice is complete: only `#613` is present, while `#609`, `#610`,
  `#611`, and `#612` are absent.
- The local archive confirms the `Idris2#624-#628` slice is complete: `#625`, `#626`, `#627`, and `#628` are present,
  while `#624` is absent.
- The local archive confirms the `Idris2#629-#633` slice is complete: `#629`, `#632`, and `#633` are present, while
  `#630` and `#631` are absent.
- The local archive confirms the `Idris2#634-#638` slice is complete: `#634`, `#636`, and `#637` are present, while
  `#635` and `#638` are absent.
- The local archive confirms the `Idris2#639-#643` slice is complete: `#639`, `#642`, and `#643` are present, while
  `#640` and `#641` are absent.
- The local archive confirms the `Idris2#644-#648` slice is complete: `#645` and `#647` are present, while `#644`,
  `#646`, and `#648` are absent.
- The local archive confirms the `Idris2#649-#653` slice is complete: `#649`, `#650`, `#651`, and `#653` are present,
  while `#652` is absent.
- The local archive confirms the `Idris2#654-#658` slice is complete: `#654`, `#657`, and `#658` are present, while
  `#655` and `#656` are absent.
- The local archive confirms the `Idris2#659-#663` slice is complete: `#660` and `#661` are present, while `#659`,
  `#662`, and `#663` are absent.
- The local archive confirms the `Idris2#664-#668` slice is complete: `#664` and `#666` are present, while `#665`,
  `#667`, and `#668` are absent.
- The local archive confirms the `Idris2#669-#673` slice is complete: `#670` and `#672` are present, while `#669`,
  `#671`, and `#673` are absent.
- The local archive confirms the `Idris2#674-#678` slice is complete: `#676` and `#677` are present, while `#674`,
  `#675`, and `#678` are absent.
- The local archive confirms the `Idris2#684-#688` slice is complete: `#685` and `#686` are present, while `#684`,
  `#687`, and `#688` are absent.
- The local archive confirms the `Idris2#694-#698` slice is complete: `#694`, `#696`, and `#698` are present, while
  `#695` and `#697` are absent.
- The local archive confirms that `Idris2#699-#703` are absent in this local snapshot; the next available local issue
  after that jump is `Idris2#704`.
- The local archive confirms the `Idris2#704-#708` slice is complete: `#704`, `#705`, and `#706` are present, while
  `#707` and `#708` are absent.
- The local archive confirms the `Idris2#709-#713` slice is complete: `#710` and `#711` are present, while `#709`,
  `#712`, and `#713` are absent.
- The local archive confirms the `Idris2#714-#718` slice is complete: only `#714` is present, while `#715`, `#716`,
  `#717`, and `#718` are absent.
- The local archive confirms that `Idris2#719-#722` are absent in this local snapshot; the next available local issue
  after that jump is `Idris2#723`.
- The local archive confirms the `Idris2#723-#727` slice is complete: `#723`, `#724`, and `#725` are present, while
  `#726` and `#727` are absent.
- The local archive confirms the `Idris2#728-#732` slice is complete: `#728` and `#732` are present, while `#729`,
  `#730`, and `#731` are absent.
- The local archive confirms the `Idris2#733-#737` slice is complete: `#733`, `#735`, and `#737` are present, while
  `#734` and `#736` are absent.
- The local archive confirms the `Idris2#738-#742` slice is complete: `#738` and `#742` are present, while `#739`,
  `#740`, and `#741` are absent.
- The local archive confirms the `Idris2#743-#747` slice is complete: `#743` and `#745` are present, while `#744`,
  `#746`, and `#747` are absent.
- The local archive confirms the `Idris2#748-#752` slice is complete: only `#751` is present, while `#748`, `#749`,
  `#750`, and `#752` are absent.
- The local archive confirms the `Idris2#758-#762` slice is complete: `#758`, `#759`, and `#762` are present, while
  `#760` and `#761` are absent.
- The local archive confirms the `Idris2#763-#767` slice is complete: `#763` and `#764` are present, while `#765`,
  `#766`, and `#767` are absent.
- The local archive confirms the `Idris2#768-#772` slice is complete: `#768`, `#769`, and `#770` are present, while
  `#771` and `#772` are absent.
- The local archive confirms the `Idris2#773-#777` slice is complete: `#773`, `#775`, `#776`, and `#777` are present,
  while `#774` is absent.
- The local archive confirms the `Idris2#778-#782` slice is complete: `#778` and `#779` are present, while `#780`,
  `#781`, and `#782` are absent.
- The local archive confirms the `Idris2#783-#787` slice is complete: `#783`, `#784`, and `#785` are present, while
  `#786` and `#787` are absent.
- The local archive confirms the `Idris2#788-#792` slice is complete: `#788`, `#791`, and `#792` are present, while
  `#789` and `#790` are absent.
- The local archive confirms the `Idris2#793-#797` slice is complete: `#794`, `#796`, and `#797` are present, while
  `#793` and `#795` are absent.
- The local archive confirms the `Idris2#798-#802` slice is complete: `#798`, `#800`, `#801`, and `#802` are present,
  while `#799` is absent.
- The next available local issue after that slice is `Idris2#803`.
